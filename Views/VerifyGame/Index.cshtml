@{
    ViewData["Title"] = "🧩 验证大闯关";
    var username = Context.Session.GetString("Username") ?? "玩家";
}

<style>
    * { box-sizing: border-box; }
    .game-container { max-width: 700px; margin: 0 auto; padding: 1rem 1.2rem 2rem; min-height: 100vh; font-family: 'Inter', -apple-system, sans-serif; }
    .game-header { text-align: center; padding: 0.5rem 0 1rem; position: relative; }
    .game-header h1 { font-size: 2.2rem; font-weight: 800; background: linear-gradient(135deg, #8B5CF6 0%, #EC4899 50%, #F59E0B 100%); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text; margin: 0; }
    .game-header .subtitle { color: rgba(255,255,255,0.15); font-size: 0.85rem; }
    .game-header .type-badge-header { display: inline-block; margin-top: 0.2rem; padding: 0.15rem 1.2rem; border-radius: 20px; background: rgba(139,92,246,0.08); color: #8B5CF6; font-size: 0.75rem; border: 1px solid rgba(139,92,246,0.04); }
    .header-particle { position: absolute; font-size: 0.6rem; opacity: 0.1; animation: floatParticle 10s infinite linear; pointer-events: none; }
    .header-particle:nth-child(1) { left: 5%; top: 10%; animation-delay: 0s; }
    .header-particle:nth-child(2) { left: 15%; top: 30%; animation-delay: 2s; }
    .header-particle:nth-child(3) { left: 85%; top: 20%; animation-delay: 4s; }
    .header-particle:nth-child(4) { left: 75%; top: 60%; animation-delay: 1s; }
    .header-particle:nth-child(5) { left: 90%; top: 80%; animation-delay: 3s; }
    @@keyframes floatParticle { 0% { transform: translateY(0) rotate(0deg); opacity: 0.05; } 50% { opacity: 0.15; } 100% { transform: translateY(-30px) rotate(180deg); opacity: 0.05; } }

    .status-bar { display: flex; justify-content: space-between; align-items: center; flex-wrap: wrap; gap: 0.3rem 1.2rem; background: rgba(255,255,255,0.02); border: 1px solid rgba(255,255,255,0.04); border-radius: 16px; padding: 0.8rem 1.5rem; margin-bottom: 1rem; }
    .status-item { color: rgba(255,255,255,0.25); font-size: 0.85rem; display: flex; align-items: center; gap: 0.3rem; }
    .status-item .value { color: #fff; font-weight: 700; font-size: 1.3rem; }
    .status-item .value.lives { color: #EC4899; font-size: 1.4rem; }
    .status-item .value.level { color: #8B5CF6; }
    .status-item .value.score { color: #F59E0B; }
    .status-item .value.combo { color: #34D399; }
    .status-item .value.passed { color: #8B5CF6; }
    .progress-bar { width: 100%; height: 4px; background: rgba(255,255,255,0.04); border-radius: 10px; margin-bottom: 1.2rem; overflow: hidden; }
    .progress-bar .fill { height: 100%; border-radius: 10px; background: linear-gradient(135deg, #8B5CF6, #EC4899); transition: width 0.5s ease; width: 0%; }

    .challenge-card { background: rgba(255,255,255,0.02); backdrop-filter: blur(20px); border: 1px solid rgba(255,255,255,0.04); border-radius: 24px; padding: 1.8rem 2rem; min-height: 320px; display: flex; flex-direction: column; align-items: center; justify-content: flex-start; position: relative; overflow: hidden; transition: all 0.3s ease; }
    .challenge-card::before { content: ''; position: absolute; top: -50%; left: -50%; width: 200%; height: 200%; background: radial-gradient(ellipse at 30% 50%, rgba(139,92,246,0.02), transparent 60%); pointer-events: none; }
    .challenge-card .type-badge { position: absolute; top: 0.8rem; right: 1rem; font-size: 0.65rem; padding: 0.15rem 0.8rem; border-radius: 20px; background: rgba(139,92,246,0.08); color: #8B5CF6; border: 1px solid rgba(139,92,246,0.04); font-weight: 500; z-index: 1; }
    .challenge-card .question-text { color: rgba(255,255,255,0.5); font-size: 1.1rem; text-align: center; line-height: 1.8; margin-bottom: 0.5rem; min-height: 40px; width: 100%; position: relative; z-index: 1; }

    .ab-display { font-size: 2.8rem; font-weight: 700; letter-spacing: 12px; color: #8B5CF6; padding: 0.5rem 0; font-family: monospace; background: rgba(255,255,255,0.02); border-radius: 12px; border: 1px solid rgba(255,255,255,0.04); margin: 0 auto 0.5rem; min-height: 70px; display: flex; align-items: center; justify-content: center; }
    .ab-status { color: rgba(255,255,255,0.2); font-size: 0.8rem; text-align: center; margin-bottom: 0.5rem; }
    .ab-status strong { color: #F59E0B; }
    .ab-history { color: rgba(255,255,255,0.12); font-size: 0.85rem; min-height: 30px; padding: 0.3rem 0.5rem; font-family: monospace; background: rgba(255,255,255,0.02); border-radius: 8px; border: 1px solid rgba(255,255,255,0.02); margin-bottom: 0.8rem; max-height: 100px; overflow-y: auto; text-align: center; line-height: 1.8; }
    .ab-history .history-item { color: rgba(255,255,255,0.3); }
    .ab-history .history-item.correct { color: #28a745; }

    .ab-keypad { display: grid; grid-template-columns: repeat(3, 1fr); gap: 8px; max-width: 260px; margin: 0 auto; }
    .ab-key-btn { padding: 0.8rem 0.5rem; border: 2px solid rgba(255,255,255,0.04); border-radius: 12px; background: rgba(255,255,255,0.03); color: rgba(255,255,255,0.6); font-size: 1.4rem; font-weight: 600; cursor: pointer; transition: all 0.2s ease; font-family: inherit; text-align: center; min-height: 52px; user-select: none; }
    .ab-key-btn:hover:not(:disabled) { transform: scale(1.04); }
    .ab-key-btn:disabled { opacity: 0.3; cursor: not-allowed; transform: none; }
    .ab-key-btn.key-backspace { background: rgba(236,72,153,0.08); color: #EC4899; border-color: rgba(236,72,153,0.08); }
    .ab-key-btn.key-backspace:hover:not(:disabled) { background: rgba(236,72,153,0.15); }
    .ab-key-btn.key-submit { background: rgba(40,167,69,0.08); color: #28a745; border-color: rgba(40,167,69,0.08); }
    .ab-key-btn.key-submit:hover:not(:disabled) { background: rgba(40,167,69,0.15); }
    .ab-key-btn.key-number:hover:not(:disabled) { background: rgba(139,92,246,0.08); border-color: rgba(139,92,246,0.15); }

    .options-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 0.6rem; width: 100%; max-width: 420px; position: relative; z-index: 1; }
    .options-grid .option-btn { padding: 0.8rem 1rem; border: 2px solid rgba(255,255,255,0.04); border-radius: 14px; background: rgba(255,255,255,0.02); color: rgba(255,255,255,0.5); font-size: 1rem; font-weight: 500; cursor: pointer; transition: all 0.3s ease; font-family: inherit; text-align: center; }
    .options-grid .option-btn:hover:not(:disabled) { border-color: rgba(139,92,246,0.2); background: rgba(139,92,246,0.04); color: #fff; transform: translateY(-2px); }
    .options-grid .option-btn:disabled { cursor: not-allowed; opacity: 0.3; }
    .options-grid .option-btn.correct { border-color: #28a745; background: rgba(40,167,69,0.08); color: #28a745; }
    .options-grid .option-btn.wrong { border-color: #dc3545; background: rgba(220,53,69,0.08); color: #dc3545; }

    .color-options-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 0.8rem; width: 100%; max-width: 400px; margin: 0 auto; position: relative; z-index: 1; }
    .color-options-grid .color-option { padding: 0.8rem 0.3rem; border: 2px solid rgba(255,255,255,0.04); border-radius: 14px; background: rgba(255,255,255,0.02); cursor: pointer; transition: all 0.3s ease; display: flex; flex-direction: column; align-items: center; gap: 6px; }
    .color-options-grid .color-option:hover:not(.disabled) { border-color: rgba(139,92,246,0.2); background: rgba(139,92,246,0.04); transform: translateY(-2px); }
    .color-options-grid .color-option.disabled { cursor: not-allowed; opacity: 0.6; }
    .color-options-grid .color-option .color-block { width: 56px; height: 56px; border-radius: 10px; border: 1px solid rgba(255,255,255,0.06); }
    .color-options-grid .color-option .color-label { color: rgba(255,255,255,0.5); font-size: 0.9rem; font-weight: 600; }
    .color-options-grid .color-option.correct { border-color: #28a745 !important; background: rgba(40,167,69,0.15) !important; }
    .color-options-grid .color-option.correct .color-label { color: #28a745 !important; }
    .color-options-grid .color-option.wrong { border-color: #dc3545 !important; background: rgba(220,53,69,0.15) !important; }
    .color-options-grid .color-option.wrong .color-label { color: #dc3545 !important; }

    .captcha-image { border-radius: 12px; border: 1px solid rgba(255,255,255,0.04); background: rgba(255,255,255,0.02); max-width: 100%; margin: 0.2rem 0 0.8rem; cursor: pointer; transition: transform 0.3s ease; }
    .captcha-image:hover { transform: scale(1.02); }
    .color-display { font-size: 2.5rem; padding: 0.3rem 1.5rem; border-radius: 12px; background: rgba(255,255,255,0.02); margin: 0.2rem 0 0.8rem; letter-spacing: 4px; }
    .display-text-container { width: 100%; display: block; margin: 0.2rem 0 0.8rem; position: relative; z-index: 1; }
    .display-text-container .display-text { font-size: 1.4rem; padding: 0.5rem 1.5rem; margin: 0.2rem 0 0.8rem; line-height: 2.2; text-align: center; font-family: 'Courier New', monospace; letter-spacing: 3px; width: 100%; }

    /* 三视图样式 */
    .view-container { display: flex; justify-content: center; gap: 1.5rem; flex-wrap: wrap; padding: 0.5rem 0; }
    .view-item { text-align: center; }
    .view-item .view-label { color: rgba(255,255,255,0.12); font-size: 0.6rem; text-transform: uppercase; letter-spacing: 0.06em; margin-bottom: 0.2rem; display: block; }
    .view-item .view-grid { display: inline-grid; gap: 3px; background: rgba(255,255,255,0.02); padding: 6px; border-radius: 8px; border: 1px solid rgba(255,255,255,0.04); }
    .view-item .view-grid .cell { width: 35px; height: 35px; border-radius: 4px; }
    .view-item .view-grid .cell.filled { background: #8B5CF6; }
    .view-item .view-grid .cell.empty { background: rgba(255,255,255,0.02); }

    .rotate-compare { display: flex; gap: 1.5rem; justify-content: center; align-items: center; flex-wrap: wrap; padding: 0.5rem 0; }
    .rotate-compare .rotate-item { text-align: center; }
    .rotate-compare .rotate-item .label { color: rgba(255,255,255,0.12); font-size: 0.6rem; text-transform: uppercase; letter-spacing: 0.06em; margin-bottom: 0.2rem; display: block; }
    .rotate-compare .rotate-item svg { border-radius: 8px; background: rgba(255,255,255,0.02); border: 1px solid rgba(255,255,255,0.04); }
    .rotate-compare .rotate-arrow { color: rgba(255,255,255,0.06); font-size: 1.5rem; }
    .rotate-angle-text { font-size: 1.2rem; font-weight: 700; color: #8B5CF6; margin-top: 0.3rem; }

    .memory-keyboard { display: grid; grid-template-columns: repeat(4, 1fr); gap: 0.5rem; max-width: 280px; margin: 0.5rem auto; width: 100%; }
    .memory-keyboard .key-btn { padding: 0.8rem 0.5rem; border: 2px solid rgba(255,255,255,0.06); border-radius: 12px; background: rgba(255,255,255,0.03); color: #fff; font-size: 1.4rem; font-weight: 600; cursor: pointer; transition: all 0.2s ease; font-family: inherit; text-align: center; }
    .memory-keyboard .key-btn:hover:not(:disabled) { background: rgba(139,92,246,0.08); border-color: rgba(139,92,246,0.15); transform: scale(1.05); }
    .memory-keyboard .key-btn:disabled { opacity: 0.3; cursor: not-allowed; transform: none; }
    .memory-keyboard .key-btn.backspace { color: #EC4899; border-color: rgba(236,72,153,0.15); font-size: 1.2rem; }
    .memory-keyboard .key-btn.backspace:hover:not(:disabled) { background: rgba(236,72,153,0.08); border-color: rgba(236,72,153,0.2); }
    .memory-input-display { text-align: center; padding: 0.5rem; min-height: 50px; font-size: 2rem; font-weight: bold; color: #8B5CF6; letter-spacing: 8px; font-family: 'Courier New', monospace; background: rgba(255,255,255,0.02); border-radius: 12px; border: 1px solid rgba(255,255,255,0.04); margin: 0.5rem auto; width: 100%; max-width: 280px; }
    .memory-controls { display: flex; gap: 0.5rem; justify-content: center; margin-top: 0.3rem; }
    .memory-controls .btn-memory { padding: 0.5rem 1.5rem; border: none; border-radius: 40px; font-weight: 600; font-size: 0.85rem; cursor: pointer; transition: all 0.3s ease; font-family: inherit; }
    .memory-controls .btn-memory.submit { background: linear-gradient(135deg, #8B5CF6, #EC4899); color: #fff; }
    .memory-controls .btn-memory.submit:hover:not(:disabled) { transform: translateY(-2px); box-shadow: 0 8px 32px rgba(108,60,225,0.2); }
    .memory-controls .btn-memory:disabled { opacity: 0.3; cursor: not-allowed; transform: none; }

    .char-btn { display: inline-block; padding: 0.3rem 0.8rem; margin: 0.1rem; border-radius: 10px; cursor: pointer; font-size: 2.4rem; font-weight: bold; font-family: monospace; transition: all 0.3s ease; user-select: none; color: #ffffff; border: 2px solid transparent; background: transparent; }
    .char-btn:hover:not(.disabled) { transform: scale(1.1); background: rgba(139,92,246,0.08); }
    .char-btn.disabled { cursor: default; opacity: 0.5; background: transparent !important; transform: none !important; }
    .char-btn.correct-answer { border-color: #28a745 !important; background: rgba(40,167,69,0.15) !important; color: #28a745 !important; }
    .char-btn.wrong-answer { border-color: #dc3545 !important; background: rgba(220,53,69,0.15) !important; color: #dc3545 !important; }

    /* 华容道 - 支持滑动 */
    .puzzle-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 4px; max-width: 240px; margin: 0 auto; background: rgba(255,255,255,0.02); padding: 6px; border-radius: 12px; border: 1px solid rgba(255,255,255,0.04); touch-action: none; user-select: none; }
    .puzzle-grid .puzzle-cell { aspect-ratio: 1; display: flex; align-items: center; justify-content: center; font-size: 1.6rem; font-weight: 700; border-radius: 8px; cursor: pointer; transition: all 0.2s ease; background: rgba(255,255,255,0.04); color: #fff; user-select: none; touch-action: none; }
    .puzzle-grid .puzzle-cell:hover:not(.empty) { background: rgba(139,92,246,0.12); transform: scale(1.04); }
    .puzzle-grid .puzzle-cell.empty { background: transparent; cursor: default; }

    .ascii-art { font-family: 'Courier New', monospace; font-size: 0.85rem; color: #8B5CF6; white-space: pre; text-align: center; line-height: 1.4; padding: 0.5rem; background: rgba(255,255,255,0.02); border-radius: 12px; border: 1px solid rgba(255,255,255,0.04); }

    .feedback { text-align: center; margin-top: 0.8rem; padding: 0.6rem 1.2rem; border-radius: 12px; font-size: 1rem; font-weight: 600; animation: fadeIn 0.4s ease; width: 100%; display: none; position: relative; z-index: 1; }
    .feedback.show { display: block; }
    .feedback.correct { background: rgba(40,167,69,0.06); color: #28a745; border: 1px solid rgba(40,167,69,0.06); }
    .feedback.wrong { background: rgba(220,53,69,0.06); color: #dc3545; border: 1px solid rgba(220,53,69,0.06); }
    .feedback .fun-message { font-weight: 400; color: rgba(255,255,255,0.3); font-size: 0.85rem; margin-top: 0.1rem; }

    .timer-section { width: 100%; margin-top: 1rem; position: relative; z-index: 1; }
    .timer-bar { width: 100%; height: 4px; background: rgba(255,255,255,0.04); border-radius: 10px; overflow: hidden; }
    .timer-bar .timer-fill { height: 100%; border-radius: 10px; background: linear-gradient(135deg, #34D399, #F59E0B); transition: width 0.3s linear; width: 100%; }
    .timer-bar .timer-fill.warning { background: linear-gradient(135deg, #F59E0B, #EC4899); }
    .timer-bar .timer-fill.danger { background: linear-gradient(135deg, #EC4899, #dc3545); }
    .timer-text { text-align: right; color: rgba(255,255,255,0.1); font-size: 0.8rem; margin-top: 0.2rem; font-weight: 500; }

    .game-over { text-align: center; padding: 1.5rem 0.5rem; width: 100%; position: relative; z-index: 1; }
    .game-over .big-icon { font-size: 4rem; margin-bottom: 0.3rem; }
    .game-over .title { color: #fff; font-size: 1.8rem; font-weight: 700; margin-bottom: 0.2rem; }
    .game-over .sub { color: rgba(255,255,255,0.2); font-size: 0.95rem; margin-bottom: 0.5rem; }
    .game-over .final-score { font-size: 3rem; font-weight: 800; color: #F59E0B; margin: 0.3rem 0; }
    .game-over .stats-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 0.4rem; max-width: 300px; margin: 0.5rem auto 1.2rem; }
    .game-over .stats-grid .stat-item { background: rgba(255,255,255,0.02); border-radius: 12px; padding: 0.5rem; border: 1px solid rgba(255,255,255,0.03); }
    .game-over .stats-grid .stat-item .stat-value { color: #fff; font-size: 1.2rem; font-weight: 700; }
    .game-over .stats-grid .stat-item .stat-label { color: rgba(255,255,255,0.15); font-size: 0.55rem; text-transform: uppercase; letter-spacing: 0.04em; }
    .btn-restart { padding: 0.8rem 3rem; border: none; border-radius: 40px; background: linear-gradient(135deg, #8B5CF6, #EC4899); color: #fff; font-weight: 600; font-size: 1rem; cursor: pointer; transition: all 0.3s ease; font-family: inherit; box-shadow: 0 4px 24px rgba(108,60,225,0.15); }
    .btn-restart:hover { transform: translateY(-3px); box-shadow: 0 8px 40px rgba(108,60,225,0.25); }

    .rank-section { margin-top: 1.5rem; padding: 1.2rem 1.5rem; background: rgba(255,255,255,0.02); border: 1px solid rgba(255,255,255,0.04); border-radius: 16px; }
    .rank-section .rank-title { color: rgba(255,255,255,0.2); font-size: 0.9rem; font-weight: 500; margin-bottom: 0.8rem; display: flex; align-items: center; gap: 0.5rem; }
    .rank-item { display: flex; align-items: center; gap: 0.6rem; padding: 0.4rem 0.6rem; border-bottom: 1px solid rgba(255,255,255,0.02); }
    .rank-item:last-child { border-bottom: none; }
    .rank-item .rank-medal { font-size: 0.9rem; min-width: 32px; text-align: center; color: rgba(255,255,255,0.15); }
    .rank-item .rank-avatar { width: 28px; height: 28px; border-radius: 50%; object-fit: cover; border: 1px solid rgba(255,255,255,0.06); flex-shrink: 0; background: rgba(255,255,255,0.03); }
    .rank-item .rank-avatar-placeholder { width: 28px; height: 28px; border-radius: 50%; background: rgba(255,255,255,0.04); display: flex; align-items: center; justify-content: center; font-size: 0.6rem; color: rgba(255,255,255,0.2); flex-shrink: 0; }
    .rank-item .rank-name { flex: 1; color: rgba(255,255,255,0.4); font-size: 0.85rem; }
    .rank-item .rank-name .me { color: #8B5CF6; font-weight: 600; }
    .rank-item .rank-score { color: #F59E0B; font-weight: 700; font-size: 0.95rem; }
    .rank-empty { text-align: center; color: rgba(255,255,255,0.08); font-size: 0.85rem; padding: 1rem 0; }

    .toast-container { position: fixed; bottom: 2rem; right: 2rem; z-index: 2000; display: flex; flex-direction: column; gap: 0.5rem; align-items: flex-end; }
    .toast-msg { padding: 0.6rem 1.5rem; border-radius: 40px; color: #fff; font-weight: 500; animation: slideUp 0.4s ease; font-size: 0.9rem; backdrop-filter: blur(10px); background: rgba(0,0,0,0.6); border: 1px solid rgba(255,255,255,0.04); }
    .toast-msg.success { background: rgba(40,167,69,0.8); }
    .toast-msg.error { background: rgba(220,53,69,0.8); }
    .toast-msg.info { background: rgba(79,172,254,0.8); }

    @@keyframes fadeIn { from { opacity: 0; transform: translateY(10px); } to { opacity: 1; transform: translateY(0); } }
    @@keyframes slideUp { from { opacity: 0; transform: translateY(20px) scale(0.95); } to { opacity: 1; transform: translateY(0) scale(1); } }
    @@keyframes shake { 0%,100% { transform: translateX(0); } 25% { transform: translateX(-8px); } 75% { transform: translateX(8px); } }
    .shake { animation: shake 0.4s ease; }

    @@media (max-width: 640px) {
        .game-header h1 { font-size: 1.6rem; }
        .challenge-card { padding: 1rem 1rem; min-height: 280px; }
        .ab-display { font-size: 2.2rem; letter-spacing: 8px; min-height: 60px; }
        .ab-keypad { max-width: 220px; gap: 6px; }
        .ab-key-btn { font-size: 1.2rem; min-height: 44px; padding: 0.6rem 0.3rem; }
        .status-bar { padding: 0.5rem 0.8rem; justify-content: center; gap: 0.2rem 0.8rem; }
        .status-item { font-size: 0.7rem; }
        .status-item .value { font-size: 1rem; }
        .status-item .value.lives { font-size: 1.1rem; }
        .game-over .final-score { font-size: 2.2rem; }
        .options-grid { grid-template-columns: 1fr 1fr; gap: 0.4rem; }
        .color-options-grid { grid-template-columns: repeat(3, 1fr); gap: 0.4rem; }
        .color-options-grid .color-option .color-block { width: 42px; height: 42px; }
        .color-options-grid .color-option .color-label { font-size: 0.8rem; }
        .ab-history { font-size: 0.75rem; max-height: 80px; }
        .char-btn { font-size: 1.8rem; padding: 0.2rem 0.5rem; }
        .puzzle-grid { max-width: 180px; }
        .puzzle-grid .puzzle-cell { font-size: 1.2rem; }
        .rotate-compare .rotate-item svg { width: 80px; height: 80px; }
        .rotate-angle-text { font-size: 1rem; }
        .view-item .view-grid .cell { width: 28px; height: 28px; }
        .view-container { gap: 0.8rem; }
    }
    @@media (max-width: 400px) {
        .game-header h1 { font-size: 1.3rem; }
        .ab-display { font-size: 1.8rem; letter-spacing: 6px; min-height: 50px; }
        .ab-keypad { max-width: 180px; gap: 4px; }
        .ab-key-btn { font-size: 1rem; min-height: 38px; padding: 0.4rem 0.2rem; }
        .status-item { font-size: 0.6rem; }
        .status-item .value { font-size: 0.85rem; }
        .status-item .value.lives { font-size: 0.9rem; }
        .ab-history { font-size: 0.65rem; max-height: 60px; }
        .char-btn { font-size: 1.4rem; padding: 0.1rem 0.4rem; }
        .puzzle-grid { max-width: 150px; }
        .puzzle-grid .puzzle-cell { font-size: 1rem; }
        .rotate-compare .rotate-item svg { width: 60px; height: 60px; }
        .rotate-compare .rotate-arrow { font-size: 1rem; }
        .rotate-angle-text { font-size: 0.9rem; }
        .view-item .view-grid .cell { width: 22px; height: 22px; }
        .view-container { gap: 0.4rem; }
    }
</style>

<div class="game-container">
    <div class="game-header">
        <span class="header-particle">✦</span>
        <span class="header-particle">●</span>
        <span class="header-particle">◆</span>
        <span class="header-particle">▲</span>
        <span class="header-particle">★</span>
        <h1>🧩 验证大闯关</h1>
        <p class="subtitle">100关 · 难度递增 · 证明你是人类</p>
        <div class="type-badge-header" id="typeNameDisplay">📝 准备开始</div>
    </div>

    <div class="status-bar" id="statusBar">
        <div class="status-item">❤️ 生命 <span class="value lives" id="livesDisplay">❤️❤️❤️</span></div>
        <div class="status-item">📊 关卡 <span class="value level" id="levelDisplay">1</span></div>
        <div class="status-item">🏆 积分 <span class="value score" id="scoreDisplay">0</span></div>
        <div class="status-item">🔥 连击 <span class="value combo" id="comboDisplay">0</span></div>
        <div class="status-item">✅ 通关 <span class="value passed" id="passedDisplay">0</span></div>
    </div>

    <div class="progress-bar"><div class="fill" id="progressFill"></div></div>

    <div class="challenge-card" id="challengeCard">
        <div class="type-badge" id="typeBadge">第 1 关</div>
        <div class="question-text" id="questionText">加载中...</div>
        <div id="gameContent" style="width:100%;"></div>
        <div class="timer-section">
            <div class="timer-bar"><div class="timer-fill" id="timerFill"></div></div>
            <div class="timer-text" id="timerText">⏱ 15s</div>
        </div>
        <div class="feedback" id="feedback"></div>
    </div>

    <div class="rank-section" id="rankSection" style="display:none;">
        <div class="rank-title">🏆 排行榜</div>
        <div id="rankList"></div>
    </div>
</div>

<div class="toast-container" id="toastContainer"></div>

<script>
    var token = document.querySelector('input[name="__RequestVerificationToken"]')?.value || '';

    var gameState = {
        level: 1, score: 0, lives: 3, combo: 0, maxCombo: 0, passed: 0,
        isPlaying: false, isAnswered: false, timer: null, timeLeft: 15,
        totalLevels: 100, currentChallenge: null,
    };

    var dom = {
        levelDisplay: document.getElementById('levelDisplay'),
        scoreDisplay: document.getElementById('scoreDisplay'),
        livesDisplay: document.getElementById('livesDisplay'),
        comboDisplay: document.getElementById('comboDisplay'),
        passedDisplay: document.getElementById('passedDisplay'),
        progressFill: document.getElementById('progressFill'),
        typeBadge: document.getElementById('typeBadge'),
        typeNameDisplay: document.getElementById('typeNameDisplay'),
        questionText: document.getElementById('questionText'),
        feedback: document.getElementById('feedback'),
        timerFill: document.getElementById('timerFill'),
        timerText: document.getElementById('timerText'),
        challengeCard: document.getElementById('challengeCard'),
        gameContent: document.getElementById('gameContent'),
        rankSection: document.getElementById('rankSection'),
        rankList: document.getElementById('rankList')
    };

    // ============================================================
    // 1A2B 游戏状态（全局）
    // ============================================================
    var abState = {
        password: '',
        digitCount: 4,
        maxAttempts: 15,
        currentGuess: '',
        attemptCount: 0,
        history: [],
        gameSolved: false,
        isActive: false
    };

    // ============================================================
    // 颜色映射表（用于颜色混合降级方案）
    // ============================================================
    var colorHexMap = {
        '红色': '#FF0000', '深红': '#8B0000', '粉色': '#FF69B4', '浅粉': '#FFB6C1',
        '蓝色': '#0000FF', '深蓝': '#00008B', '天蓝': '#87CEEB', '青色': '#00CED1',
        '绿色': '#00AA00', '深绿': '#006400', '草绿': '#7CFC00', '黄色': '#FFD700',
        '金色': '#DAA520', '橙色': '#FF6600', '深橙': '#CC5500', '紫色': '#8800CC',
        '深紫': '#4B0082', '棕色': '#8B4513', '灰色': '#808080', '黑色': '#000000',
        '白色': '#FFFFFF', '银色': '#C0C0C0', '紫罗兰': '#EE82EE', '靛蓝': '#4B0082',
        '玫瑰红': '#FF007F', '柠檬黄': '#FFF700', '薄荷绿': '#98FF98', '珊瑚橙': '#FF7F50',
        '象牙白': '#FFFFF0', '巧克力棕': '#D2691E', '琥珀金': '#FFBF00', '翠绿': '#50C878',
        '宝石蓝': '#0F52BA', '玛瑙红': '#C04000', '珍珠白': '#F5F5F5', '青蓝': '#00BFFF',
        '紫红': '#C71585', '橙红': '#FF4500', '黄绿': '#9ACD32', '蓝紫': '#8A2BE2',
        '粉红': '#FFB6C1', '米色': '#F5F5DC', '卡其': '#F0E68C', '珊瑚': '#FF7F50',
        '青绿': '#008B8B', '靛青': '#4B0082', '藏青': '#000080', '酒红': '#800020',
        '橄榄绿': '#556B2F', '石板灰': '#708090', '杏色': '#FFDAB9', '薰衣草': '#E6E6FA',
        '紫藤': '#C9A0DC', '樱花粉': '#FFB7C5', '奶油': '#FFFDD0',
        '浅绿': '#90EE90', '浅蓝': '#ADD8E6', '浅紫': '#D8BFD8', '浅黄': '#FFFFE0',
        '浅橙': '#FFDAB9', '浅灰': '#D3D3D3', '深灰': '#A9A9A9', '墨绿': '#006400',
        '海军蓝': '#000080', '克莱因蓝': '#002FA7', '蒂芙尼蓝': '#81D8D0',
        '马卡龙粉': '#FFB5C5', '马卡龙蓝': '#A7D8DE', '马卡龙黄': '#FDE8B6',
        '马卡龙紫': '#C9B1E0', '马卡龙绿': '#B5D4C5', '莫兰迪粉': '#DDB5B5',
        '莫兰迪蓝': '#A8BCCD', '莫兰迪绿': '#B5C4B5', '莫兰迪紫': '#C4B5D4',
        '荧光粉': '#FF1493', '荧光绿': '#00FF00', '荧光黄': '#CCFF00', '荧光橙': '#FF6B00',
        '暗红': '#8B1A1A', '暗蓝': '#1A2A6B', '暗绿': '#1A4A2A', '暗紫': '#4A1A6B',
        '桃色': '#FFDAB9', '珊瑚粉': '#F08080', '橙黄': '#FFA500', '柠檬': '#FFF44F',
        '海蓝': '#2E8B57', '天空': '#87CEEB', '薰衣草紫': '#E6E6FA'
    };

    function getColorHex(name) {
        return colorHexMap[name] || '#808080';
    }

    // ============================================================
    // 启动游戏
    // ============================================================
    function startGame() {
        if (gameState.timer) { clearInterval(gameState.timer); gameState.timer = null; }
        gameState.level = 1; gameState.score = 0; gameState.lives = 3;
        gameState.combo = 0; gameState.maxCombo = 0; gameState.passed = 0;
        gameState.isPlaying = true; gameState.isAnswered = false;
        gameState.timeLeft = 15;
        gameState.currentChallenge = null;
        dom.rankSection.style.display = 'none';
        dom.feedback.className = 'feedback'; dom.feedback.style.display = 'none';
        dom.feedback.innerHTML = '';
        dom.gameContent.innerHTML = '';
        updateUI();
        dom.typeNameDisplay.textContent = '📝 第 1 关 · 入门';
        loadChallenge();
    }

    // ============================================================
    // 加载挑战
    // ============================================================
    function loadChallenge() {
        if (!gameState.isPlaying) return;
        if (gameState.level > gameState.totalLevels) { gameWin(); return; }

        gameState.isAnswered = false;
        dom.feedback.className = 'feedback';
        dom.feedback.style.display = 'none';
        dom.gameContent.innerHTML = '';

        if (window._abCleanup) {
            window._abCleanup();
            window._abCleanup = null;
        }

        var diffLabel = getDifficultyLabel(gameState.level);
        dom.typeBadge.textContent = '第 ' + gameState.level + ' 关';

        fetch('/VerifyGame/GetChallenge?level=' + gameState.level)
            .then(function(r) { return r.json(); })
            .then(function(data) {
                if (data.success) {
                    gameState.currentChallenge = data.data;
                    if (data.data.typeName) {
                        dom.typeNameDisplay.textContent = '🧩 ' + data.data.typeName + ' · ' + diffLabel;
                    }
                    renderChallenge(data.data);
                } else {
                    showToast('加载挑战失败，请重试', 'error');
                }
            })
            .catch(function() {
                showToast('网络错误，请刷新重试', 'error');
            });
    }

    // ============================================================
    // 渲染挑战
    // ============================================================
    function renderChallenge(challenge) {
        gameState.currentChallenge = challenge;
        var typeName = challenge.typeName || challenge.type || '未知';
        var diffLabel = getDifficultyLabel(challenge.level);
        dom.typeBadge.textContent = '第 ' + challenge.level + ' 关 · ' + typeName + ' · ' + diffLabel;
        dom.typeNameDisplay.textContent = '🧩 ' + typeName + ' · ' + diffLabel;
        dom.questionText.innerHTML = challenge.question || '请完成以下挑战';

        var container = dom.gameContent;

        // ============================================================
        // ⭐ 1A2B 游戏
        // ============================================================
        if (challenge.type === 'abgame') {
            renderABGame(challenge);
            return;
        }

        // ============================================================
        // ⭐ 颜色混合
        // ============================================================
        if (challenge.type === 'colorMix') {
            renderColorMix(challenge);
            return;
        }

        // ============================================================
        // ⭐ 图形旋转
        // ============================================================
        if (challenge.type === 'rotate' && challenge.originalSvg) {
            var rotateDiv = document.createElement('div');
            rotateDiv.className = 'rotate-compare';
            rotateDiv.innerHTML = `
                <div class="rotate-item">
                    <span class="label">📐 原图</span>
                    ${challenge.originalSvg}
                </div>
                <div class="rotate-arrow">→</div>
                <div class="rotate-item">
                    <span class="label">🔄 旋转后</span>
                    ${challenge.rotatedSvg || challenge.originalSvg}
                </div>
            `;
            container.appendChild(rotateDiv);

            if (challenge.options && challenge.options.length > 0) {
                var optGrid = document.createElement('div');
                optGrid.className = 'options-grid';
                var letters = ['A', 'B', 'C', 'D', 'E', 'F', 'G', 'H'];
                challenge.options.forEach(function(opt, idx) {
                    var btn = document.createElement('button');
                    btn.className = 'option-btn';
                    btn.innerHTML = (idx < letters.length ? letters[idx] + '. ' : '') + '<span style="font-size:1.2rem;font-weight:700;color:#8B5CF6;">' + opt + '</span>';
                    btn.dataset.answer = opt;
                    btn.addEventListener('click', function() {
                        if (gameState.isAnswered) return;
                        submitAnswer(challenge, this.dataset.answer, this);
                    });
                    optGrid.appendChild(btn);
                });
                container.appendChild(optGrid);
            }
            startTimer(challenge.timeLimit || 15);
            updateUI();
            return;
        }

        // ============================================================
        // ⭐ 找不同
        // ============================================================
        if (challenge.type === 'findDifferent' && challenge.originalDisplay) {
            renderFindDifferent(challenge);
            return;
        }

        // ============================================================
        // ⭐ 数字记忆
        // ============================================================
        if (challenge.type === 'memory' && challenge.keyboardRows) {
            renderMemoryGame(challenge);
            return;
        }

        // ============================================================
        // ⭐ 华容道（带滑动支持）
        // ============================================================
        if (challenge.type === 'puzzle' && challenge.puzzle) {
            renderPuzzle(challenge.puzzle);
            return;
        }

        // ============================================================
        // ⭐ 三视图（完整渲染）
        // ============================================================
        if (challenge.type === 'threeView') {
            renderThreeView(challenge);
            return;
        }

        // ============================================================
        // ⭐ 其他题型（通用渲染）
        // ============================================================

        if (challenge.imageSvg) {
            var imgDiv = document.createElement('div');
            imgDiv.style.cssText = 'width:100%;text-align:center;margin:0.2rem 0 0.8rem;';
            imgDiv.innerHTML = '<div class="captcha-image">' + challenge.imageSvg + '</div>';
            container.appendChild(imgDiv);
        }

        if (challenge.displayHtml) {
            var colorDiv = document.createElement('div');
            colorDiv.className = 'color-display';
            colorDiv.innerHTML = challenge.displayHtml;
            container.appendChild(colorDiv);
        }

        if (challenge.displayText) {
            var textDiv = document.createElement('div');
            textDiv.className = 'display-text-container';
            textDiv.style.display = 'block';
            textDiv.innerHTML = '<div class="display-text">' + challenge.displayText + '</div>';
            container.appendChild(textDiv);
        }

        if (challenge.type === 'shapeCount' && challenge.asciiArt) {
            var asciiDiv = document.createElement('div');
            asciiDiv.className = 'ascii-art';
            asciiDiv.textContent = challenge.asciiArt;
            container.appendChild(asciiDiv);
        }

        if (challenge.options && challenge.options.length > 0) {
            var optGrid = document.createElement('div');
            optGrid.className = 'options-grid';
            var letters = ['A', 'B', 'C', 'D', 'E', 'F', 'G', 'H'];
            challenge.options.forEach(function(opt, idx) {
                var btn = document.createElement('button');
                btn.className = 'option-btn';
                btn.textContent = (idx < letters.length ? letters[idx] + '. ' : '') + opt;
                btn.dataset.answer = opt;
                btn.addEventListener('click', function() {
                    if (gameState.isAnswered) return;
                    submitAnswer(challenge, this.dataset.answer, this);
                });
                optGrid.appendChild(btn);
            });
            container.appendChild(optGrid);
        }

        startTimer(challenge.timeLimit || 15);
        updateUI();
    }

    // ============================================================
    // ⭐ 三视图渲染（完整版）
    // ============================================================
    function renderThreeView(challenge) {
        var container = dom.gameContent;

        // 显示三个视图
        var viewContainer = document.createElement('div');
        viewContainer.className = 'view-container';

        // 俯视图
        if (challenge.topView) {
            var topItem = document.createElement('div');
            topItem.className = 'view-item';
            topItem.innerHTML = challenge.topView;
            viewContainer.appendChild(topItem);
        }

        // 主视图
        if (challenge.frontView) {
            var frontItem = document.createElement('div');
            frontItem.className = 'view-item';
            frontItem.innerHTML = challenge.frontView;
            viewContainer.appendChild(frontItem);
        }

        // 左视图
        if (challenge.sideView) {
            var sideItem = document.createElement('div');
            sideItem.className = 'view-item';
            sideItem.innerHTML = challenge.sideView;
            viewContainer.appendChild(sideItem);
        }

        container.appendChild(viewContainer);

        // 选项
        if (challenge.options && challenge.options.length > 0) {
            var optGrid = document.createElement('div');
            optGrid.className = 'options-grid';
            var letters = ['A', 'B', 'C', 'D', 'E', 'F', 'G', 'H'];
            challenge.options.forEach(function(opt, idx) {
                var btn = document.createElement('button');
                btn.className = 'option-btn';
                btn.textContent = (idx < letters.length ? letters[idx] + '. ' : '') + opt;
                btn.dataset.answer = opt;
                btn.addEventListener('click', function() {
                    if (gameState.isAnswered) return;
                    submitAnswer(challenge, this.dataset.answer, this);
                });
                optGrid.appendChild(btn);
            });
            container.appendChild(optGrid);
        }

        startTimer(challenge.timeLimit || 20);
        updateUI();
    }

    // ============================================================
    // ⭐ 颜色混合渲染
    // ============================================================
    function renderColorMix(challenge) {
        var container = dom.gameContent;
        container.innerHTML = '';

        // 显示颜色混合展示区
        if (challenge.displayHtml) {
            var displayDiv = document.createElement('div');
            displayDiv.className = 'display-text-container';
            displayDiv.style.display = 'block';
            displayDiv.style.textAlign = 'center';
            displayDiv.style.margin = '0.2rem 0 1rem';
            displayDiv.innerHTML = challenge.displayHtml;
            container.appendChild(displayDiv);
        }

        var colorGrid = document.createElement('div');
        colorGrid.className = 'color-options-grid';

        var options = challenge.options || [];
        var correctAnswer = challenge.correctAnswer || '';

        if (challenge.optionsHtml && challenge.optionsHtml.trim().length > 0) {
            colorGrid.innerHTML = challenge.optionsHtml;
        } else if (options.length > 0) {
            var html = '';
            options.forEach(function(colorName) {
                var hex = getColorHex(colorName) || '#808080';
                var displayName = colorName.replace('色', '');
                html += '<div class="color-option" data-color="' + colorName + '">';
                html += '<div class="color-block" style="background:' + hex + ';"></div>';
                html += '<span class="color-label">' + displayName + '</span>';
                html += '</div>';
            });
            colorGrid.innerHTML = html;
        } else {
            var fallbackOptions = ['红色', '蓝色', '绿色', '黄色', '紫色', '橙色'];
            var optGrid = document.createElement('div');
            optGrid.className = 'options-grid';
            fallbackOptions.forEach(function(opt) {
                var btn = document.createElement('button');
                btn.className = 'option-btn';
                btn.textContent = opt;
                btn.dataset.answer = opt;
                btn.addEventListener('click', function() {
                    if (gameState.isAnswered) return;
                    submitAnswer(challenge, this.dataset.answer, this);
                });
                optGrid.appendChild(btn);
            });
            container.appendChild(optGrid);
            startTimer(challenge.timeLimit || 15);
            updateUI();
            return;
        }

        colorGrid.querySelectorAll('.color-option').forEach(function(el) {
            el.addEventListener('click', function() {
                if (gameState.isAnswered) return;
                var answer = this.dataset.color;
                if (answer) submitColorMixAnswer(challenge, answer, this);
            });
        });

        container.appendChild(colorGrid);
        startTimer(challenge.timeLimit || 15);
        updateUI();
    }

    // ============================================================
    // ⭐ 颜色混合提交
    // ============================================================
    function submitColorMixAnswer(challenge, answer, element) {
        if (gameState.isAnswered) return;
        var isCorrect = answer === challenge.correctAnswer;
        var container = dom.gameContent;

        container.querySelectorAll('.color-option').forEach(function(el) {
            el.classList.add('disabled');
        });

        container.querySelectorAll('.color-option').forEach(function(el) {
            if (el.dataset.color === challenge.correctAnswer) {
                el.classList.add('correct');
            }
        });

        if (!isCorrect) {
            element.classList.add('wrong');
            dom.challengeCard.classList.add('shake');
            setTimeout(function() { dom.challengeCard.classList.remove('shake'); }, 500);
        } else {
            element.classList.add('correct');
        }

        handleAnswerResult(challenge, isCorrect);
    }

    // ============================================================
    // ⭐ 华容道渲染（带滑动支持）
    // ============================================================
    var puzzleState = null;
    var puzzleEmptyRow = 0;
    var puzzleEmptyCol = 0;

    function renderPuzzle(puzzle) {
        var container = dom.gameContent;
        container.innerHTML = '';

        var hintDiv = document.createElement('div');
        hintDiv.style.cssText = 'color:rgba(255,255,255,0.08);font-size:0.7rem;text-align:center;margin-bottom:0.5rem;';
        hintDiv.textContent = '💡 点击数字 或 滑动方向 移动';
        container.appendChild(hintDiv);

        var grid = document.createElement('div');
        grid.className = 'puzzle-grid';
        grid.id = 'puzzleGrid';
        container.appendChild(grid);

        puzzleState = puzzle.map(function(row) { return row.slice(); });

        for (var r = 0; r < 3; r++) {
            for (var c = 0; c < 3; c++) {
                var cell = document.createElement('div');
                cell.className = 'puzzle-cell';
                var val = puzzle[r][c];
                if (val === 0) {
                    cell.classList.add('empty');
                    cell.textContent = '';
                    puzzleEmptyRow = r;
                    puzzleEmptyCol = c;
                } else {
                    cell.textContent = val;
                    cell.dataset.row = r;
                    cell.dataset.col = c;
                    cell.addEventListener('click', function() {
                        if (gameState.isAnswered) return;
                        var row = parseInt(this.dataset.row);
                        var col = parseInt(this.dataset.col);
                        movePuzzleTile(row, col);
                    });
                }
                grid.appendChild(cell);
            }
        }

        setupPuzzleSwipe(grid);
        startTimer(60);
    }

    function setupPuzzleSwipe(grid) {
        var startX = 0, startY = 0;
        var isDragging = false;

        grid.addEventListener('mousedown', function(e) {
            startX = e.clientX;
            startY = e.clientY;
            isDragging = true;
            e.preventDefault();
        });

        grid.addEventListener('mouseup', function(e) {
            if (!isDragging) return;
            isDragging = false;
            var deltaX = e.clientX - startX;
            var deltaY = e.clientY - startY;
            handleSwipe(deltaX, deltaY);
        });

        grid.addEventListener('mouseleave', function() {
            isDragging = false;
        });

        grid.addEventListener('touchstart', function(e) {
            var touch = e.touches[0];
            startX = touch.clientX;
            startY = touch.clientY;
            isDragging = true;
            e.preventDefault();
        }, { passive: false });

        grid.addEventListener('touchmove', function(e) {
            e.preventDefault();
        }, { passive: false });

        grid.addEventListener('touchend', function(e) {
            if (!isDragging) return;
            isDragging = false;
            var touch = e.changedTouches[0];
            var deltaX = touch.clientX - startX;
            var deltaY = touch.clientY - startY;
            handleSwipe(deltaX, deltaY);
        });
    }

    function handleSwipe(deltaX, deltaY) {
        if (gameState.isAnswered) return;
        var threshold = 20;
        if (Math.abs(deltaX) < threshold && Math.abs(deltaY) < threshold) return;

        var targetRow = puzzleEmptyRow;
        var targetCol = puzzleEmptyCol;

        if (Math.abs(deltaX) > Math.abs(deltaY)) {
            if (deltaX > 0) {
                targetCol = puzzleEmptyCol - 1;
            } else {
                targetCol = puzzleEmptyCol + 1;
            }
        } else {
            if (deltaY > 0) {
                targetRow = puzzleEmptyRow - 1;
            } else {
                targetRow = puzzleEmptyRow + 1;
            }
        }

        if (targetRow >= 0 && targetRow < 3 && targetCol >= 0 && targetCol < 3) {
            movePuzzleTile(targetRow, targetCol);
        }
    }

    function movePuzzleTile(row, col) {
        var dr = Math.abs(row - puzzleEmptyRow);
        var dc = Math.abs(col - puzzleEmptyCol);
        if ((dr === 1 && dc === 0) || (dr === 0 && dc === 1)) {
            puzzleState[puzzleEmptyRow][puzzleEmptyCol] = puzzleState[row][col];
            puzzleState[row][col] = 0;
            puzzleEmptyRow = row;
            puzzleEmptyCol = col;
            renderPuzzleState(puzzleState);
            if (checkPuzzleSolved(puzzleState)) {
                gameState.isAnswered = true;
                clearInterval(gameState.timer);
                var challenge = gameState.currentChallenge;
                handleAnswerResult(challenge, true);
            }
        }
    }

    function renderPuzzleState(puzzle) {
        var cells = document.querySelectorAll('.puzzle-grid .puzzle-cell');
        var idx = 0;
        for (var r = 0; r < 3; r++) {
            for (var c = 0; c < 3; c++) {
                var val = puzzle[r][c];
                var cell = cells[idx];
                if (val === 0) {
                    cell.classList.add('empty');
                    cell.textContent = '';
                    cell.dataset.row = r;
                    cell.dataset.col = c;
                    cell.onclick = null;
                } else {
                    cell.classList.remove('empty');
                    cell.textContent = val;
                    cell.dataset.row = r;
                    cell.dataset.col = c;
                    cell.onclick = function() {
                        if (gameState.isAnswered) return;
                        var row = parseInt(this.dataset.row);
                        var col = parseInt(this.dataset.col);
                        movePuzzleTile(row, col);
                    };
                }
                idx++;
            }
        }
    }

    function checkPuzzleSolved(puzzle) {
        var expected = [[1,2,3],[4,5,6],[7,8,0]];
        for (var r = 0; r < 3; r++) {
            for (var c = 0; c < 3; c++) {
                if (puzzle[r][c] !== expected[r][c]) return false;
            }
        }
        return true;
    }

    // ============================================================
    // ⭐ 1A2B 游戏渲染（带规则说明）
    // ============================================================
    function renderABGame(challenge) {
        var digitCount = challenge.digitCount || 4;
        var maxAttempts = challenge.maxAttempts || 15;
        var password = challenge.password || '';

        abState.password = password;
        abState.digitCount = digitCount;
        abState.maxAttempts = maxAttempts;
        abState.currentGuess = '';
        abState.attemptCount = 0;
        abState.history = [];
        abState.gameSolved = false;
        abState.isActive = true;

        var container = dom.gameContent;
        container.innerHTML = '';

        var ruleDiv = document.createElement('div');
        ruleDiv.style.cssText = 'background:rgba(139,92,246,0.04);border:1px solid rgba(139,92,246,0.06);border-radius:12px;padding:0.6rem 1rem;margin-bottom:0.6rem;width:100%;';
        ruleDiv.innerHTML = `
            <div style="color:rgba(255,255,255,0.2);font-size:0.75rem;display:flex;flex-wrap:wrap;gap:0.3rem 1rem;justify-content:center;">
                <span>🎯 猜 <strong style="color:#8B5CF6;">${digitCount}</strong> 位不重复数字（第一位不能是0）</span>
                <span style="color:rgba(255,255,255,0.05);">|</span>
                <span style="color:#28a745;">✅ <strong>A</strong> = 位置和数字都正确</span>
                <span style="color:#F59E0B;">🟡 <strong>B</strong> = 数字正确但位置不对</span>
                <span style="color:rgba(255,255,255,0.1);font-size:0.65rem;">💡 例：<span style="color:#8B5CF6;">1A2B</span></span>
            </div>
        `;
        container.appendChild(ruleDiv);

        var displayDiv = document.createElement('div');
        displayDiv.className = 'ab-display';
        displayDiv.id = 'abDisplay';
        displayDiv.textContent = ' '.repeat(digitCount).split('').join(' ');
        container.appendChild(displayDiv);

        var statusDiv = document.createElement('div');
        statusDiv.className = 'ab-status';
        statusDiv.id = 'abStatus';
        statusDiv.innerHTML = '💡 剩余 <strong id="abAttempts">' + maxAttempts + '</strong> 次机会';
        container.appendChild(statusDiv);

        var historyDiv = document.createElement('div');
        historyDiv.className = 'ab-history';
        historyDiv.id = 'abHistory';
        historyDiv.textContent = '📝 输入你的猜测...';
        container.appendChild(historyDiv);

        var keypadDiv = document.createElement('div');
        keypadDiv.className = 'ab-keypad';
        keypadDiv.id = 'abKeypad';

        var keypadRows = [
            ['1', '2', '3'],
            ['4', '5', '6'],
            ['7', '8', '9'],
            ['⌫', '0', '✅']
        ];

        keypadRows.forEach(function(row) {
            row.forEach(function(key) {
                var btn = document.createElement('button');
                btn.className = 'ab-key-btn';
                btn.textContent = key;
                btn.dataset.key = key;
                if (key === '⌫') btn.classList.add('key-backspace');
                else if (key === '✅') btn.classList.add('key-submit');
                else btn.classList.add('key-number');

                btn.addEventListener('click', function() {
                    handleABKeyPress(key);
                });

                keypadDiv.appendChild(btn);
            });
        });

        container.appendChild(keypadDiv);

        function handlePhysicalKey(e) {
            if (!abState.isActive || abState.gameSolved || gameState.isAnswered) return;
            var key = e.key;
            if (key >= '0' && key <= '9') {
                e.preventDefault();
                handleABKeyPress(key);
            } else if (key === 'Backspace' || key === 'Delete') {
                e.preventDefault();
                handleABKeyPress('⌫');
            } else if (key === 'Enter') {
                e.preventDefault();
                handleABKeyPress('✅');
            }
        }

        document.addEventListener('keydown', handlePhysicalKey);
        window._abCleanup = function() {
            document.removeEventListener('keydown', handlePhysicalKey);
        };

        startTimer(challenge.timeLimit || 200);
    }

    // ============================================================
    // 1A2B 键盘处理
    // ============================================================
    function handleABKeyPress(key) {
        if (!abState.isActive || abState.gameSolved || gameState.isAnswered) return;

        var digitCount = abState.digitCount;
        var maxAttempts = abState.maxAttempts;
        var password = abState.password;

        var displayEl = document.getElementById('abDisplay');
        var historyEl = document.getElementById('abHistory');
        var attemptsEl = document.getElementById('abAttempts');

        if (key === '⌫') {
            if (abState.currentGuess.length > 0) {
                abState.currentGuess = abState.currentGuess.slice(0, -1);
                updateABDisplay();
            }
            return;
        }

        if (key === '✅') {
            if (abState.currentGuess.length !== digitCount) {
                showToast('请完整输入 ' + digitCount + ' 位数字', 'error');
                return;
            }
            if (new Set(abState.currentGuess).size !== abState.currentGuess.length) {
                showToast('数字不能重复！', 'error');
                return;
            }
            if (abState.currentGuess[0] === '0') {
                showToast('第一位不能是0！', 'error');
                return;
            }

            abState.attemptCount++;
            var remaining = maxAttempts - abState.attemptCount;
            attemptsEl.textContent = remaining;

            var passwordArr = password.split('');
            var guessArr = abState.currentGuess.split('');
            var a = 0, b = 0;
            var matchedIndexes = [];

            for (var i = 0; i < digitCount; i++) {
                if (guessArr[i] === passwordArr[i]) {
                    a++;
                    matchedIndexes.push(i);
                }
            }

            var unmatchedPassword = [];
            var unmatchedGuess = [];
            for (var i = 0; i < digitCount; i++) {
                if (matchedIndexes.indexOf(i) === -1) {
                    unmatchedPassword.push(passwordArr[i]);
                    unmatchedGuess.push(guessArr[i]);
                }
            }

            for (var g of unmatchedGuess) {
                var idx = unmatchedPassword.indexOf(g);
                if (idx !== -1) {
                    b++;
                    unmatchedPassword.splice(idx, 1);
                }
            }

            var resultText = a + 'A' + b + 'B';
            var guessDisplay = abState.currentGuess.split('').join(' ');

            var historyItem = guessDisplay + ' → ' + resultText;
            abState.history.push(historyItem);
            updateABHistory();

            if (a === digitCount) {
                abState.gameSolved = true;
                abState.isActive = false;
                gameState.isAnswered = true;
                clearInterval(gameState.timer);
                disableABKeyboard();
                var points = 10 + Math.floor(gameState.level / 5) + Math.max(0, maxAttempts - abState.attemptCount);
                showFeedback(true, points, '🎉 ' + abState.attemptCount + ' 次猜中！');
                gameState.score += points;
                gameState.combo++;
                gameState.passed++;
                if (gameState.combo > gameState.maxCombo) gameState.maxCombo = gameState.combo;
                updateUI();
                setTimeout(function() {
                    gameState.level++;
                    updateUI();
                    loadChallenge();
                }, 2000);
                return;
            }

            if (remaining <= 0) {
                abState.isActive = false;
                gameState.isAnswered = true;
                clearInterval(gameState.timer);
                disableABKeyboard();
                showFeedback(false, 0, '💀 机会用尽！密码是 ' + password);
                gameState.combo = 0;
                gameState.lives--;
                updateUI();
                if (gameState.lives <= 0) {
                    setTimeout(gameOver, 1500);
                } else {
                    setTimeout(loadChallenge, 2000);
                }
                return;
            }

            abState.currentGuess = '';
            updateABDisplay();
            showToast('第 ' + abState.attemptCount + ' 次：' + resultText, 'info');
            return;
        }

        if (abState.currentGuess.length < digitCount) {
            if (abState.currentGuess.indexOf(key) !== -1) {
                showToast('数字不能重复！', 'error');
                return;
            }
            if (abState.currentGuess.length === 0 && key === '0') {
                showToast('第一位不能是0！', 'error');
                return;
            }
            abState.currentGuess += key;
            updateABDisplay();
        } else {
            showToast('已输入 ' + digitCount + ' 位，请点 ✅ 提交', 'info');
        }
    }

    function updateABDisplay() {
        var displayEl = document.getElementById('abDisplay');
        if (!displayEl) return;
        var displayText = abState.currentGuess.padEnd(abState.digitCount, ' ');
        displayEl.textContent = displayText.split('').join(' ');
    }

    function updateABHistory() {
        var historyEl = document.getElementById('abHistory');
        if (!historyEl) return;
        if (abState.history.length === 0) {
            historyEl.textContent = '📝 输入你的猜测...';
            return;
        }
        var html = '';
        var items = abState.history.slice(-6);
        for (var i = 0; i < items.length; i++) {
            var isCorrect = items[i].indexOf('A' + abState.digitCount + 'B') !== -1;
            html += '<div class="history-item' + (isCorrect ? ' correct' : '') + '">🔹 ' + items[i] + '</div>';
        }
        historyEl.innerHTML = html;
        historyEl.scrollTop = historyEl.scrollHeight;
    }

    function disableABKeyboard() {
        document.querySelectorAll('.ab-key-btn').forEach(function(b) {
            b.disabled = true;
        });
    }

    // ============================================================
    // 提交答案（通用）
    // ============================================================
    function submitAnswer(challenge, answer, btn) {
        if (gameState.isAnswered) return;
        var isCorrect = answer === challenge.correctAnswer;
        var container = dom.gameContent;
        container.querySelectorAll('.option-btn').forEach(function(b) {
            b.disabled = true;
            if (b.dataset.answer === challenge.correctAnswer) b.classList.add('correct');
        });
        if (!isCorrect) {
            btn.classList.add('wrong');
            dom.challengeCard.classList.add('shake');
            setTimeout(function() { dom.challengeCard.classList.remove('shake'); }, 500);
        }
        handleAnswerResult(challenge, isCorrect);
    }

    // ============================================================
    // 处理答案结果
    // ============================================================
    function handleAnswerResult(challenge, isCorrect) {
        gameState.isAnswered = true;
        clearInterval(gameState.timer);

        var points = 0;
        if (isCorrect) {
            var bonus = Math.min(gameState.combo, 10);
            points = 10 + Math.floor(gameState.level / 5) + bonus * 2;
            gameState.score += points;
            gameState.combo++;
            gameState.passed++;
            if (gameState.combo > gameState.maxCombo) gameState.maxCombo = gameState.combo;
            showFeedback(true, points, challenge.funMessage);
            gameState.level++;
        } else {
            gameState.combo = 0;
            gameState.lives--;
            showFeedback(false, 0, '');
        }

        updateUI();

        if (gameState.lives <= 0) {
            setTimeout(gameOver, 1500);
            return;
        }
        setTimeout(loadChallenge, 1500);
    }

    // ============================================================
    // 计时器
    // ============================================================
    function startTimer(limit) {
        clearInterval(gameState.timer);
        gameState.timeLeft = limit || 15;
        updateTimerDisplay(limit || 15);

        gameState.timer = setInterval(function() {
            gameState.timeLeft--;
            updateTimerDisplay(limit || 15);
            if (gameState.timeLeft <= 0) {
                clearInterval(gameState.timer);
                if (!gameState.isAnswered) {
                    gameState.isAnswered = true;
                    disableAllInteractions();
                    gameState.combo = 0;
                    gameState.lives--;
                    updateUI();
                    showFeedback(false, 0, '⏱ 时间到！');
                    if (gameState.lives <= 0) {
                        setTimeout(gameOver, 1200);
                    } else {
                        setTimeout(loadChallenge, 1500);
                    }
                }
            }
        }, 1000);
    }

    function updateTimerDisplay(limit) {
        var pct = (gameState.timeLeft / Math.max(1, limit || 15)) * 100;
        dom.timerFill.style.width = Math.max(0, Math.min(100, pct)) + '%';
        dom.timerText.textContent = '⏱ ' + gameState.timeLeft + 's';
        dom.timerFill.className = 'timer-fill';
        if (pct < 30) dom.timerFill.classList.add('danger');
        else if (pct < 50) dom.timerFill.classList.add('warning');
    }

    function disableAllInteractions() {
        var container = dom.gameContent;
        container.querySelectorAll('.option-btn').forEach(function(b) { b.disabled = true; });
        container.querySelectorAll('.color-option').forEach(function(el) { el.classList.add('disabled'); });
        container.querySelectorAll('.ab-key-btn').forEach(function(b) { b.disabled = true; });
        container.querySelectorAll('.puzzle-cell').forEach(function(cell) { cell.onclick = null; });
        container.querySelectorAll('.char-btn').forEach(function(el) { el.classList.add('disabled'); el.onclick = null; });
        container.querySelectorAll('.key-btn').forEach(function(b) { b.disabled = true; });
        document.getElementById('memorySubmitBtn')?.setAttribute('disabled', 'disabled');
    }

    // ============================================================
    // 反馈
    // ============================================================
    function showFeedback(isCorrect, points, funMessage) {
        var el = dom.feedback;
        el.style.display = 'block';
        el.className = 'feedback show ' + (isCorrect ? 'correct' : 'wrong');
        if (isCorrect) {
            el.innerHTML = '✅ 答对了！ +' + points + ' 分' +
                (funMessage ? '<div class="fun-message">' + funMessage + '</div>' : '');
        } else {
            el.innerHTML = '❌ ' + (funMessage || '答错了！连击中断');
        }
    }

    // ============================================================
    // UI 更新
    // ============================================================
    function updateUI() {
        dom.levelDisplay.textContent = gameState.level;
        dom.scoreDisplay.textContent = gameState.score;
        dom.comboDisplay.textContent = gameState.combo;
        dom.passedDisplay.textContent = gameState.passed;
        var hearts = '❤️'.repeat(gameState.lives) + '🖤'.repeat(Math.max(0, 3 - gameState.lives));
        dom.livesDisplay.textContent = hearts || '💀';
        var progress = (gameState.passed / gameState.totalLevels) * 100;
        dom.progressFill.style.width = Math.min(progress, 100) + '%';
    }

    function getDifficultyLabel(level) {
        if (level >= 81) return '👑 传说';
        if (level >= 61) return '💀 地狱';
        if (level >= 41) return '🔥 噩梦';
        if (level >= 21) return '⚡ 困难';
        return '📝 入门';
    }

    // ============================================================
    // Toast
    // ============================================================
    function showToast(message, type) {
        var container = document.getElementById('toastContainer');
        var toast = document.createElement('div');
        toast.className = 'toast-msg ' + (type || 'info');
        toast.textContent = message;
        container.appendChild(toast);
        setTimeout(function() {
            toast.style.opacity = '0';
            toast.style.transform = 'translateX(20px)';
            setTimeout(function() { toast.remove(); }, 300);
        }, 3000);
    }

    // ============================================================
    // 游戏结束
    // ============================================================
    function gameOver() {
        gameState.isPlaying = false;
        clearInterval(gameState.timer);
        var finalScore = gameState.score;
        dom.challengeCard.innerHTML = `
            <div class="game-over">
                <div class="big-icon">${finalScore > 100 ? '🏆' : '💪'}</div>
                <div class="title">${finalScore > 100 ? '🎉 挑战结束！' : '😅 继续加油！'}</div>
                <div class="sub">你成功通过了 ${gameState.passed} 关</div>
                <div class="final-score">${finalScore}</div>
                <div class="stats-grid">
                    <div class="stat-item"><div class="stat-value">${gameState.passed}</div><div class="stat-label">通关</div></div>
                    <div class="stat-item"><div class="stat-value">${gameState.maxCombo}</div><div class="stat-label">最高连击</div></div>
                    <div class="stat-item"><div class="stat-value">${gameState.level - 1}</div><div class="stat-label">到达关卡</div></div>
                </div>
                <button class="btn-restart" onclick="restartGame()">🔄 再来一次</button>
            </div>
        `;
        saveAndShowRanking();
    }

    function gameWin() {
        gameState.isPlaying = false;
        clearInterval(gameState.timer);
        dom.challengeCard.innerHTML = `
            <div class="game-over">
                <div class="big-icon">👑</div>
                <div class="title">🎉 恭喜通关！</div>
                <div class="sub">你通过了全部 100 关！</div>
                <div class="final-score">${gameState.score}</div>
                <div class="stats-grid">
                    <div class="stat-item"><div class="stat-value">100</div><div class="stat-label">通关</div></div>
                    <div class="stat-item"><div class="stat-value">${gameState.maxCombo}</div><div class="stat-label">最高连击</div></div>
                    <div class="stat-item"><div class="stat-value">🏆</div><div class="stat-label">传说</div></div>
                </div>
                <button class="btn-restart" onclick="restartGame()">🔄 再来一次</button>
            </div>
        `;
        saveAndShowRanking();
    }

    function restartGame() {
        window.location.reload();
    }

    // ============================================================
    // 排行榜
    // ============================================================
    function saveAndShowRanking() {
        var finalScore = gameState.score;
        fetch('/VerifyGame/SaveScore', {
            method: 'POST',
            headers: { 'Content-Type': 'application/x-www-form-urlencoded', 'RequestVerificationToken': token },
            body: 'score=' + finalScore + '&level=' + gameState.level + '&maxCombo=' + gameState.maxCombo + '&passed=' + gameState.passed
        }).then(function() { loadRanking(); });
    }

    function loadRanking() {
        var userId = @(Context.Session.GetInt32("UserId") ?? 0);
        fetch('/VerifyGame/GetRanking')
            .then(function(r) { return r.json(); })
            .then(function(data) {
                var container = dom.rankList;
                var section = dom.rankSection;
                if (data.success && data.data && data.data.length > 0) {
                    var html = '';
                    var medals = ['🥇', '🥈', '🥉'];
                    data.data.forEach(function(item, index) {
                        var medal = index < 3 ? medals[index] : '#' + (index + 1);
                        var avatarHtml = item.avatarUrl && item.isAvatarApproved ?
                            '<img src="' + item.avatarUrl + '" class="rank-avatar" />' :
                            '<div class="rank-avatar-placeholder">' + (item.username ? item.username.charAt(0).toUpperCase() : '?') + '</div>';
                        var isMe = item.isMe || false;
                        html += '<div class="rank-item">' +
                            '<span class="rank-medal">' + medal + '</span>' +
                            avatarHtml +
                            '<span class="rank-name">' + item.username + (isMe ? ' <span class="me">(我)</span>' : '') + '</span>' +
                            '<span class="rank-score">' + item.totalPoints + '</span>' +
                            '</div>';
                    });
                    container.innerHTML = html;
                    section.style.display = 'block';
                } else {
                    container.innerHTML = '<div class="rank-empty">暂无排行数据<br>快来挑战吧！</div>';
                    section.style.display = 'block';
                }
            })
            .catch(function() {
                dom.rankList.innerHTML = '<div class="rank-empty">加载排行榜失败</div>';
                dom.rankSection.style.display = 'block';
            });
    }

    // ============================================================
    // 找不同渲染
    // ============================================================
    function renderFindDifferent(challenge) {
        var container = dom.gameContent;
        var displayTime = challenge.displayTime || 3;

        var textDiv = document.createElement('div');
        textDiv.className = 'display-text-container';
        textDiv.style.display = 'block';
        textDiv.id = 'findDifferentDisplay';
        textDiv.innerHTML = '<div class="display-text" style="font-size:2.8rem;font-weight:bold;color:#8B5CF6;padding:1.5rem;margin:0.5rem 0;background:rgba(255,255,255,0.02);border-radius:16px;border:1px solid rgba(255,255,255,0.04);text-align:center;letter-spacing:8px;font-family:monospace;">' + challenge.originalDisplay + '</div>';
        container.appendChild(textDiv);

        dom.timerText.textContent = '⏱ 记忆 ' + displayTime + 's';
        dom.timerFill.style.width = '100%';
        dom.timerFill.className = 'timer-fill';

        var countdownInterval = setInterval(function() {
            displayTime--;
            dom.timerText.textContent = '⏱ 记忆 ' + displayTime + 's';
            var pct = (displayTime / (challenge.displayTime || 3)) * 100;
            dom.timerFill.style.width = pct + '%';
            if (pct < 30) dom.timerFill.classList.add('danger');
            else if (pct < 50) dom.timerFill.classList.add('warning');
            if (displayTime <= 0) {
                clearInterval(countdownInterval);
                var displayEl = document.getElementById('findDifferentDisplay');
                if (displayEl) {
                    var shuffled = challenge.shuffledDisplay || '';
                    var html = '';
                    for (var i = 0; i < shuffled.length; i++) {
                        var ch = shuffled[i];
                        html += '<span class="char-btn" data-index="' + i + '" data-char="' + ch + '" onclick="selectChar(' + i + ', \'' + ch + '\', this)">' + ch + '</span>';
                    }
                    displayEl.innerHTML = '<div class="display-text" style="padding:1.5rem;margin:0.5rem 0;background:rgba(255,255,255,0.02);border-radius:16px;border:1px solid rgba(255,255,255,0.04);text-align:center;letter-spacing:4px;">' + html + '</div>';
                    displayEl.dataset.correctAnswer = challenge.correctAnswer;
                    displayEl.dataset.shuffledPos = challenge.shuffledPos;
                    dom.timerText.textContent = '⏱ 选择';
                    startTimer(challenge.timeLimit || 10);
                }
            }
        }, 1000);
    }

    // ============================================================
    // 找不同选择字符
    // ============================================================
    function selectChar(index, char, element) {
        if (gameState.isAnswered) return;
        var container = document.getElementById('findDifferentDisplay');
        if (!container) return;
        var shuffledPos = parseInt(container.dataset.shuffledPos);
        var isCorrect = (index === shuffledPos);
        var challenge = gameState.currentChallenge;

        gameState.isAnswered = true;
        clearInterval(gameState.timer);

        var allChars = container.querySelectorAll('.char-btn');
        allChars.forEach(function(el) {
            el.classList.add('disabled');
            el.onclick = null;
            if (parseInt(el.dataset.index) === shuffledPos) {
                el.classList.add('correct-answer');
            }
        });
        if (!isCorrect) {
            element.classList.add('wrong-answer');
            dom.challengeCard.classList.add('shake');
            setTimeout(function() { dom.challengeCard.classList.remove('shake'); }, 500);
        }
        handleAnswerResult(challenge, isCorrect);
    }

    // ============================================================
    // 数字记忆渲染
    // ============================================================
    function renderMemoryGame(challenge) {
        var container = dom.gameContent;
        var memoryTime = challenge.memoryTime || 5;
        var timeLimit = challenge.timeLimit || 15;
        var correctAnswer = challenge.correctAnswer || '';

        container.innerHTML = '';

        var displayDiv = document.createElement('div');
        displayDiv.id = 'memoryDisplay';
        displayDiv.style.cssText = 'font-size:3.5rem;font-weight:bold;color:#8B5CF6;padding:2rem;margin:0.5rem 0;background:rgba(139,92,246,0.04);border-radius:16px;border:1px solid rgba(139,92,246,0.06);text-align:center;letter-spacing:16px;font-family:monospace;';
        displayDiv.textContent = challenge.displayNumber || '';
        container.appendChild(displayDiv);

        var statusDiv = document.createElement('div');
        statusDiv.id = 'memoryStatus';
        statusDiv.style.cssText = 'color:rgba(255,255,255,0.15);font-size:0.8rem;text-align:center;margin:0.2rem 0 0.5rem;';
        statusDiv.textContent = '⏱ 记忆 ' + memoryTime + 's';
        container.appendChild(statusDiv);

        var inputDiv = document.createElement('div');
        inputDiv.className = 'memory-input-display';
        inputDiv.id = 'memoryInputDisplay';
        inputDiv.style.display = 'none';
        inputDiv.textContent = '_ '.repeat(correctAnswer.length).trim();
        container.appendChild(inputDiv);

        var keyboardDiv = document.createElement('div');
        keyboardDiv.className = 'memory-keyboard';
        keyboardDiv.id = 'memoryKeyboard';
        keyboardDiv.style.display = 'none';

        var keyboardRows = challenge.keyboardRows || [
            ['1', '2', '3'],
            ['4', '5', '6'],
            ['7', '8', '9'],
            ['', '0', '⌫']
        ];

        keyboardRows.forEach(function(row) {
            row.forEach(function(key) {
                if (!key) {
                    var empty = document.createElement('div');
                    keyboardDiv.appendChild(empty);
                    return;
                }
                var btn = document.createElement('button');
                btn.className = 'key-btn';
                if (key === '⌫') {
                    btn.classList.add('backspace');
                    btn.textContent = '⌫';
                    btn.addEventListener('click', function() {
                        if (gameState.isAnswered) return;
                        memoryBackspace();
                    });
                } else {
                    btn.textContent = key;
                    btn.addEventListener('click', function() {
                        if (gameState.isAnswered) return;
                        memoryKeyPress(key);
                    });
                }
                keyboardDiv.appendChild(btn);
            });
        });

        container.appendChild(keyboardDiv);

        var controlsDiv = document.createElement('div');
        controlsDiv.className = 'memory-controls';
        controlsDiv.innerHTML = '<button class="btn-memory submit" id="memorySubmitBtn" style="display:none;" onclick="submitMemoryAnswer()">✅ 确认</button>';
        container.appendChild(controlsDiv);

        var memoryState = {
            input: '',
            correctAnswer: correctAnswer,
            isActive: true,
            isMemorizing: true
        };
        window._memoryState = memoryState;

        var memInterval = setInterval(function() {
            memoryTime--;
            var statusEl = document.getElementById('memoryStatus');
            if (statusEl) {
                statusEl.textContent = '⏱ 记忆 ' + memoryTime + 's';
            }
            var pct = (memoryTime / (challenge.memoryTime || 5)) * 100;
            dom.timerFill.style.width = Math.max(0, Math.min(100, pct)) + '%';
            if (pct < 30) dom.timerFill.classList.add('danger');
            else if (pct < 50) dom.timerFill.classList.add('warning');
            else dom.timerFill.classList.remove('danger', 'warning');

            if (memoryTime <= 0) {
                clearInterval(memInterval);

                var displayEl = document.getElementById('memoryDisplay');
                if (displayEl) {
                    displayEl.textContent = '✏️ 输入你记住的数字';
                    displayEl.style.color = 'rgba(255,255,255,0.3)';
                    displayEl.style.fontSize = '1.5rem';
                    displayEl.style.letterSpacing = 'normal';
                    displayEl.style.padding = '1.5rem';
                }

                var statusEl = document.getElementById('memoryStatus');
                if (statusEl) {
                    statusEl.textContent = '⌨️ 输入你记住的数字';
                    statusEl.style.color = 'rgba(255,255,255,0.08)';
                }

                document.getElementById('memoryInputDisplay').style.display = 'block';
                document.getElementById('memoryKeyboard').style.display = 'grid';
                document.getElementById('memorySubmitBtn').style.display = 'inline-block';

                memoryState.isMemorizing = false;

                dom.timerText.textContent = '⏱ ' + timeLimit + 's';
                dom.timerFill.style.width = '100%';
                dom.timerFill.className = 'timer-fill';
                startTimer(timeLimit);
            }
        }, 1000);
    }

    function memoryKeyPress(value) {
        if (gameState.isAnswered) return;
        var state = window._memoryState;
        if (!state || !state.isActive) return;
        if (state.isMemorizing) return;
        var len = state.correctAnswer.length;
        if (state.input.length >= len) return;
        state.input += value;
        updateMemoryDisplay(state);
    }

    function memoryBackspace() {
        if (gameState.isAnswered) return;
        var state = window._memoryState;
        if (!state || !state.isActive) return;
        if (state.isMemorizing) return;
        if (state.input.length > 0) {
            state.input = state.input.slice(0, -1);
            updateMemoryDisplay(state);
        }
    }

    function updateMemoryDisplay(state) {
        var display = document.getElementById('memoryInputDisplay');
        if (!display) return;
        var len = state.correctAnswer.length;
        var text = '';
        for (var i = 0; i < len; i++) {
            if (i < state.input.length) {
                text += state.input[i] + ' ';
            } else {
                text += '_ ';
            }
        }
        display.textContent = text.trim();
    }

    function submitMemoryAnswer() {
        if (gameState.isAnswered) return;
        var state = window._memoryState;
        if (!state) return;
        if (state.input.length !== state.correctAnswer.length) {
            showToast('请完整输入所有数字', 'error');
            return;
        }
        var isCorrect = state.input === state.correctAnswer;
        var challenge = gameState.currentChallenge;
        document.querySelectorAll('.memory-keyboard .key-btn').forEach(function(b) { b.disabled = true; });
        document.getElementById('memorySubmitBtn').disabled = true;
        state.isActive = false;
        handleAnswerResult(challenge, isCorrect);
    }

    // ============================================================
    // 启动
    // ============================================================
    document.addEventListener('DOMContentLoaded', function() {
        gameState.isPlaying = true;
        startGame();
    });
</script>
