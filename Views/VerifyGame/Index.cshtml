@{
    ViewData["Title"] = "🧩 验证大闯关";
    var username = Context.Session.GetString("Username") ?? "玩家";
}

<style>
    /* ============================================================
       全局重置 + 字体
       ============================================================ */
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body { background: #0a0a0f; font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; color: #e0e0e0; min-height: 100vh; overflow-x: hidden; }

    /* ============================================================
       游戏容器
       ============================================================ */
    .game-container { max-width: 720px; margin: 0 auto; padding: 0.8rem 1.2rem 2rem; min-height: 100vh; position: relative; }

    /* ============================================================
       头部
       ============================================================ */
    .game-header { text-align: center; padding: 0.3rem 0 0.8rem; position: relative; }
    .game-header h1 { font-size: 2.4rem; font-weight: 900; background: linear-gradient(135deg, #8B5CF6 0%, #EC4899 40%, #F59E0B 100%); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text; margin: 0; letter-spacing: -0.02em; }
    .game-header .subtitle { color: rgba(255,255,255,0.12); font-size: 0.8rem; margin-top: 0.05rem; letter-spacing: 0.06em; }
    .game-header .type-badge-header { display: inline-block; margin-top: 0.2rem; padding: 0.15rem 1.4rem; border-radius: 30px; background: rgba(139,92,246,0.06); color: #8B5CF6; font-size: 0.7rem; border: 1px solid rgba(139,92,246,0.04); font-weight: 500; letter-spacing: 0.04em; }
    .header-particle { position: absolute; font-size: 0.5rem; opacity: 0.08; animation: floatParticle 12s infinite linear; pointer-events: none; }
    .header-particle:nth-child(1) { left: 3%; top: 15%; animation-delay: 0s; }
    .header-particle:nth-child(2) { left: 12%; top: 40%; animation-delay: 2.5s; }
    .header-particle:nth-child(3) { right: 8%; top: 20%; animation-delay: 4s; }
    .header-particle:nth-child(4) { right: 3%; top: 55%; animation-delay: 1.5s; }
    @@keyframes floatParticle { 0% { transform: translateY(0) rotate(0deg); opacity: 0.05; } 50% { opacity: 0.12; } 100% { transform: translateY(-25px) rotate(180deg); opacity: 0.05; } }

    /* ============================================================
       开始界面
       ============================================================ */
    .start-screen { text-align: center; padding: 0.8rem 0.5rem 1.5rem; width: 100%; }
    .start-screen .logo-icon { font-size: 4.5rem; margin-bottom: 0.3rem; display: block; animation: logoBounce 2.5s ease-in-out infinite; }
    @@keyframes logoBounce { 0%,100% { transform: scale(1) rotate(0deg); } 50% { transform: scale(1.06) rotate(2deg); } }
    .start-screen .title { font-size: 2.6rem; font-weight: 900; background: linear-gradient(135deg, #8B5CF6 0%, #EC4899 45%, #F59E0B 100%); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text; margin-bottom: 0.2rem; }
    .start-screen .desc { color: rgba(255,255,255,0.2); font-size: 0.95rem; margin-bottom: 1.8rem; }
    .start-screen .start-buttons { display: flex; flex-direction: column; gap: 0.7rem; max-width: 300px; margin: 0 auto; }
    .start-screen .start-buttons .btn-start { padding: 0.85rem 1.8rem; border: none; border-radius: 60px; font-weight: 700; font-size: 1rem; cursor: pointer; transition: all 0.35s cubic-bezier(0.23, 1, 0.32, 1); font-family: inherit; position: relative; overflow: hidden; }
    .start-screen .start-buttons .btn-start.primary { background: linear-gradient(135deg, #8B5CF6, #EC4899); color: #fff; box-shadow: 0 4px 30px rgba(108,60,225,0.2); }
    .start-screen .start-buttons .btn-start.primary:hover { transform: translateY(-3px) scale(1.02); box-shadow: 0 12px 50px rgba(108,60,225,0.3); }
    .start-screen .start-buttons .btn-start.secondary { background: rgba(255,255,255,0.03); color: rgba(255,255,255,0.4); border: 1px solid rgba(255,255,255,0.06); }
    .start-screen .start-buttons .btn-start.secondary:hover { background: rgba(255,255,255,0.06); color: #fff; transform: translateY(-2px); }
    .start-screen .start-buttons .btn-start.ranking { background: rgba(139,92,246,0.06); color: #8B5CF6; border: 1px solid rgba(139,92,246,0.06); }
    .start-screen .start-buttons .btn-start.ranking:hover { background: rgba(139,92,246,0.12); transform: translateY(-2px); }
    .start-screen .start-buttons .btn-start.tutorial-btn { background: rgba(245,158,11,0.06); color: #F59E0B; border: 1px solid rgba(245,158,11,0.06); }
    .start-screen .start-buttons .btn-start.tutorial-btn:hover { background: rgba(245,158,11,0.12); transform: translateY(-2px); }

    /* ============================================================
       状态栏
       ============================================================ */
    .status-bar { display: flex; justify-content: space-between; align-items: center; flex-wrap: wrap; gap: 0.2rem 0.8rem; background: rgba(255,255,255,0.02); border: 1px solid rgba(255,255,255,0.04); border-radius: 16px; padding: 0.6rem 1.2rem; margin-bottom: 0.8rem; backdrop-filter: blur(10px); }
    .status-item { color: rgba(255,255,255,0.2); font-size: 0.8rem; display: flex; align-items: center; gap: 0.25rem; }
    .status-item .value { color: #fff; font-weight: 700; font-size: 1.2rem; min-width: 20px; }
    .status-item .value.lives { color: #EC4899; font-size: 1.3rem; }
    .status-item .value.level { color: #8B5CF6; }
    .status-item .value.score { color: #F59E0B; }
    .status-item .value.combo { color: #34D399; }
    .status-item .value.passed { color: #8B5CF6; }

    .progress-bar { width: 100%; height: 3px; background: rgba(255,255,255,0.04); border-radius: 10px; margin-bottom: 0.8rem; overflow: hidden; }
    .progress-bar .fill { height: 100%; border-radius: 10px; background: linear-gradient(135deg, #8B5CF6, #EC4899); transition: width 0.6s ease; width: 0%; }

    /* ============================================================
       挑战卡片
       ============================================================ */
    .challenge-card { background: rgba(255,255,255,0.02); backdrop-filter: blur(20px); border: 1px solid rgba(255,255,255,0.04); border-radius: 24px; padding: 1.5rem 1.8rem; min-height: 300px; display: flex; flex-direction: column; align-items: center; justify-content: flex-start; position: relative; overflow: hidden; transition: all 0.4s ease; box-shadow: 0 8px 40px rgba(0,0,0,0.2); }
    .challenge-card::before { content: ''; position: absolute; top: -50%; left: -50%; width: 200%; height: 200%; background: radial-gradient(ellipse at 30% 30%, rgba(139,92,246,0.015), transparent 70%); pointer-events: none; }
    .challenge-card .type-badge { position: absolute; top: 0.6rem; right: 0.8rem; font-size: 0.6rem; padding: 0.1rem 0.7rem; border-radius: 20px; background: rgba(139,92,246,0.06); color: #8B5CF6; border: 1px solid rgba(139,92,246,0.04); font-weight: 500; z-index: 1; }
    .challenge-card .question-text { color: rgba(255,255,255,0.4); font-size: 1.05rem; text-align: center; line-height: 1.8; margin-bottom: 0.3rem; min-height: 36px; width: 100%; position: relative; z-index: 1; }

    /* ============================================================
       选项按钮 - 高级样式
       ============================================================ */
    .options-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 0.6rem; width: 100%; max-width: 420px; position: relative; z-index: 1; }
    .options-grid .option-btn { padding: 0.75rem 1rem; border: 2px solid rgba(255,255,255,0.04); border-radius: 16px; background: rgba(255,255,255,0.02); color: rgba(255,255,255,0.5); font-size: 1rem; font-weight: 500; cursor: pointer; transition: all 0.3s ease; font-family: inherit; text-align: center; position: relative; overflow: hidden; }
    .options-grid .option-btn::after { content: ''; position: absolute; inset: 0; background: linear-gradient(135deg, rgba(139,92,246,0.05), rgba(236,72,153,0.05)); opacity: 0; transition: opacity 0.3s ease; }
    .options-grid .option-btn:hover:not(:disabled)::after { opacity: 1; }
    .options-grid .option-btn:hover:not(:disabled) { border-color: rgba(139,92,246,0.15); color: #fff; transform: translateY(-2px); box-shadow: 0 8px 30px rgba(0,0,0,0.15); }
    .options-grid .option-btn:disabled { cursor: not-allowed; opacity: 0.3; transform: none !important; }
    .options-grid .option-btn.correct { border-color: #28a745; background: rgba(40,167,69,0.08); color: #28a745; box-shadow: 0 0 30px rgba(40,167,69,0.05); }
    .options-grid .option-btn.correct::after { opacity: 0; }
    .options-grid .option-btn.wrong { border-color: #dc3545; background: rgba(220,53,69,0.08); color: #dc3545; box-shadow: 0 0 30px rgba(220,53,69,0.05); }
    .options-grid .option-btn.wrong::after { opacity: 0; }

    /* ============================================================
       颜色选项
       ============================================================ */
    .color-options-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 0.6rem; width: 100%; max-width: 380px; margin: 0 auto; position: relative; z-index: 1; }
    .color-options-grid .color-option { padding: 0.7rem 0.2rem; border: 2px solid rgba(255,255,255,0.04); border-radius: 16px; background: rgba(255,255,255,0.02); cursor: pointer; transition: all 0.35s ease; display: flex; flex-direction: column; align-items: center; gap: 5px; }
    .color-options-grid .color-option:hover:not(.disabled) { border-color: rgba(139,92,246,0.15); background: rgba(139,92,246,0.04); transform: translateY(-3px); box-shadow: 0 8px 30px rgba(0,0,0,0.12); }
    .color-options-grid .color-option.disabled { cursor: not-allowed; opacity: 0.5; transform: none !important; }
    .color-options-grid .color-option .color-block { width: 50px; height: 50px; border-radius: 12px; border: 1px solid rgba(255,255,255,0.06); transition: all 0.3s ease; }
    .color-options-grid .color-option .color-label { color: rgba(255,255,255,0.4); font-size: 0.85rem; font-weight: 600; transition: all 0.3s ease; }
    .color-options-grid .color-option.correct { border-color: #28a745 !important; background: rgba(40,167,69,0.08) !important; box-shadow: 0 0 40px rgba(40,167,69,0.05); }
    .color-options-grid .color-option.correct .color-label { color: #28a745 !important; }
    .color-options-grid .color-option.wrong { border-color: #dc3545 !important; background: rgba(220,53,69,0.08) !important; box-shadow: 0 0 40px rgba(220,53,69,0.05); }
    .color-options-grid .color-option.wrong .color-label { color: #dc3545 !important; }

    /* ============================================================
       其他组件
       ============================================================ */
    .captcha-image { border-radius: 14px; border: 1px solid rgba(255,255,255,0.04); background: rgba(255,255,255,0.02); max-width: 100%; margin: 0.1rem 0 0.6rem; cursor: pointer; transition: transform 0.3s ease; }
    .captcha-image:hover { transform: scale(1.02); }
    .color-display { font-size: 2.2rem; padding: 0.3rem 1.5rem; border-radius: 14px; background: rgba(255,255,255,0.02); margin: 0.1rem 0 0.6rem; letter-spacing: 4px; }
    .display-text-container { width: 100%; display: block; margin: 0.1rem 0 0.6rem; position: relative; z-index: 1; }
    .display-text-container .display-text { font-size: 1.3rem; padding: 0.4rem 1.2rem; margin: 0.1rem 0 0.6rem; line-height: 2; text-align: center; font-family: 'Courier New', monospace; letter-spacing: 3px; width: 100%; }

    .view-container { display: flex; justify-content: center; gap: 1.2rem; flex-wrap: wrap; padding: 0.3rem 0; }
    .view-item { text-align: center; }
    .view-item .view-label { color: rgba(255,255,255,0.08); font-size: 0.55rem; text-transform: uppercase; letter-spacing: 0.06em; margin-bottom: 0.15rem; display: block; }
    .view-item .view-grid { display: inline-grid; gap: 2px; background: rgba(255,255,255,0.02); padding: 4px; border-radius: 8px; border: 1px solid rgba(255,255,255,0.03); }
    .view-item .view-grid .cell { width: 32px; height: 32px; border-radius: 4px; }
    .view-item .view-grid .cell.filled { background: #8B5CF6; }
    .view-item .view-grid .cell.empty { background: rgba(255,255,255,0.02); }

    .rotate-compare { display: flex; gap: 1.2rem; justify-content: center; align-items: center; flex-wrap: wrap; padding: 0.3rem 0; }
    .rotate-compare .rotate-item { text-align: center; }
    .rotate-compare .rotate-item .label { color: rgba(255,255,255,0.08); font-size: 0.55rem; text-transform: uppercase; letter-spacing: 0.06em; margin-bottom: 0.15rem; display: block; }
    .rotate-compare .rotate-item svg { border-radius: 10px; background: rgba(255,255,255,0.02); border: 1px solid rgba(255,255,255,0.03); }
    .rotate-compare .rotate-arrow { color: rgba(255,255,255,0.04); font-size: 1.2rem; }
    .rotate-angle-text { font-size: 1.1rem; font-weight: 700; color: #8B5CF6; margin-top: 0.2rem; }

    .memory-keyboard { display: grid; grid-template-columns: repeat(4, 1fr); gap: 0.4rem; max-width: 260px; margin: 0.3rem auto; width: 100%; }
    .memory-keyboard .key-btn { padding: 0.7rem 0.3rem; border: 2px solid rgba(255,255,255,0.04); border-radius: 12px; background: rgba(255,255,255,0.02); color: #fff; font-size: 1.3rem; font-weight: 600; cursor: pointer; transition: all 0.2s ease; font-family: inherit; text-align: center; }
    .memory-keyboard .key-btn:hover:not(:disabled) { background: rgba(139,92,246,0.06); border-color: rgba(139,92,246,0.12); transform: scale(1.04); }
    .memory-keyboard .key-btn:disabled { opacity: 0.3; cursor: not-allowed; transform: none; }
    .memory-keyboard .key-btn.backspace { color: #EC4899; border-color: rgba(236,72,153,0.1); }
    .memory-keyboard .key-btn.backspace:hover:not(:disabled) { background: rgba(236,72,153,0.06); border-color: rgba(236,72,153,0.15); }
    .memory-input-display { text-align: center; padding: 0.4rem; min-height: 44px; font-size: 1.8rem; font-weight: bold; color: #8B5CF6; letter-spacing: 8px; font-family: 'Courier New', monospace; background: rgba(255,255,255,0.02); border-radius: 12px; border: 1px solid rgba(255,255,255,0.04); margin: 0.3rem auto; width: 100%; max-width: 260px; }
    .memory-controls { display: flex; gap: 0.4rem; justify-content: center; margin-top: 0.2rem; }
    .memory-controls .btn-memory { padding: 0.4rem 1.4rem; border: none; border-radius: 40px; font-weight: 600; font-size: 0.8rem; cursor: pointer; transition: all 0.3s ease; font-family: inherit; }
    .memory-controls .btn-memory.submit { background: linear-gradient(135deg, #8B5CF6, #EC4899); color: #fff; }
    .memory-controls .btn-memory.submit:hover:not(:disabled) { transform: translateY(-2px); box-shadow: 0 8px 30px rgba(108,60,225,0.15); }
    .memory-controls .btn-memory:disabled { opacity: 0.3; cursor: not-allowed; transform: none; }

    .char-btn { display: inline-block; padding: 0.2rem 0.6rem; margin: 0.05rem; border-radius: 10px; cursor: pointer; font-size: 2.2rem; font-weight: bold; font-family: monospace; transition: all 0.3s ease; user-select: none; color: #ffffff; border: 2px solid transparent; background: transparent; }
    .char-btn:hover:not(.disabled) { transform: scale(1.08); background: rgba(139,92,246,0.06); }
    .char-btn.disabled { cursor: default; opacity: 0.4; background: transparent !important; transform: none !important; }
    .char-btn.correct-answer { border-color: #28a745 !important; background: rgba(40,167,69,0.1) !important; color: #28a745 !important; }
    .char-btn.wrong-answer { border-color: #dc3545 !important; background: rgba(220,53,69,0.1) !important; color: #dc3545 !important; }

    .puzzle-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 4px; max-width: 210px; margin: 0 auto; background: rgba(255,255,255,0.02); padding: 6px; border-radius: 14px; border: 1px solid rgba(255,255,255,0.04); touch-action: none; user-select: none; }
    .puzzle-grid .puzzle-cell { aspect-ratio: 1; display: flex; align-items: center; justify-content: center; font-size: 1.4rem; font-weight: 700; border-radius: 10px; cursor: pointer; transition: all 0.2s ease; background: rgba(255,255,255,0.04); color: #fff; user-select: none; touch-action: none; }
    .puzzle-grid .puzzle-cell:hover:not(.empty) { background: rgba(139,92,246,0.08); transform: scale(1.04); }
    .puzzle-grid .puzzle-cell.empty { background: transparent; cursor: default; }

    .ascii-art { font-family: 'Courier New', monospace; font-size: 0.8rem; color: #8B5CF6; white-space: pre; text-align: center; line-height: 1.3; padding: 0.4rem; background: rgba(255,255,255,0.02); border-radius: 12px; border: 1px solid rgba(255,255,255,0.04); }

    /* ============================================================
       反馈
       ============================================================ */
    .feedback { text-align: center; margin-top: 0.6rem; padding: 0.5rem 1.2rem; border-radius: 14px; font-size: 1rem; font-weight: 600; animation: feedbackIn 0.5s cubic-bezier(0.23, 1, 0.32, 1); width: 100%; display: none; position: relative; z-index: 1; }
    .feedback.show { display: block; }
    .feedback.correct { background: rgba(40,167,69,0.06); color: #28a745; border: 1px solid rgba(40,167,69,0.06); box-shadow: 0 0 40px rgba(40,167,69,0.02); }
    .feedback.wrong { background: rgba(220,53,69,0.06); color: #dc3545; border: 1px solid rgba(220,53,69,0.06); box-shadow: 0 0 40px rgba(220,53,69,0.02); }
    .feedback .fun-message { font-weight: 400; color: rgba(255,255,255,0.25); font-size: 0.8rem; margin-top: 0.05rem; }
    @@keyframes feedbackIn { from { opacity: 0; transform: translateY(12px) scale(0.95); } to { opacity: 1; transform: translateY(0) scale(1); } }

    /* ============================================================
       计时器
       ============================================================ */
    .timer-section { width: 100%; margin-top: 0.8rem; position: relative; z-index: 1; }
    .timer-bar { width: 100%; height: 3px; background: rgba(255,255,255,0.04); border-radius: 10px; overflow: hidden; }
    .timer-bar .timer-fill { height: 100%; border-radius: 10px; background: linear-gradient(135deg, #34D399, #F59E0B); transition: width 0.3s linear; width: 100%; }
    .timer-bar .timer-fill.warning { background: linear-gradient(135deg, #F59E0B, #EC4899); }
    .timer-bar .timer-fill.danger { background: linear-gradient(135deg, #EC4899, #dc3545); }
    .timer-text { text-align: right; color: rgba(255,255,255,0.06); font-size: 0.75rem; margin-top: 0.15rem; font-weight: 500; letter-spacing: 0.04em; }

    /* ============================================================
       游戏结束
       ============================================================ */
    .game-over { text-align: center; padding: 1rem 0.5rem; width: 100%; position: relative; z-index: 1; }
    .game-over .big-icon { font-size: 3.8rem; margin-bottom: 0.2rem; animation: gameOverBounce 1.2s ease-in-out infinite; }
    @@keyframes gameOverBounce { 0%,100% { transform: scale(1); } 50% { transform: scale(1.08); } }
    .game-over .title { color: #fff; font-size: 1.8rem; font-weight: 800; margin-bottom: 0.1rem; }
    .game-over .sub { color: rgba(255,255,255,0.15); font-size: 0.9rem; margin-bottom: 0.4rem; }
    .game-over .final-score { font-size: 3rem; font-weight: 900; color: #F59E0B; margin: 0.2rem 0; text-shadow: 0 0 60px rgba(245,158,11,0.05); }
    .game-over .stats-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 0.4rem; max-width: 280px; margin: 0.4rem auto 1rem; }
    .game-over .stats-grid .stat-item { background: rgba(255,255,255,0.02); border-radius: 14px; padding: 0.5rem; border: 1px solid rgba(255,255,255,0.03); }
    .game-over .stats-grid .stat-item .stat-value { color: #fff; font-size: 1.2rem; font-weight: 700; }
    .game-over .stats-grid .stat-item .stat-label { color: rgba(255,255,255,0.1); font-size: 0.5rem; text-transform: uppercase; letter-spacing: 0.06em; margin-top: 0.05rem; }
    .btn-restart { padding: 0.7rem 2.8rem; border: none; border-radius: 60px; background: linear-gradient(135deg, #8B5CF6, #EC4899); color: #fff; font-weight: 700; font-size: 0.95rem; cursor: pointer; transition: all 0.3s ease; font-family: inherit; box-shadow: 0 4px 30px rgba(108,60,225,0.15); }
    .btn-restart:hover { transform: translateY(-3px); box-shadow: 0 12px 50px rgba(108,60,225,0.25); }

    /* ============================================================
       排行榜
       ============================================================ */
    .rank-section { margin-top: 1.2rem; padding: 1rem 1.2rem; background: rgba(255,255,255,0.02); border: 1px solid rgba(255,255,255,0.04); border-radius: 16px; }
    .rank-section .rank-title { color: rgba(255,255,255,0.15); font-size: 0.85rem; font-weight: 500; margin-bottom: 0.6rem; display: flex; align-items: center; gap: 0.4rem; }
    .rank-item { display: flex; align-items: center; gap: 0.5rem; padding: 0.3rem 0.5rem; border-bottom: 1px solid rgba(255,255,255,0.02); }
    .rank-item:last-child { border-bottom: none; }
    .rank-item .rank-medal { font-size: 0.8rem; min-width: 28px; text-align: center; color: rgba(255,255,255,0.1); }
    .rank-item .rank-avatar { width: 26px; height: 26px; border-radius: 50%; object-fit: cover; border: 1px solid rgba(255,255,255,0.04); flex-shrink: 0; background: rgba(255,255,255,0.03); }
    .rank-item .rank-avatar-placeholder { width: 26px; height: 26px; border-radius: 50%; background: rgba(255,255,255,0.04); display: flex; align-items: center; justify-content: center; font-size: 0.55rem; color: rgba(255,255,255,0.15); flex-shrink: 0; }
    .rank-item .rank-name { flex: 1; color: rgba(255,255,255,0.3); font-size: 0.8rem; }
    .rank-item .rank-name .me { color: #8B5CF6; font-weight: 600; }
    .rank-item .rank-score { color: #F59E0B; font-weight: 700; font-size: 0.85rem; }
    .rank-empty { text-align: center; color: rgba(255,255,255,0.06); font-size: 0.8rem; padding: 0.8rem 0; }

    /* ============================================================
       Toast
       ============================================================ */
    .toast-container { position: fixed; bottom: 1.5rem; right: 1.5rem; z-index: 2000; display: flex; flex-direction: column; gap: 0.4rem; align-items: flex-end; }
    .toast-msg { padding: 0.5rem 1.5rem; border-radius: 40px; color: #fff; font-weight: 500; animation: toastIn 0.4s cubic-bezier(0.23, 1, 0.32, 1); font-size: 0.85rem; backdrop-filter: blur(12px); background: rgba(0,0,0,0.6); border: 1px solid rgba(255,255,255,0.04); }
    .toast-msg.success { background: rgba(40,167,69,0.7); }
    .toast-msg.error { background: rgba(220,53,69,0.7); }
    .toast-msg.info { background: rgba(79,172,254,0.7); }
    @@keyframes toastIn { from { opacity: 0; transform: translateY(16px) scale(0.95); } to { opacity: 1; transform: translateY(0) scale(1); } }

    /* ============================================================
       教程弹窗 - 精美版
       ============================================================ */
    .tutorial-overlay { position: fixed; inset: 0; background: rgba(0,0,0,0.88); backdrop-filter: blur(24px); z-index: 9999; display: none; align-items: center; justify-content: center; padding: 1rem; overflow-y: auto; }
    .tutorial-overlay.active { display: flex; }
    .tutorial-overlay::-webkit-scrollbar { width: 3px; }
    .tutorial-overlay::-webkit-scrollbar-thumb { background: rgba(139,92,246,0.15); border-radius: 10px; }

    .tutorial-modal { background: rgba(20,20,40,0.98); border: 1px solid rgba(255,255,255,0.04); border-radius: 28px; max-width: 660px; width: 100%; max-height: 90vh; overflow-y: auto; padding: 2rem 2.2rem; box-shadow: 0 40px 120px rgba(0,0,0,0.6); position: relative; animation: modalIn 0.4s cubic-bezier(0.23, 1, 0.32, 1); }
    @@keyframes modalIn { from { opacity: 0; transform: scale(0.96) translateY(20px); } to { opacity: 1; transform: scale(1) translateY(0); } }
    .tutorial-modal::-webkit-scrollbar { width: 3px; }
    .tutorial-modal::-webkit-scrollbar-thumb { background: rgba(139,92,246,0.15); border-radius: 10px; }
    .tutorial-modal .modal-close { position: absolute; top: 0.8rem; right: 1.2rem; background: none; border: none; color: rgba(255,255,255,0.06); font-size: 1.5rem; cursor: pointer; transition: color 0.3s ease; }
    .tutorial-modal .modal-close:hover { color: rgba(255,255,255,0.2); }
    .tutorial-modal .tutorial-title { color: #fff; font-size: 1.5rem; font-weight: 700; margin-bottom: 0.1rem; display: flex; align-items: center; gap: 0.5rem; }
    .tutorial-modal .tutorial-sub { color: rgba(255,255,255,0.15); font-size: 0.8rem; margin-bottom: 1rem; }
    .tutorial-modal .tutorial-section { background: rgba(255,255,255,0.02); border-radius: 16px; padding: 0.8rem 1rem; margin-bottom: 0.6rem; border: 1px solid rgba(255,255,255,0.03); }
    .tutorial-modal .tutorial-section .sec-title { color: rgba(255,255,255,0.5); font-weight: 600; font-size: 0.8rem; margin-bottom: 0.2rem; }
    .tutorial-modal .tutorial-section .sec-content { color: rgba(255,255,255,0.25); font-size: 0.85rem; line-height: 1.7; }
    .tutorial-modal .tutorial-section .example-box { background: rgba(139,92,246,0.03); border-radius: 12px; padding: 0.6rem 0.8rem; margin-top: 0.3rem; border: 1px solid rgba(139,92,246,0.04); }
    .tutorial-modal .tutorial-section .example-box .example-label { color: rgba(255,255,255,0.08); font-size: 0.55rem; text-transform: uppercase; letter-spacing: 0.06em; }
    .tutorial-modal .tutorial-section .example-box .example-content { color: rgba(255,255,255,0.35); font-size: 0.85rem; margin-top: 0.05rem; }

    /* 教程演示区域 - 带动画 */
    .tutorial-demo-area { background: rgba(255,255,255,0.02); border-radius: 16px; padding: 0.8rem; margin-top: 0.6rem; border: 1px solid rgba(255,255,255,0.03); min-height: 70px; display: none; align-items: center; justify-content: center; flex-direction: column; }
    .tutorial-demo-area.active { display: flex; animation: demoFadeIn 0.5s ease; }
    @@keyframes demoFadeIn { from { opacity: 0; transform: translateY(8px); } to { opacity: 1; transform: translateY(0); } }
    .tutorial-demo-area .demo-content { color: rgba(255,255,255,0.4); font-size: 0.9rem; text-align: center; line-height: 1.8; width: 100%; }

    .tutorial-nav { display: flex; gap: 0.4rem; flex-wrap: wrap; margin-top: 1rem; justify-content: center; }
    .tutorial-nav .nav-btn { padding: 0.4rem 1.2rem; border: 1px solid rgba(255,255,255,0.04); border-radius: 30px; background: rgba(255,255,255,0.02); color: rgba(255,255,255,0.2); font-size: 0.8rem; cursor: pointer; transition: all 0.3s ease; font-family: inherit; }
    .tutorial-nav .nav-btn:hover:not(:disabled) { background: rgba(255,255,255,0.04); color: #fff; }
    .tutorial-nav .nav-btn.active { background: rgba(139,92,246,0.08); color: #8B5CF6; border-color: rgba(139,92,246,0.06); }
    .tutorial-nav .nav-btn:disabled { opacity: 0.2; cursor: not-allowed; }
    .tutorial-nav .nav-btn.demo-btn { background: rgba(245,158,11,0.06); color: #F59E0B; border-color: rgba(245,158,11,0.06); }
    .tutorial-nav .nav-btn.demo-btn:hover { background: rgba(245,158,11,0.1); }
    .tutorial-nav .nav-btn.close-tutorial { background: rgba(220,53,69,0.04); color: rgba(220,53,69,0.2); border-color: rgba(220,53,69,0.04); }
    .tutorial-nav .nav-btn.close-tutorial:hover { background: rgba(220,53,69,0.08); color: rgba(220,53,69,0.3); }

    /* ============================================================
       响应式
       ============================================================ */
    @@media (max-width: 640px) {
        .game-container { padding: 0.4rem 0.8rem 1rem; }
        .game-header h1 { font-size: 1.8rem; }
        .start-screen .title { font-size: 2rem; }
        .start-screen .logo-icon { font-size: 3.5rem; }
        .challenge-card { padding: 1rem 1.2rem; min-height: 260px; }
        .status-bar { padding: 0.4rem 0.8rem; gap: 0.1rem 0.5rem; }
        .status-item { font-size: 0.7rem; }
        .status-item .value { font-size: 1rem; }
        .status-item .value.lives { font-size: 1.1rem; }
        .options-grid { gap: 0.4rem; }
        .options-grid .option-btn { padding: 0.6rem 0.6rem; font-size: 0.85rem; }
        .color-options-grid { gap: 0.4rem; max-width: 300px; }
        .color-options-grid .color-option .color-block { width: 40px; height: 40px; }
        .color-options-grid .color-option .color-label { font-size: 0.7rem; }
        .ab-display { font-size: 2rem; letter-spacing: 8px; min-height: 56px; }
        .ab-keypad { max-width: 200px; gap: 5px; }
        .ab-key-btn { font-size: 1.1rem; min-height: 40px; padding: 0.4rem 0.2rem; }
        .ab-history { font-size: 0.7rem; max-height: 70px; }
        .char-btn { font-size: 1.8rem; padding: 0.15rem 0.4rem; }
        .view-item .view-grid .cell { width: 26px; height: 26px; }
        .view-container { gap: 0.6rem; }
        .rotate-compare .rotate-item svg { width: 70px; height: 70px; }
        .rotate-angle-text { font-size: 0.9rem; }
        .puzzle-grid { max-width: 170px; }
        .puzzle-grid .puzzle-cell { font-size: 1.1rem; }
        .game-over .final-score { font-size: 2.4rem; }
        .game-over .big-icon { font-size: 3rem; }
        .tutorial-modal { padding: 1.2rem 1rem; }
        .memory-keyboard { max-width: 220px; }
        .memory-keyboard .key-btn { font-size: 1.1rem; padding: 0.5rem 0.2rem; }
        .memory-input-display { font-size: 1.5rem; letter-spacing: 6px; max-width: 220px; }
        .display-text-container .display-text { font-size: 1rem; }
    }

    @@media (max-width: 400px) {
        .game-header h1 { font-size: 1.4rem; }
        .start-screen .title { font-size: 1.6rem; }
        .start-screen .logo-icon { font-size: 2.8rem; }
        .start-screen .start-buttons .btn-start { padding: 0.7rem 1.2rem; font-size: 0.85rem; }
        .status-item { font-size: 0.6rem; }
        .status-item .value { font-size: 0.8rem; }
        .status-item .value.lives { font-size: 0.9rem; }
        .options-grid { grid-template-columns: 1fr 1fr; gap: 0.3rem; }
        .options-grid .option-btn { padding: 0.5rem 0.4rem; font-size: 0.75rem; border-radius: 12px; }
        .color-options-grid { grid-template-columns: repeat(3, 1fr); gap: 0.3rem; max-width: 240px; }
        .color-options-grid .color-option .color-block { width: 32px; height: 32px; border-radius: 8px; }
        .color-options-grid .color-option .color-label { font-size: 0.6rem; }
        .ab-display { font-size: 1.6rem; letter-spacing: 6px; min-height: 44px; }
        .ab-keypad { max-width: 160px; gap: 3px; }
        .ab-key-btn { font-size: 0.9rem; min-height: 34px; padding: 0.3rem 0.1rem; }
        .ab-history { font-size: 0.6rem; max-height: 56px; }
        .char-btn { font-size: 1.4rem; padding: 0.1rem 0.3rem; }
        .view-item .view-grid .cell { width: 20px; height: 20px; }
        .rotate-compare .rotate-item svg { width: 50px; height: 50px; }
        .rotate-compare .rotate-arrow { font-size: 0.8rem; }
        .puzzle-grid { max-width: 140px; gap: 3px; padding: 4px; }
        .puzzle-grid .puzzle-cell { font-size: 0.9rem; }
        .game-over .final-score { font-size: 2rem; }
        .game-over .big-icon { font-size: 2.4rem; }
        .game-over .title { font-size: 1.3rem; }
        .tutorial-modal { padding: 0.8rem 0.6rem; }
        .tutorial-modal .tutorial-title { font-size: 1.1rem; }
        .tutorial-modal .tutorial-section { padding: 0.5rem 0.6rem; }
        .tutorial-modal .tutorial-section .sec-content { font-size: 0.75rem; }
        .memory-keyboard { max-width: 180px; gap: 3px; }
        .memory-keyboard .key-btn { font-size: 0.9rem; padding: 0.4rem 0.1rem; }
        .memory-input-display { font-size: 1.2rem; letter-spacing: 4px; max-width: 180px; }
        .display-text-container .display-text { font-size: 0.85rem; }
    }
</style>

<div class="game-container">
    <div class="game-header">
        <span class="header-particle">✦</span>
        <span class="header-particle">●</span>
        <span class="header-particle">◆</span>
        <span class="header-particle">▲</span>
        <h1>🧩 验证大闯关</h1>
        <p class="subtitle">100关 · 20种玩法 · 证明你是人类</p>
        <div class="type-badge-header" id="typeNameDisplay">🎮 准备开始</div>
    </div>

    <!-- 状态栏 -->
    <div class="status-bar" id="statusBar" style="display:none;">
        <div class="status-item">❤️ 生命 <span class="value lives" id="livesDisplay">❤️❤️❤️</span></div>
        <div class="status-item">📊 关卡 <span class="value level" id="levelDisplay">1</span></div>
        <div class="status-item">🏆 积分 <span class="value score" id="scoreDisplay">0</span></div>
        <div class="status-item">🔥 连击 <span class="value combo" id="comboDisplay">0</span></div>
        <div class="status-item">✅ 通关 <span class="value passed" id="passedDisplay">0</span></div>
    </div>

    <div class="progress-bar" id="progressBar" style="display:none;"><div class="fill" id="progressFill"></div></div>

    <!-- 挑战卡片 -->
    <div class="challenge-card" id="challengeCard">
        <div class="type-badge" id="typeBadge">第 1 关</div>
        <div class="question-text" id="questionText">加载中...</div>
        <div id="gameContent" style="width:100%;"></div>
        <div class="timer-section" id="timerSection" style="display:none;">
            <div class="timer-bar"><div class="timer-fill" id="timerFill"></div></div>
            <div class="timer-text" id="timerText">⏱ 15s</div>
        </div>
        <div class="feedback" id="feedback"></div>
    </div>

    <!-- 排行榜 -->
    <div class="rank-section" id="rankSection" style="display:none;">
        <div class="rank-title">🏆 排行榜</div>
        <div id="rankList"></div>
    </div>
</div>

<!-- 教程弹窗 -->
<div class="tutorial-overlay" id="tutorialOverlay">
    <div class="tutorial-modal">
        <button class="modal-close" onclick="closeTutorial()">✕</button>
        <div id="tutorialContent"></div>
    </div>
</div>

<div class="toast-container" id="toastContainer"></div>

<script>
    // ============================================================
    // ⭐ 音效系统（Web Audio API）
    // ============================================================
    var SoundFX = {
        ctx: null, enabled: true,

        init: function() {
            try { this.ctx = new (window.AudioContext || window.webkitAudioContext)(); }
            catch(e) { this.enabled = false; }
        },

        ensure: function() {
            if (this.ctx && this.ctx.state === 'suspended') this.ctx.resume();
        },

        play: function(type) {
            if (!this.enabled || !this.ctx) return;
            this.ensure();
            try {
                var now = this.ctx.currentTime;
                switch(type) {
                    case 'correct':
                        this.playNote(523.25, now, 0.12);
                        setTimeout(function() { SoundFX.playNote(659.25, SoundFX.ctx.currentTime, 0.12); }, 120);
                        setTimeout(function() { SoundFX.playNote(783.99, SoundFX.ctx.currentTime, 0.2); }, 250);
                        break;
                    case 'wrong':
                        var osc = this.ctx.createOscillator();
                        var gain = this.ctx.createGain();
                        osc.connect(gain); gain.connect(this.ctx.destination);
                        osc.type = 'sawtooth';
                        osc.frequency.setValueAtTime(300, now);
                        osc.frequency.exponentialRampToValueAtTime(150, now + 0.4);
                        gain.gain.setValueAtTime(0.15, now);
                        gain.gain.exponentialRampToValueAtTime(0.01, now + 0.4);
                        osc.start(now); osc.stop(now + 0.4);
                        break;
                    case 'timeout':
                        var osc = this.ctx.createOscillator();
                        var gain = this.ctx.createGain();
                        osc.connect(gain); gain.connect(this.ctx.destination);
                        osc.type = 'square';
                        osc.frequency.setValueAtTime(800, now);
                        osc.frequency.exponentialRampToValueAtTime(400, now + 0.3);
                        gain.gain.setValueAtTime(0.08, now);
                        gain.gain.exponentialRampToValueAtTime(0.01, now + 0.3);
                        osc.start(now); osc.stop(now + 0.3);
                        break;
                    case 'gameover':
                        [0, 200, 400, 600].forEach(function(delay) {
                            setTimeout(function() {
                                var o = SoundFX.ctx.createOscillator();
                                var g = SoundFX.ctx.createGain();
                                o.connect(g); g.connect(SoundFX.ctx.destination);
                                o.type = 'sine';
                                o.frequency.setValueAtTime(600 - delay * 0.5, SoundFX.ctx.currentTime);
                                g.gain.setValueAtTime(0.1, SoundFX.ctx.currentTime);
                                g.gain.exponentialRampToValueAtTime(0.01, SoundFX.ctx.currentTime + 0.2);
                                o.start(SoundFX.ctx.currentTime);
                                o.stop(SoundFX.ctx.currentTime + 0.2);
                            }, delay);
                        });
                        break;
                    case 'start':
                        this.playNote(523.25, now, 0.08);
                        setTimeout(function() { SoundFX.playNote(659.25, SoundFX.ctx.currentTime, 0.08); }, 80);
                        setTimeout(function() { SoundFX.playNote(783.99, SoundFX.ctx.currentTime, 0.12); }, 170);
                        break;
                    case 'tick':
                        var osc = this.ctx.createOscillator();
                        var gain = this.ctx.createGain();
                        osc.connect(gain); gain.connect(this.ctx.destination);
                        osc.type = 'sine';
                        osc.frequency.setValueAtTime(1000, now);
                        gain.gain.setValueAtTime(0.03, now);
                        gain.gain.exponentialRampToValueAtTime(0.01, now + 0.04);
                        osc.start(now); osc.stop(now + 0.04);
                        break;
                }
            } catch(e) {}
        },

        playNote: function(freq, time, duration) {
            var osc = this.ctx.createOscillator();
            var gain = this.ctx.createGain();
            osc.connect(gain); gain.connect(this.ctx.destination);
            osc.type = 'sine';
            osc.frequency.setValueAtTime(freq, time);
            gain.gain.setValueAtTime(0.12, time);
            gain.gain.exponentialRampToValueAtTime(0.01, time + duration);
            osc.start(time); osc.stop(time + duration);
        }
    };

    // ============================================================
    // 游戏状态
    // ============================================================
    var token = document.querySelector('input[name="__RequestVerificationToken"]')?.value || '';
    var userId = @(Context.Session.GetInt32("UserId") ?? 0);

    var gameState = {
        level: 1, score: 0, lives: 3, combo: 0, maxCombo: 0, passed: 0,
        isPlaying: false, isAnswered: false, timer: null, timeLeft: 15,
        totalLevels: 100, currentChallenge: null,
    };

    var dom = {
        levelDisplay: document.getElementById('levelDisplay'),
        scoreDisplay: document.getElementById('scoreDisplay'),
        livesDisplay: document.getElementById('livesDisplay'),
        comboDisplay: document.getElementById('comboDisplay'),
        passedDisplay: document.getElementById('passedDisplay'),
        progressFill: document.getElementById('progressFill'),
        typeBadge: document.getElementById('typeBadge'),
        typeNameDisplay: document.getElementById('typeNameDisplay'),
        questionText: document.getElementById('questionText'),
        feedback: document.getElementById('feedback'),
        timerFill: document.getElementById('timerFill'),
        timerText: document.getElementById('timerText'),
        challengeCard: document.getElementById('challengeCard'),
        gameContent: document.getElementById('gameContent'),
        rankSection: document.getElementById('rankSection'),
        rankList: document.getElementById('rankList'),
        statusBar: document.getElementById('statusBar'),
        progressBar: document.getElementById('progressBar'),
        timerSection: document.getElementById('timerSection')
    };

    var abState = { password: '', digitCount: 4, maxAttempts: 15, currentGuess: '', attemptCount: 0, history: [], gameSolved: false, isActive: false };

    var colorHexMap = {
        '红色': '#FF0000', '深红': '#8B0000', '粉色': '#FF69B4', '浅粉': '#FFB6C1',
        '蓝色': '#0000FF', '深蓝': '#00008B', '天蓝': '#87CEEB', '青色': '#00CED1',
        '绿色': '#00AA00', '深绿': '#006400', '草绿': '#7CFC00', '黄色': '#FFD700',
        '金色': '#DAA520', '橙色': '#FF6600', '深橙': '#CC5500', '紫色': '#8800CC',
        '深紫': '#4B0082', '棕色': '#8B4513', '灰色': '#808080', '黑色': '#000000',
        '白色': '#FFFFFF', '银色': '#C0C0C0', '紫罗兰': '#EE82EE', '靛蓝': '#4B0082',
        '玫瑰红': '#FF007F', '柠檬黄': '#FFF700', '薄荷绿': '#98FF98', '珊瑚橙': '#FF7F50',
        '象牙白': '#FFFFF0', '巧克力棕': '#D2691E', '琥珀金': '#FFBF00', '翠绿': '#50C878',
        '宝石蓝': '#0F52BA', '玛瑙红': '#C04000', '珍珠白': '#F5F5F5', '青蓝': '#00BFFF',
        '紫红': '#C71585', '橙红': '#FF4500', '黄绿': '#9ACD32', '蓝紫': '#8A2BE2',
        '粉红': '#FFB6C1', '米色': '#F5F5DC', '卡其': '#F0E68C', '珊瑚': '#FF7F50',
        '青绿': '#008B8B', '靛青': '#4B0082', '藏青': '#000080', '酒红': '#800020',
        '橄榄绿': '#556B2F', '石板灰': '#708090', '杏色': '#FFDAB9', '薰衣草': '#E6E6FA',
        '紫藤': '#C9A0DC', '樱花粉': '#FFB7C5', '奶油': '#FFFDD0', '浅绿': '#90EE90',
        '浅蓝': '#ADD8E6', '浅紫': '#D8BFD8', '浅黄': '#FFFFE0', '浅橙': '#FFDAB9',
        '浅灰': '#D3D3D3', '深灰': '#A9A9A9', '墨绿': '#006400', '海军蓝': '#000080',
        '克莱因蓝': '#002FA7', '蒂芙尼蓝': '#81D8D0', '马卡龙粉': '#FFB5C5',
        '马卡龙蓝': '#A7D8DE', '马卡龙黄': '#FDE8B6', '马卡龙紫': '#C9B1E0',
        '马卡龙绿': '#B5D4C5', '莫兰迪粉': '#DDB5B5', '莫兰迪蓝': '#A8BCCD',
        '莫兰迪绿': '#B5C4B5', '莫兰迪紫': '#C4B5D4', '荧光粉': '#FF1493',
        '荧光绿': '#00FF00', '荧光黄': '#CCFF00', '荧光橙': '#FF6B00',
        '暗红': '#8B1A1A', '暗蓝': '#1A2A6B', '暗绿': '#1A4A2A', '暗紫': '#4A1A6B',
        '桃色': '#FFDAB9', '珊瑚粉': '#F08080', '橙黄': '#FFA500', '柠檬': '#FFF44F',
        '海蓝': '#2E8B57', '天空': '#87CEEB', '薰衣草紫': '#E6E6FA'
    };

    function getColorHex(name) { return colorHexMap[name] || '#808080'; }

    // ============================================================
    // 教程数据
    // ============================================================
    var gameTutorials = {
        'text': {
            name: '文字识别', icon: '👁️',
            desc: '识别图片中扭曲的文字，考验视力和抗干扰能力。',
            howTo: '观察下方图片中扭曲、带有噪点和干扰线的文字，从选项中选择正确的文字内容。图片中的文字可能被旋转、拉伸、添加了噪点，需要仔细辨认。',
            example: '图片中显示的是 "HELLO" 的扭曲版本，正确答案是 "HELLO"。',
            demo: function() {
                return '<div style="text-align:center;padding:0.3rem;">' +
                    '<div style="background:rgba(255,255,255,0.02);border-radius:12px;padding:0.5rem;border:1px solid rgba(255,255,255,0.04);">' +
                    '<div style="font-size:2.8rem;font-weight:bold;color:#8B5CF6;letter-spacing:8px;font-family:monospace;text-shadow:2px 2px 0 rgba(139,92,246,0.1), -1px -1px 0 rgba(236,72,153,0.05);transform:rotate(-2deg) scale(1.02);">H E L L O</div>' +
                    '<div style="color:rgba(255,255,255,0.08);font-size:0.6rem;margin-top:0.1rem;">👆 识别图片中的文字</div>' +
                    '</div>' +
                    '<div style="display:flex;gap:0.4rem;justify-content:center;margin-top:0.4rem;flex-wrap:wrap;">' +
                    '<span style="padding:0.2rem 0.8rem;border-radius:20px;background:rgba(255,255,255,0.02);border:1px solid rgba(255,255,255,0.04);color:rgba(255,255,255,0.2);font-size:0.85rem;">HELL0</span>' +
                    '<span style="padding:0.2rem 0.8rem;border-radius:20px;background:rgba(40,167,69,0.06);border:1px solid rgba(40,167,69,0.04);color:#28a745;font-size:0.85rem;font-weight:600;">✅ HELLO</span>' +
                    '<span style="padding:0.2rem 0.8rem;border-radius:20px;background:rgba(255,255,255,0.02);border:1px solid rgba(255,255,255,0.04);color:rgba(255,255,255,0.2);font-size:0.85rem;">HALLO</span>' +
                    '</div>' +
                    '<div style="color:rgba(40,167,69,0.15);font-size:0.65rem;margin-top:0.2rem;">💡 仔细辨认扭曲的文字</div>' +
                    '</div>';
            }
        },
        'arithmetic': {
            name: '算术计算', icon: '🧮',
            desc: '快速计算数学表达式，测试心算能力。',
            howTo: '计算给出的数学表达式，从选项中选择正确的答案。随着难度提升，数字会越来越大，运算也会更复杂。',
            example: '25 × 4 = 100，正确答案是 100。',
            demo: function() {
                return '<div style="text-align:center;padding:0.3rem;">' +
                    '<div style="font-size:2rem;font-weight:700;color:#fff;">25 × 4 = ?</div>' +
                    '<div style="display:flex;gap:0.4rem;justify-content:center;margin-top:0.4rem;flex-wrap:wrap;">' +
                    '<span style="padding:0.2rem 0.8rem;border-radius:20px;background:rgba(255,255,255,0.02);border:1px solid rgba(255,255,255,0.04);color:rgba(255,255,255,0.2);font-size:0.85rem;">80</span>' +
                    '<span style="padding:0.2rem 0.8rem;border-radius:20px;background:rgba(40,167,69,0.06);border:1px solid rgba(40,167,69,0.04);color:#28a745;font-size:0.85rem;font-weight:600;">✅ 100</span>' +
                    '<span style="padding:0.2rem 0.8rem;border-radius:20px;background:rgba(255,255,255,0.02);border:1px solid rgba(255,255,255,0.04);color:rgba(255,255,255,0.2);font-size:0.85rem;">120</span>' +
                    '</div>' +
                    '<div style="color:rgba(40,167,69,0.15);font-size:0.65rem;margin-top:0.2rem;">💡 快速心算</div>' +
                    '</div>';
            }
        },
        'stroke': {
            name: '汉字笔画', icon: '📝',
            desc: '计算汉字的笔画数，测试对汉字结构的了解。',
            howTo: '数出给定汉字的笔画总数（如 "人" 是 2 画），从选项中选择正确的数字。注意区分不同字形的笔画差异。',
            example: '汉字「人」有 2 画，正确答案是 2。',
            demo: function() {
                return '<div style="text-align:center;padding:0.3rem;">' +
                    '<div style="font-size:4rem;font-weight:700;color:#fff;">人</div>' +
                    '<div style="color:rgba(255,255,255,0.08);font-size:0.7rem;">"人" 字有几画？</div>' +
                    '<div style="display:flex;gap:0.4rem;justify-content:center;margin-top:0.4rem;flex-wrap:wrap;">' +
                    '<span style="padding:0.2rem 0.8rem;border-radius:20px;background:rgba(255,255,255,0.02);border:1px solid rgba(255,255,255,0.04);color:rgba(255,255,255,0.2);font-size:0.85rem;">1</span>' +
                    '<span style="padding:0.2rem 0.8rem;border-radius:20px;background:rgba(40,167,69,0.06);border:1px solid rgba(40,167,69,0.04);color:#28a745;font-size:0.85rem;font-weight:600;">✅ 2</span>' +
                    '<span style="padding:0.2rem 0.8rem;border-radius:20px;background:rgba(255,255,255,0.02);border:1px solid rgba(255,255,255,0.04);color:rgba(255,255,255,0.2);font-size:0.85rem;">3</span>' +
                    '</div>' +
                    '<div style="color:rgba(40,167,69,0.15);font-size:0.65rem;margin-top:0.2rem;">💡 一画一画数</div>' +
                    '</div>';
            }
        },
        'color': {
            name: '颜色识别', icon: '🎨',
            desc: '识别文字的颜色而不是文字本身，测试注意力。',
            howTo: '注意文字的颜色，而不是文字的内容。例如 "红" 字用蓝色显示，正确答案是 "蓝色"。',
            example: '文字 "红" 用蓝色显示，正确答案是 "蓝色"。',
            demo: function() {
                return '<div style="text-align:center;padding:0.3rem;">' +
                    '<div style="font-size:3rem;font-weight:700;color:#0000FF;">红</div>' +
                    '<div style="color:rgba(255,255,255,0.08);font-size:0.7rem;">这个字是什么颜色？（不是读字）</div>' +
                    '<div style="display:flex;gap:0.4rem;justify-content:center;margin-top:0.4rem;flex-wrap:wrap;">' +
                    '<span style="padding:0.2rem 0.8rem;border-radius:20px;background:rgba(255,255,255,0.02);border:1px solid rgba(255,255,255,0.04);color:rgba(255,255,255,0.2);font-size:0.85rem;">红色</span>' +
                    '<span style="padding:0.2rem 0.8rem;border-radius:20px;background:rgba(40,167,69,0.06);border:1px solid rgba(40,167,69,0.04);color:#28a745;font-size:0.85rem;font-weight:600;">✅ 蓝色</span>' +
                    '<span style="padding:0.2rem 0.8rem;border-radius:20px;background:rgba(255,255,255,0.02);border:1px solid rgba(255,255,255,0.04);color:rgba(255,255,255,0.2);font-size:0.85rem;">绿色</span>' +
                    '</div>' +
                    '<div style="color:rgba(40,167,69,0.15);font-size:0.65rem;margin-top:0.2rem;">💡 看颜色，不看文字</div>' +
                    '</div>';
            }
        },
        'findDifferent': {
            name: '找不同', icon: '🔍',
            desc: '记住原始字符序列，找出被替换的那个字符。',
            howTo: '先记住显示的原始字符（有记忆时间），然后字符会被打乱并替换其中一个，找出被替换的字符。',
            example: '原始: ABCDE → 打乱后: ABXDE，被替换的是 "C"。',
            demo: function() {
                return '<div style="text-align:center;padding:0.3rem;">' +
                    '<div style="font-size:2.2rem;font-weight:700;color:#8B5CF6;letter-spacing:10px;font-family:monospace;animation:pulseText 1.5s ease-in-out infinite;">A B C D E</div>' +
                    '<div style="color:rgba(255,255,255,0.05);font-size:0.6rem;">⏱ 记忆 3 秒</div>' +
                    '<div style="font-size:2.2rem;font-weight:700;color:#8B5CF6;letter-spacing:10px;font-family:monospace;margin-top:0.2rem;">A B X D E</div>' +
                    '<div style="color:rgba(255,255,255,0.05);font-size:0.6rem;">🔍 哪个字符被替换了？</div>' +
                    '<div style="display:flex;gap:0.4rem;justify-content:center;margin-top:0.4rem;flex-wrap:wrap;">' +
                    '<span style="padding:0.2rem 0.8rem;border-radius:20px;background:rgba(255,255,255,0.02);border:1px solid rgba(255,255,255,0.04);color:rgba(255,255,255,0.2);font-size:0.85rem;">A</span>' +
                    '<span style="padding:0.2rem 0.8rem;border-radius:20px;background:rgba(40,167,69,0.06);border:1px solid rgba(40,167,69,0.04);color:#28a745;font-size:0.85rem;font-weight:600;">✅ C</span>' +
                    '<span style="padding:0.2rem 0.8rem;border-radius:20px;background:rgba(255,255,255,0.02);border:1px solid rgba(255,255,255,0.04);color:rgba(255,255,255,0.2);font-size:0.85rem;">D</span>' +
                    '</div>' +
                    '<div style="color:rgba(40,167,69,0.15);font-size:0.65rem;margin-top:0.2rem;">💡 找出被替换的字符</div>' +
                    '</div>';
            }
        },
        'reverse': {
            name: '倒序识别', icon: '🔄',
            desc: '识别倒过来显示的文字，考验空间想象力。',
            howTo: '图片中的文字是倒过来显示的，你需要识别出正确的文字内容。可以旋转屏幕或在脑海中倒转文字。',
            example: '图片显示 "OLLEH"，正确答案是 "HELLO"。',
            demo: function() {
                return '<div style="text-align:center;padding:0.3rem;">' +
                    '<div style="font-size:2.8rem;font-weight:700;color:#8B5CF6;letter-spacing:8px;font-family:monospace;transform:rotate(180deg);display:inline-block;background:rgba(255,255,255,0.02);padding:0.3rem 0.8rem;border-radius:12px;">HELLO</div>' +
                    '<div style="color:rgba(255,255,255,0.05);font-size:0.6rem;margin-top:0.2rem;">🔄 倒过来看是什么？</div>' +
                    '<div style="display:flex;gap:0.4rem;justify-content:center;margin-top:0.4rem;flex-wrap:wrap;">' +
                    '<span style="padding:0.2rem 0.8rem;border-radius:20px;background:rgba(255,255,255,0.02);border:1px solid rgba(255,255,255,0.04);color:rgba(255,255,255,0.2);font-size:0.85rem;">OLLEH</span>' +
                    '<span style="padding:0.2rem 0.8rem;border-radius:20px;background:rgba(40,167,69,0.06);border:1px solid rgba(40,167,69,0.04);color:#28a745;font-size:0.85rem;font-weight:600;">✅ HELLO</span>' +
                    '<span style="padding:0.2rem 0.8rem;border-radius:20px;background:rgba(255,255,255,0.02);border:1px solid rgba(255,255,255,0.04);color:rgba(255,255,255,0.2);font-size:0.85rem;">HALLO</span>' +
                    '</div>' +
                    '<div style="color:rgba(40,167,69,0.15);font-size:0.65rem;margin-top:0.2rem;">💡 倒过来读</div>' +
                    '</div>';
            }
        },
        'missingLetter': {
            name: '空缺字母', icon: '🔤',
            desc: '补全英文单词中缺失的字母，测试词汇量。',
            howTo: '英文单词中有字母被隐藏了，根据上下文和词汇知识补全缺失的字母。',
            example: 'A P P _ E → 缺失 "L"，单词是 APPLE。',
            demo: function() {
                return '<div style="text-align:center;padding:0.3rem;">' +
                    '<div style="font-size:2.4rem;font-weight:700;color:#8B5CF6;letter-spacing:10px;font-family:monospace;">A P P _ E</div>' +
                    '<div style="color:rgba(255,255,255,0.05);font-size:0.6rem;">🔤 缺失哪个字母？</div>' +
                    '<div style="display:flex;gap:0.4rem;justify-content:center;margin-top:0.4rem;flex-wrap:wrap;">' +
                    '<span style="padding:0.2rem 0.8rem;border-radius:20px;background:rgba(255,255,255,0.02);border:1px solid rgba(255,255,255,0.04);color:rgba(255,255,255,0.2);font-size:0.85rem;">M</span>' +
                    '<span style="padding:0.2rem 0.8rem;border-radius:20px;background:rgba(40,167,69,0.06);border:1px solid rgba(40,167,69,0.04);color:#28a745;font-size:0.85rem;font-weight:600;">✅ L</span>' +
                    '<span style="padding:0.2rem 0.8rem;border-radius:20px;background:rgba(255,255,255,0.02);border:1px solid rgba(255,255,255,0.04);color:rgba(255,255,255,0.2);font-size:0.85rem;">N</span>' +
                    '</div>' +
                    '<div style="color:rgba(40,167,69,0.15);font-size:0.65rem;margin-top:0.2rem;">💡 补全英文单词</div>' +
                    '</div>';
            }
        },
        'timeCalc': {
            name: '时间计算', icon: '⏰',
            desc: '计算两个时间点之间的差值，测试时间感。',
            howTo: '计算从开始时间到结束时间经过了多少时间，选择正确的时长。注意小时和分钟的换算。',
            example: '从 08:00 到 12:30 经过了 4小时30分钟。',
            demo: function() {
                return '<div style="text-align:center;padding:0.3rem;">' +
                    '<div style="font-size:1.3rem;font-weight:700;color:#fff;">⏰ 从 08:00 到 12:30</div>' +
                    '<div style="color:rgba(255,255,255,0.05);font-size:0.6rem;">经过了多少时间？</div>' +
                    '<div style="display:flex;gap:0.4rem;justify-content:center;margin-top:0.4rem;flex-wrap:wrap;">' +
                    '<span style="padding:0.2rem 0.8rem;border-radius:20px;background:rgba(255,255,255,0.02);border:1px solid rgba(255,255,255,0.04);color:rgba(255,255,255,0.2);font-size:0.85rem;">4小时</span>' +
                    '<span style="padding:0.2rem 0.8rem;border-radius:20px;background:rgba(40,167,69,0.06);border:1px solid rgba(40,167,69,0.04);color:#28a745;font-size:0.85rem;font-weight:600;">✅ 4小时30分钟</span>' +
                    '<span style="padding:0.2rem 0.8rem;border-radius:20px;background:rgba(255,255,255,0.02);border:1px solid rgba(255,255,255,0.04);color:rgba(255,255,255,0.2);font-size:0.85rem;">5小时</span>' +
                    '</div>' +
                    '<div style="color:rgba(40,167,69,0.15);font-size:0.65rem;margin-top:0.2rem;">💡 计算时间差</div>' +
                    '</div>';
            }
        },
        'idiom': {
            name: '成语填空', icon: '📖',
            desc: '补全成语中缺失的字，测试语文功底。',
            howTo: '成语中有字被隐藏了，根据上下文和成语知识补全。',
            example: '一马当_ → 缺失 "先"，成语是 "一马当先"。',
            demo: function() {
                return '<div style="text-align:center;padding:0.3rem;">' +
                    '<div style="font-size:2.4rem;font-weight:700;color:#8B5CF6;letter-spacing:10px;">一马当_</div>' +
                    '<div style="color:rgba(255,255,255,0.05);font-size:0.6rem;">📖 缺失哪个字？</div>' +
                    '<div style="display:flex;gap:0.4rem;justify-content:center;margin-top:0.4rem;flex-wrap:wrap;">' +
                    '<span style="padding:0.2rem 0.8rem;border-radius:20px;background:rgba(255,255,255,0.02);border:1px solid rgba(255,255,255,0.04);color:rgba(255,255,255,0.2);font-size:0.85rem;">前</span>' +
                    '<span style="padding:0.2rem 0.8rem;border-radius:20px;background:rgba(40,167,69,0.06);border:1px solid rgba(40,167,69,0.04);color:#28a745;font-size:0.85rem;font-weight:600;">✅ 先</span>' +
                    '<span style="padding:0.2rem 0.8rem;border-radius:20px;background:rgba(255,255,255,0.02);border:1px solid rgba(255,255,255,0.04);color:rgba(255,255,255,0.2);font-size:0.85rem;">后</span>' +
                    '</div>' +
                    '<div style="color:rgba(40,167,69,0.15);font-size:0.65rem;margin-top:0.2rem;">💡 补全成语</div>' +
                    '</div>';
            }
        },
        'memory': {
            name: '数字记忆', icon: '🧠',
            desc: '记住显示的数字并在消失后输入，测试短期记忆力。',
            howTo: '数字会显示几秒钟然后消失，在键盘上输入你记住的数字。',
            example: '显示 "3847" → 输入 "3847"。',
            demo: function() {
                return '<div style="text-align:center;padding:0.3rem;">' +
                    '<div style="font-size:2.8rem;font-weight:700;color:#8B5CF6;letter-spacing:14px;font-family:monospace;">3 8 4 7</div>' +
                    '<div style="color:rgba(255,255,255,0.05);font-size:0.6rem;">⏱ 记忆 3 秒</div>' +
                    '<div style="font-size:2.8rem;font-weight:700;color:rgba(255,255,255,0.05);letter-spacing:14px;font-family:monospace;margin-top:0.2rem;">_ _ _ _</div>' +
                    '<div style="color:rgba(255,255,255,0.05);font-size:0.6rem;">⌨️ 输入你记住的数字</div>' +
                    '<div style="color:rgba(40,167,69,0.15);font-size:0.65rem;margin-top:0.2rem;">💡 记住数字并输入</div>' +
                    '</div>';
            }
        },
        'pattern': {
            name: '找规律', icon: '📐',
            desc: '根据数列规律填空，测试数学推理能力。',
            howTo: '观察数列的规律，找出缺失的数字。规律可能是等差、等比、递推等。',
            example: '2, 4, 6, ?, 10 → 规律是+2，答案是 8。',
            demo: function() {
                return '<div style="text-align:center;padding:0.3rem;">' +
                    '<div style="font-size:1.8rem;font-weight:700;color:#fff;">2, 4, 6, ?, 10</div>' +
                    '<div style="color:rgba(255,255,255,0.05);font-size:0.6rem;">📐 规律是什么？</div>' +
                    '<div style="display:flex;gap:0.4rem;justify-content:center;margin-top:0.4rem;flex-wrap:wrap;">' +
                    '<span style="padding:0.2rem 0.8rem;border-radius:20px;background:rgba(255,255,255,0.02);border:1px solid rgba(255,255,255,0.04);color:rgba(255,255,255,0.2);font-size:0.85rem;">6</span>' +
                    '<span style="padding:0.2rem 0.8rem;border-radius:20px;background:rgba(40,167,69,0.06);border:1px solid rgba(40,167,69,0.04);color:#28a745;font-size:0.85rem;font-weight:600;">✅ 8</span>' +
                    '<span style="padding:0.2rem 0.8rem;border-radius:20px;background:rgba(255,255,255,0.02);border:1px solid rgba(255,255,255,0.04);color:rgba(255,255,255,0.2);font-size:0.85rem;">9</span>' +
                    '</div>' +
                    '<div style="color:rgba(40,167,69,0.15);font-size:0.65rem;margin-top:0.2rem;">💡 找规律填空</div>' +
                    '</div>';
            }
        },
        'colorMix': {
            name: '颜色混合', icon: '🎨',
            desc: '将几种颜色混合后判断结果颜色，测试色彩感知。',
            howTo: '观察给出的颜色，混合后是什么颜色？从选项中选择正确的颜色。',
            example: '红色 + 蓝色 = 紫色。',
            demo: function() {
                return '<div style="text-align:center;padding:0.3rem;">' +
                    '<div style="display:flex;justify-content:center;align-items:center;gap:0.6rem;flex-wrap:wrap;">' +
                    '<div style="width:44px;height:44px;border-radius:10px;background:#FF0000;border:1px solid rgba(255,255,255,0.06);"></div>' +
                    '<span style="color:rgba(255,255,255,0.06);font-size:1rem;">+</span>' +
                    '<div style="width:44px;height:44px;border-radius:10px;background:#0000FF;border:1px solid rgba(255,255,255,0.06);"></div>' +
                    '<span style="color:rgba(255,255,255,0.06);font-size:1rem;">→</span>' +
                    '<div style="width:44px;height:44px;border-radius:10px;background:rgba(139,92,246,0.3);border:2px dashed rgba(255,255,255,0.06);display:flex;align-items:center;justify-content:center;font-size:1.2rem;color:rgba(255,255,255,0.06);">?</div>' +
                    '</div>' +
                    '<div style="display:flex;gap:0.4rem;justify-content:center;margin-top:0.4rem;flex-wrap:wrap;">' +
                    '<span style="padding:0.2rem 0.8rem;border-radius:20px;background:rgba(255,255,255,0.02);border:1px solid rgba(255,255,255,0.04);color:rgba(255,255,255,0.2);font-size:0.85rem;">红色</span>' +
                    '<span style="padding:0.2rem 0.8rem;border-radius:20px;background:rgba(40,167,69,0.06);border:1px solid rgba(40,167,69,0.04);color:#28a745;font-size:0.85rem;font-weight:600;">✅ 紫色</span>' +
                    '<span style="padding:0.2rem 0.8rem;border-radius:20px;background:rgba(255,255,255,0.02);border:1px solid rgba(255,255,255,0.04);color:rgba(255,255,255,0.2);font-size:0.85rem;">蓝色</span>' +
                    '</div>' +
                    '<div style="color:rgba(40,167,69,0.15);font-size:0.65rem;margin-top:0.2rem;">💡 混合颜色</div>' +
                    '</div>';
            }
        },
        'trueFalse': {
            name: '真假判断', icon: '⚖️',
            desc: '判断陈述的真假，测试常识和逻辑推理。',
            howTo: '阅读陈述内容，判断它是真的还是假的。注意细节和逻辑陷阱。',
            example: '地球是平的 → 假的。',
            demo: function() {
                return '<div style="text-align:center;padding:0.3rem;">' +
                    '<div style="font-size:1.1rem;color:#fff;">🌍 地球是平的</div>' +
                    '<div style="color:rgba(255,255,255,0.05);font-size:0.6rem;">⚖️ 真的还是假的？</div>' +
                    '<div style="display:flex;gap:0.4rem;justify-content:center;margin-top:0.4rem;flex-wrap:wrap;">' +
                    '<span style="padding:0.2rem 0.8rem;border-radius:20px;background:rgba(255,255,255,0.02);border:1px solid rgba(255,255,255,0.04);color:rgba(255,255,255,0.2);font-size:0.85rem;">✅ 真的</span>' +
                    '<span style="padding:0.2rem 0.8rem;border-radius:20px;background:rgba(40,167,69,0.06);border:1px solid rgba(40,167,69,0.04);color:#28a745;font-size:0.85rem;font-weight:600;">✅ 假的</span>' +
                    '</div>' +
                    '<div style="color:rgba(40,167,69,0.15);font-size:0.65rem;margin-top:0.2rem;">💡 判断真假</div>' +
                    '</div>';
            }
        },
        'puzzle': {
            name: '数字华容道', icon: '🧩',
            desc: '移动数字方块，按顺序排列，测试空间思维。',
            howTo: '点击数字方块移动，将数字按 1-8 的顺序排列，空格在右下角。',
            example: '将 1-8 按顺序排列，空格在最后。',
            demo: function() {
                return '<div style="text-align:center;padding:0.3rem;">' +
                    '<div style="display:grid;grid-template-columns:repeat(3,1fr);gap:3px;max-width:140px;margin:0 auto;background:rgba(255,255,255,0.02);padding:4px;border-radius:10px;border:1px solid rgba(255,255,255,0.04);">' +
                    '<div style="aspect-ratio:1;display:flex;align-items:center;justify-content:center;font-size:1.1rem;font-weight:700;border-radius:6px;background:rgba(255,255,255,0.04);color:#fff;">1</div>' +
                    '<div style="aspect-ratio:1;display:flex;align-items:center;justify-content:center;font-size:1.1rem;font-weight:700;border-radius:6px;background:rgba(255,255,255,0.04);color:#fff;">2</div>' +
                    '<div style="aspect-ratio:1;display:flex;align-items:center;justify-content:center;font-size:1.1rem;font-weight:700;border-radius:6px;background:rgba(255,255,255,0.04);color:#fff;">3</div>' +
                    '<div style="aspect-ratio:1;display:flex;align-items:center;justify-content:center;font-size:1.1rem;font-weight:700;border-radius:6px;background:rgba(255,255,255,0.04);color:#fff;">4</div>' +
                    '<div style="aspect-ratio:1;display:flex;align-items:center;justify-content:center;font-size:1.1rem;font-weight:700;border-radius:6px;background:rgba(255,255,255,0.04);color:#fff;">5</div>' +
                    '<div style="aspect-ratio:1;display:flex;align-items:center;justify-content:center;font-size:1.1rem;font-weight:700;border-radius:6px;background:rgba(255,255,255,0.04);color:#fff;">6</div>' +
                    '<div style="aspect-ratio:1;display:flex;align-items:center;justify-content:center;font-size:1.1rem;font-weight:700;border-radius:6px;background:rgba(255,255,255,0.04);color:#fff;">7</div>' +
                    '<div style="aspect-ratio:1;display:flex;align-items:center;justify-content:center;font-size:1.1rem;font-weight:700;border-radius:6px;background:rgba(255,255,255,0.04);color:#fff;">8</div>' +
                    '<div style="aspect-ratio:1;display:flex;align-items:center;justify-content:center;font-size:1.1rem;font-weight:700;border-radius:6px;background:transparent;color:transparent;"></div>' +
                    '</div>' +
                    '<div style="color:rgba(40,167,69,0.15);font-size:0.65rem;margin-top:0.2rem;">💡 点击数字移动，按顺序排列</div>' +
                    '</div>';
            }
        },
        'threeView': {
            name: '立体三视图', icon: '📐',
            desc: '根据三视图判断正方体数量，测试空间想象力。',
            howTo: '观察俯视图、主视图、左视图，推断出总共用了多少个正方体。',
            example: '三个视图显示共有 8 个正方体。',
            demo: function() {
                return '<div style="text-align:center;padding:0.3rem;">' +
                    '<div style="display:flex;justify-content:center;gap:0.8rem;flex-wrap:wrap;">' +
                    '<div><div style="color:rgba(255,255,255,0.05);font-size:0.45rem;">俯视图</div><div style="display:grid;grid-template-columns:repeat(3,28px);gap:2px;"><div style="width:28px;height:28px;background:#8B5CF6;border-radius:2px;"></div><div style="width:28px;height:28px;background:#8B5CF6;border-radius:2px;"></div><div style="width:28px;height:28px;background:#8B5CF6;border-radius:2px;"></div><div style="width:28px;height:28px;background:#8B5CF6;border-radius:2px;"></div><div style="width:28px;height:28px;background:#8B5CF6;border-radius:2px;"></div><div style="width:28px;height:28px;background:#8B5CF6;border-radius:2px;"></div><div style="width:28px;height:28px;background:#8B5CF6;border-radius:2px;"></div><div style="width:28px;height:28px;background:#8B5CF6;border-radius:2px;"></div><div style="width:28px;height:28px;background:#8B5CF6;border-radius:2px;"></div></div></div>' +
                    '<div><div style="color:rgba(255,255,255,0.05);font-size:0.45rem;">主视图</div><div style="display:grid;grid-template-columns:repeat(3,28px);gap:2px;"><div style="width:28px;height:28px;background:#8B5CF6;border-radius:2px;"></div><div style="width:28px;height:28px;background:#8B5CF6;border-radius:2px;"></div><div style="width:28px;height:28px;background:#8B5CF6;border-radius:2px;"></div><div style="width:28px;height:28px;background:#8B5CF6;border-radius:2px;"></div><div style="width:28px;height:28px;background:#8B5CF6;border-radius:2px;"></div><div style="width:28px;height:28px;background:#8B5CF6;border-radius:2px;"></div><div style="width:28px;height:28px;background:#8B5CF6;border-radius:2px;"></div><div style="width:28px;height:28px;background:#8B5CF6;border-radius:2px;"></div><div style="width:28px;height:28px;background:#8B5CF6;border-radius:2px;"></div></div></div>' +
                    '<div><div style="color:rgba(255,255,255,0.05);font-size:0.45rem;">左视图</div><div style="display:grid;grid-template-columns:repeat(3,28px);gap:2px;"><div style="width:28px;height:28px;background:#8B5CF6;border-radius:2px;"></div><div style="width:28px;height:28px;background:#8B5CF6;border-radius:2px;"></div><div style="width:28px;height:28px;background:#8B5CF6;border-radius:2px;"></div><div style="width:28px;height:28px;background:#8B5CF6;border-radius:2px;"></div><div style="width:28px;height:28px;background:#8B5CF6;border-radius:2px;"></div><div style="width:28px;height:28px;background:#8B5CF6;border-radius:2px;"></div><div style="width:28px;height:28px;background:#8B5CF6;border-radius:2px;"></div><div style="width:28px;height:28px;background:#8B5CF6;border-radius:2px;"></div><div style="width:28px;height:28px;background:#8B5CF6;border-radius:2px;"></div></div></div>' +
                    '</div>' +
                    '<div style="color:rgba(40,167,69,0.15);font-size:0.65rem;margin-top:0.2rem;">💡 根据三视图推断正方体数量</div>' +
                    '</div>';
            }
        },
        'shapeCount': {
            name: '图形计数', icon: '🔢',
            desc: '统计特定图形出现的次数，测试观察力。',
            howTo: '在一组图形中，统计特定图形（如 ●）出现的次数。',
            example: '● ■ ● ▲ ● → ● 出现了 3 次。',
            demo: function() {
                return '<div style="text-align:center;padding:0.3rem;">' +
                    '<div style="font-size:1.8rem;letter-spacing:10px;">● ■ ● ▲ ●</div>' +
                    '<div style="color:rgba(255,255,255,0.05);font-size:0.6rem;">🔢 「●」出现了几次？</div>' +
                    '<div style="display:flex;gap:0.4rem;justify-content:center;margin-top:0.4rem;flex-wrap:wrap;">' +
                    '<span style="padding:0.2rem 0.8rem;border-radius:20px;background:rgba(255,255,255,0.02);border:1px solid rgba(255,255,255,0.04);color:rgba(255,255,255,0.2);font-size:0.85rem;">2</span>' +
                    '<span style="padding:0.2rem 0.8rem;border-radius:20px;background:rgba(40,167,69,0.06);border:1px solid rgba(40,167,69,0.04);color:#28a745;font-size:0.85rem;font-weight:600;">✅ 3</span>' +
                    '<span style="padding:0.2rem 0.8rem;border-radius:20px;background:rgba(255,255,255,0.02);border:1px solid rgba(255,255,255,0.04);color:rgba(255,255,255,0.2);font-size:0.85rem;">4</span>' +
                    '</div>' +
                    '<div style="color:rgba(40,167,69,0.15);font-size:0.65rem;margin-top:0.2rem;">💡 统计图形出现次数</div>' +
                    '</div>';
            }
        },
        'dateReasoning': {
            name: '日期推理', icon: '📅',
            desc: '根据已知日期推算星期几，测试日期计算能力。',
            howTo: '已知一个日期是星期几，推算另一个日期是星期几。注意月份天数和闰年。',
            example: '2026年8月7日是星期五，8月15日是星期六。',
            demo: function() {
                return '<div style="text-align:center;padding:0.3rem;">' +
                    '<div style="font-size:1rem;color:#fff;">📅 2026年8月7日是星期五</div>' +
                    '<div style="color:rgba(255,255,255,0.05);font-size:0.6rem;">8月15日是星期几？</div>' +
                    '<div style="display:flex;gap:0.4rem;justify-content:center;margin-top:0.4rem;flex-wrap:wrap;">' +
                    '<span style="padding:0.2rem 0.8rem;border-radius:20px;background:rgba(255,255,255,0.02);border:1px solid rgba(255,255,255,0.04);color:rgba(255,255,255,0.2);font-size:0.85rem;">星期五</span>' +
                    '<span style="padding:0.2rem 0.8rem;border-radius:20px;background:rgba(40,167,69,0.06);border:1px solid rgba(40,167,69,0.04);color:#28a745;font-size:0.85rem;font-weight:600;">✅ 星期六</span>' +
                    '<span style="padding:0.2rem 0.8rem;border-radius:20px;background:rgba(255,255,255,0.02);border:1px solid rgba(255,255,255,0.04);color:rgba(255,255,255,0.2);font-size:0.85rem;">星期日</span>' +
                    '</div>' +
                    '<div style="color:rgba(40,167,69,0.15);font-size:0.65rem;margin-top:0.2rem;">💡 推算星期几</div>' +
                    '</div>';
            }
        },
        'rotate': {
            name: '图形旋转', icon: '🔄',
            desc: '判断图形旋转了多少度，测试空间想象力。',
            howTo: '观察原图和旋转后的图形，判断旋转了多少度。',
            example: '三角形旋转了 90°。',
            demo: function() {
                return '<div style="text-align:center;padding:0.3rem;">' +
                    '<div style="display:flex;justify-content:center;align-items:center;gap:0.8rem;flex-wrap:wrap;">' +
                    '<div><svg width="52" height="52"><polygon points="26,8 44,44 8,44" fill="#8B5CF6" opacity="0.85"/></svg><div style="color:rgba(255,255,255,0.05);font-size:0.45rem;">原图</div></div>' +
                    '<span style="color:rgba(255,255,255,0.04);">→</span>' +
                    '<div><svg width="52" height="52"><polygon points="26,8 44,44 8,44" fill="#8B5CF6" opacity="0.85" transform="rotate(90,26,26)"/></svg><div style="color:rgba(255,255,255,0.05);font-size:0.45rem;">旋转后</div></div>' +
                    '</div>' +
                    '<div style="display:flex;gap:0.4rem;justify-content:center;margin-top:0.4rem;flex-wrap:wrap;">' +
                    '<span style="padding:0.2rem 0.8rem;border-radius:20px;background:rgba(255,255,255,0.02);border:1px solid rgba(255,255,255,0.04);color:rgba(255,255,255,0.2);font-size:0.85rem;">45°</span>' +
                    '<span style="padding:0.2rem 0.8rem;border-radius:20px;background:rgba(40,167,69,0.06);border:1px solid rgba(40,167,69,0.04);color:#28a745;font-size:0.85rem;font-weight:600;">✅ 90°</span>' +
                    '<span style="padding:0.2rem 0.8rem;border-radius:20px;background:rgba(255,255,255,0.02);border:1px solid rgba(255,255,255,0.04);color:rgba(255,255,255,0.2);font-size:0.85rem;">135°</span>' +
                    '</div>' +
                    '<div style="color:rgba(40,167,69,0.15);font-size:0.65rem;margin-top:0.2rem;">💡 判断旋转角度</div>' +
                    '</div>';
            }
        },
        'abgame': {
            name: '1A2B猜数字', icon: '🎯',
            desc: '猜一个不重复的数字组合，根据提示推理出正确答案。',
            howTo: '输入你猜测的数字组合，系统会返回 A 和 B 的提示：A 表示位置和数字都正确，B 表示数字正确但位置不对。',
            example: '密码是 1234，猜测 1243 → 提示 2A2B。',
            demo: function() {
                return '<div style="text-align:center;padding:0.3rem;">' +
                    '<div style="font-size:1.2rem;color:#8B5CF6;font-weight:700;letter-spacing:6px;font-family:monospace;">_ _ _ _</div>' +
                    '<div style="color:rgba(255,255,255,0.05);font-size:0.6rem;">🎯 猜一个4位不重复数字</div>' +
                    '<div style="display:flex;gap:0.4rem;justify-content:center;margin-top:0.3rem;flex-wrap:wrap;font-size:0.7rem;color:rgba(255,255,255,0.15);">' +
                    '<span><span style="color:#28a745;">A</span> = 位置和数字都正确</span>' +
                    '<span><span style="color:#F59E0B;">B</span> = 数字正确但位置不对</span>' +
                    '</div>' +
                    '<div style="color:rgba(40,167,69,0.15);font-size:0.65rem;margin-top:0.2rem;">💡 根据提示推理</div>' +
                    '</div>';
            }
        },
        'tripleColor': {
            name: '颜色三重干扰', icon: '🎯',
            desc: '在颜色、背景、文字含义的三重干扰中找出正确答案。',
            howTo: '文字的颜色、背景色和文字本身的含义可能不同，根据问题要求选择正确的颜色。',
            example: '文字 "红" 用蓝色显示，背景是绿色，问文字颜色 → 答案是蓝色。',
            demo: function() {
                return '<div style="text-align:center;padding:0.3rem;">' +
                    '<div style="background:#00AA00;padding:0.6rem 1.5rem;border-radius:12px;display:inline-block;">' +
                    '<span style="color:#0000FF;font-size:2.8rem;font-weight:900;letter-spacing:10px;">红</span>' +
                    '</div>' +
                    '<div style="color:rgba(255,255,255,0.05);font-size:0.6rem;margin-top:0.2rem;">🎯 文字颜色是什么？</div>' +
                    '<div style="display:flex;gap:0.4rem;justify-content:center;margin-top:0.4rem;flex-wrap:wrap;">' +
                    '<span style="padding:0.2rem 0.8rem;border-radius:20px;background:rgba(255,255,255,0.02);border:1px solid rgba(255,255,255,0.04);color:rgba(255,255,255,0.2);font-size:0.85rem;">红色</span>' +
                    '<span style="padding:0.2rem 0.8rem;border-radius:20px;background:rgba(40,167,69,0.06);border:1px solid rgba(40,167,69,0.04);color:#28a745;font-size:0.85rem;font-weight:600;">✅ 蓝色</span>' +
                    '<span style="padding:0.2rem 0.8rem;border-radius:20px;background:rgba(255,255,255,0.02);border:1px solid rgba(255,255,255,0.04);color:rgba(255,255,255,0.2);font-size:0.85rem;">绿色</span>' +
                    '</div>' +
                    '<div style="color:rgba(40,167,69,0.15);font-size:0.65rem;margin-top:0.2rem;">💡 注意干扰项</div>' +
                    '</div>';
            }
        }
    };

    var tutorialOrder = ['text','arithmetic','stroke','color','findDifferent','reverse','missingLetter','timeCalc','idiom','memory','pattern','colorMix','trueFalse','puzzle','threeView','shapeCount','dateReasoning','rotate','abgame','tripleColor'];
    var currentTutorialIndex = 0;

    // ============================================================
    // 开始界面
    // ============================================================
    function renderStartScreen() {
        var container = dom.gameContent;
        dom.typeBadge.style.display = 'none';
        dom.questionText.innerHTML = '';
        dom.statusBar.style.display = 'none';
        dom.progressBar.style.display = 'none';
        dom.timerSection.style.display = 'none';
        dom.rankSection.style.display = 'none';
        dom.feedback.className = 'feedback';
        dom.feedback.style.display = 'none';

        container.innerHTML = `
            <div class="start-screen">
                <span class="logo-icon">🧩</span>
                <div class="title">验证大闯关</div>
                <p class="desc">100关 · 20种玩法 · 难度递增</p>
                <div class="start-buttons">
                    <button class="btn-start primary" onclick="startGame()">🚀 开始游戏</button>
                    <button class="btn-start ranking" onclick="showRankingOnly()">🏆 排行榜</button>
                    <button class="btn-start tutorial-btn" onclick="showTutorial()">📖 玩法教程</button>
                </div>
                <div style="margin-top:1.2rem;color:rgba(255,255,255,0.04);font-size:0.65rem;">
                    💡 点击 "玩法教程" 了解每种玩法的详细说明
                </div>
            </div>
        `;
        dom.typeNameDisplay.textContent = '🎮 准备开始';
        dom.challengeCard.querySelector('.type-badge').style.display = 'none';
    }

    // ============================================================
    // 排行榜（独立）
    // ============================================================
    function showRankingOnly() {
        var container = dom.gameContent;
        container.innerHTML = `
            <div style="width:100%;padding:0.3rem 0;">
                <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:0.8rem;">
                    <div style="color:#fff;font-weight:600;font-size:1.1rem;">🏆 排行榜</div>
                    <button class="btn-start secondary" style="padding:0.3rem 1.2rem;border:none;border-radius:30px;background:rgba(255,255,255,0.04);color:rgba(255,255,255,0.3);cursor:pointer;font-family:inherit;font-size:0.8rem;" onclick="renderStartScreen()">← 返回</button>
                </div>
                <div id="rankListOnly"></div>
            </div>
        `;
        dom.typeNameDisplay.textContent = '🏆 排行榜';
        loadRankingOnly();
    }

    function loadRankingOnly() {
        var container = document.getElementById('rankListOnly');
        if (!container) return;
        fetch('/VerifyGame/GetRanking')
            .then(function(r) { return r.json(); })
            .then(function(data) {
                if (data.success && data.data && data.data.length > 0) {
                    var html = '';
                    var medals = ['🥇', '🥈', '🥉'];
                    data.data.forEach(function(item, index) {
                        var medal = index < 3 ? medals[index] : '#' + (index + 1);
                        var avatarHtml = item.avatarUrl && item.isAvatarApproved ?
                            '<img src="' + item.avatarUrl + '" style="width:26px;height:26px;border-radius:50%;object-fit:cover;border:1px solid rgba(255,255,255,0.04);flex-shrink:0;" />' :
                            '<div style="width:26px;height:26px;border-radius:50%;background:rgba(255,255,255,0.04);display:flex;align-items:center;justify-content:center;font-size:0.55rem;color:rgba(255,255,255,0.15);flex-shrink:0;">' + (item.username ? item.username.charAt(0).toUpperCase() : '?') + '</div>';
                        var isMe = item.isMe || false;
                        html += '<div style="display:flex;align-items:center;gap:0.5rem;padding:0.3rem 0.5rem;border-bottom:1px solid rgba(255,255,255,0.02);">' +
                            '<span style="font-size:0.8rem;min-width:28px;text-align:center;color:rgba(255,255,255,0.1);">' + medal + '</span>' +
                            avatarHtml +
                            '<span style="flex:1;color:rgba(255,255,255,0.3);font-size:0.8rem;">' + item.username + (isMe ? ' <span style="color:#8B5CF6;font-weight:600;">(我)</span>' : '') + '</span>' +
                            '<span style="color:#F59E0B;font-weight:700;font-size:0.85rem;">' + item.totalPoints + '</span>' +
                            '</div>';
                    });
                    container.innerHTML = html;
                } else {
                    container.innerHTML = '<div style="text-align:center;color:rgba(255,255,255,0.06);padding:1.5rem;">暂无排行数据</div>';
                }
            })
            .catch(function() {
                container.innerHTML = '<div style="text-align:center;color:rgba(255,255,255,0.06);padding:1.5rem;">加载失败</div>';
            });
    }

    // ============================================================
    // 教程系统
    // ============================================================
    function showTutorial() {
        currentTutorialIndex = 0;
        document.getElementById('tutorialOverlay').classList.add('active');
        renderTutorial(currentTutorialIndex);
        document.body.style.overflow = 'hidden';
        SoundFX.play('start');
    }

    function closeTutorial() {
        document.getElementById('tutorialOverlay').classList.remove('active');
        document.body.style.overflow = '';
        renderStartScreen();
    }

    function renderTutorial(index) {
        var container = document.getElementById('tutorialContent');
        var key = tutorialOrder[index];
        var data = gameTutorials[key];
        if (!data) return;

        var isDemo = typeof data.demo === 'function';
        var total = tutorialOrder.length;
        var prevDisabled = index === 0 ? 'disabled' : '';
        var nextDisabled = index === total - 1 ? 'disabled' : '';

        container.innerHTML = `
            <div class="tutorial-title">
                <span>${data.icon}</span>
                ${data.name}
                <span style="font-size:0.6rem;color:rgba(255,255,255,0.06);font-weight:400;margin-left:auto;">${index + 1}/${total}</span>
            </div>
            <div class="tutorial-sub">${data.desc}</div>

            <div class="tutorial-section">
                <div class="sec-title">📖 玩法说明</div>
                <div class="sec-content">${data.howTo}</div>
            </div>

            <div class="tutorial-section">
                <div class="sec-title">💡 示例</div>
                <div class="example-box">
                    <div class="example-label">📌 例题</div>
                    <div class="example-content">${data.example}</div>
                </div>
            </div>

            ${isDemo ? `
            <div class="tutorial-section">
                <div class="sec-title">🎬 交互演示</div>
                <div class="tutorial-demo-area active" id="demoArea">
                    ${data.demo()}
                </div>
            </div>
            ` : ''}

            <div class="tutorial-nav">
                <button class="nav-btn" onclick="renderTutorial(${index - 1})" ${prevDisabled}>← 上一项</button>
                ${isDemo ? `<button class="nav-btn demo-btn" onclick="replayDemo('${key}')">🔄 重播演示</button>` : ''}
                <button class="nav-btn ${index === total - 1 ? 'active' : ''}" onclick="${index === total - 1 ? 'closeTutorial()' : 'renderTutorial(' + (index + 1) + ')'}" ${nextDisabled}>
                    ${index === total - 1 ? '✅ 完成' : '下一项 →'}
                </button>
                <button class="nav-btn close-tutorial" onclick="closeTutorial()">✕ 关闭</button>
            </div>
        `;
    }

    function replayDemo(key) {
        var data = gameTutorials[key];
        if (!data || typeof data.demo !== 'function') return;
        var demoArea = document.getElementById('demoArea');
        if (demoArea) {
            demoArea.innerHTML = data.demo();
            var content = demoArea.querySelector('.demo-content');
            if (content) {
                content.style.animation = 'none';
                setTimeout(function() { content.style.animation = ''; }, 10);
            }
            SoundFX.play('start');
        }
    }

    // ============================================================
    // 游戏核心
    // ============================================================
    function startGame() {
        if (gameState.timer) { clearInterval(gameState.timer); gameState.timer = null; }
        gameState.level = 1; gameState.score = 0; gameState.lives = 3;
        gameState.combo = 0; gameState.maxCombo = 0; gameState.passed = 0;
        gameState.isPlaying = true; gameState.isAnswered = false;
        gameState.timeLeft = 15;
        gameState.currentChallenge = null;

        dom.statusBar.style.display = 'flex';
        dom.progressBar.style.display = 'block';
        dom.timerSection.style.display = 'block';
        dom.rankSection.style.display = 'none';
        dom.feedback.className = 'feedback';
        dom.feedback.style.display = 'none';
        dom.feedback.innerHTML = '';
        dom.challengeCard.querySelector('.type-badge').style.display = 'block';

        dom.typeNameDisplay.textContent = '📝 第 1 关 · 入门';
        updateUI();
        SoundFX.play('start');
        loadChallenge();
    }

    function loadChallenge() {
        if (!gameState.isPlaying) return;
        if (gameState.level > gameState.totalLevels) { gameWin(); return; }

        gameState.isAnswered = false;
        dom.feedback.className = 'feedback';
        dom.feedback.style.display = 'none';
        dom.gameContent.innerHTML = '';

        if (window._abCleanup) { window._abCleanup(); window._abCleanup = null; }

        var diffLabel = getDifficultyLabel(gameState.level);
        dom.typeBadge.textContent = '第 ' + gameState.level + ' 关';

        fetch('/VerifyGame/GetChallenge?level=' + gameState.level)
            .then(function(r) { return r.json(); })
            .then(function(data) {
                if (data.success) {
                    gameState.currentChallenge = data.data;
                    if (data.data.typeName) {
                        dom.typeNameDisplay.textContent = '🧩 ' + data.data.typeName + ' · ' + diffLabel;
                    }
                    renderChallenge(data.data);
                } else {
                    showToast('加载挑战失败，请重试', 'error');
                }
            })
            .catch(function() { showToast('网络错误，请刷新重试', 'error'); });
    }

    // ============================================================
    // 渲染挑战（核心渲染器）
    // ============================================================
    function renderChallenge(challenge) {
        gameState.currentChallenge = challenge;
        var typeName = challenge.typeName || challenge.type || '未知';
        var diffLabel = getDifficultyLabel(challenge.level);
        dom.typeBadge.textContent = '第 ' + challenge.level + ' 关 · ' + typeName + ' · ' + diffLabel;
        dom.typeNameDisplay.textContent = '🧩 ' + typeName + ' · ' + diffLabel;
        dom.questionText.innerHTML = challenge.question || '请完成以下挑战';

        var container = dom.gameContent;

        // 1A2B
        if (challenge.type === 'abgame') { renderABGame(challenge); return; }

        // 颜色混合
        if (challenge.type === 'colorMix') { renderColorMix(challenge); return; }

        // 图形旋转
        if (challenge.type === 'rotate' && challenge.originalSvg) {
            var rotateDiv = document.createElement('div');
            rotateDiv.className = 'rotate-compare';
            rotateDiv.innerHTML = `
                <div class="rotate-item">
                    <span class="label">📐 原图</span>
                    ${challenge.originalSvg}
                </div>
                <div class="rotate-arrow">→</div>
                <div class="rotate-item">
                    <span class="label">🔄 旋转后</span>
                    ${challenge.rotatedSvg || challenge.originalSvg}
                </div>
            `;
            container.appendChild(rotateDiv);
            renderOptions(challenge, container);
            startTimer(challenge.timeLimit || 15);
            updateUI();
            return;
        }

        // 找不同
        if (challenge.type === 'findDifferent' && challenge.originalDisplay) {
            renderFindDifferent(challenge);
            return;
        }

        // 数字记忆
        if (challenge.type === 'memory' && challenge.keyboardRows) {
            renderMemoryGame(challenge);
            return;
        }

        // 华容道
        if (challenge.type === 'puzzle' && challenge.puzzle) {
            renderPuzzle(challenge.puzzle);
            return;
        }

        // 三视图
        if (challenge.type === 'threeView') {
            renderThreeView(challenge);
            return;
        }

        // 通用渲染
        if (challenge.imageSvg) {
            var imgDiv = document.createElement('div');
            imgDiv.style.cssText = 'width:100%;text-align:center;margin:0.1rem 0 0.6rem;';
            imgDiv.innerHTML = '<div class="captcha-image">' + challenge.imageSvg + '</div>';
            container.appendChild(imgDiv);
        }

        if (challenge.displayHtml) {
            var colorDiv = document.createElement('div');
            colorDiv.className = 'color-display';
            colorDiv.innerHTML = challenge.displayHtml;
            container.appendChild(colorDiv);
        }

        if (challenge.displayText) {
            var textDiv = document.createElement('div');
            textDiv.className = 'display-text-container';
            textDiv.innerHTML = '<div class="display-text">' + challenge.displayText + '</div>';
            container.appendChild(textDiv);
        }

        if (challenge.type === 'shapeCount' && challenge.asciiArt) {
            var asciiDiv = document.createElement('div');
            asciiDiv.className = 'ascii-art';
            asciiDiv.textContent = challenge.asciiArt;
            container.appendChild(asciiDiv);
        }

        renderOptions(challenge, container);
        startTimer(challenge.timeLimit || 15);
        updateUI();
    }

    // ============================================================
    // 渲染选项（通用）
    // ============================================================
    function renderOptions(challenge, container) {
        if (challenge.options && challenge.options.length > 0) {
            var optGrid = document.createElement('div');
            optGrid.className = 'options-grid';
            var letters = ['A', 'B', 'C', 'D', 'E', 'F', 'G', 'H'];
            challenge.options.forEach(function(opt, idx) {
                var btn = document.createElement('button');
                btn.className = 'option-btn';
                btn.innerHTML = (idx < letters.length ? letters[idx] + '. ' : '') + opt;
                btn.dataset.answer = opt;
                btn.addEventListener('click', function() {
                    if (gameState.isAnswered) return;
                    submitAnswer(challenge, this.dataset.answer, this);
                });
                optGrid.appendChild(btn);
            });
            container.appendChild(optGrid);
        }
    }

    // ============================================================
    // 三视图
    // ============================================================
    function renderThreeView(challenge) {
        var container = dom.gameContent;
        var viewContainer = document.createElement('div');
        viewContainer.className = 'view-container';
        if (challenge.topView) {
            var topItem = document.createElement('div');
            topItem.className = 'view-item';
            topItem.innerHTML = challenge.topView;
            viewContainer.appendChild(topItem);
        }
        if (challenge.frontView) {
            var frontItem = document.createElement('div');
            frontItem.className = 'view-item';
            frontItem.innerHTML = challenge.frontView;
            viewContainer.appendChild(frontItem);
        }
        if (challenge.sideView) {
            var sideItem = document.createElement('div');
            sideItem.className = 'view-item';
            sideItem.innerHTML = challenge.sideView;
            viewContainer.appendChild(sideItem);
        }
        container.appendChild(viewContainer);
        renderOptions(challenge, container);
        startTimer(challenge.timeLimit || 20);
        updateUI();
    }

    // ============================================================
    // 颜色混合
    // ============================================================
    function renderColorMix(challenge) {
        var container = dom.gameContent;
        container.innerHTML = '';

        if (challenge.displayHtml) {
            var displayDiv = document.createElement('div');
            displayDiv.className = 'display-text-container';
            displayDiv.style.textAlign = 'center';
            displayDiv.style.margin = '0.1rem 0 0.6rem';
            displayDiv.innerHTML = challenge.displayHtml;
            container.appendChild(displayDiv);
        }

        var colorGrid = document.createElement('div');
        colorGrid.className = 'color-options-grid';

        if (challenge.optionsHtml && challenge.optionsHtml.trim().length > 0) {
            colorGrid.innerHTML = challenge.optionsHtml;
        } else if (challenge.options && challenge.options.length > 0) {
            var html = '';
            challenge.options.forEach(function(colorName) {
                var hex = getColorHex(colorName) || '#808080';
                var displayName = colorName.replace('色', '');
                html += '<div class="color-option" data-color="' + colorName + '">';
                html += '<div class="color-block" style="background:' + hex + ';"></div>';
                html += '<span class="color-label">' + displayName + '</span>';
                html += '</div>';
            });
            colorGrid.innerHTML = html;
        } else {
            renderOptions(challenge, container);
            startTimer(challenge.timeLimit || 15);
            updateUI();
            return;
        }

        colorGrid.querySelectorAll('.color-option').forEach(function(el) {
            el.addEventListener('click', function() {
                if (gameState.isAnswered) return;
                var answer = this.dataset.color;
                if (answer) submitColorMixAnswer(challenge, answer, this);
            });
        });

        container.appendChild(colorGrid);
        startTimer(challenge.timeLimit || 15);
        updateUI();
    }

    function submitColorMixAnswer(challenge, answer, element) {
        if (gameState.isAnswered) return;
        var isCorrect = answer === challenge.correctAnswer;
        var container = dom.gameContent;

        container.querySelectorAll('.color-option').forEach(function(el) {
            el.classList.add('disabled');
            if (el.dataset.color === challenge.correctAnswer) el.classList.add('correct');
        });

        if (!isCorrect) {
            element.classList.add('wrong');
            dom.challengeCard.classList.add('shake');
            setTimeout(function() { dom.challengeCard.classList.remove('shake'); }, 500);
        } else {
            element.classList.add('correct');
        }

        handleAnswerResult(challenge, isCorrect);
    }

    // ============================================================
    // 华容道
    // ============================================================
    var puzzleState = null, puzzleEmptyRow = 0, puzzleEmptyCol = 0;

    function renderPuzzle(puzzle) {
        var container = dom.gameContent;
        container.innerHTML = '';
        var hintDiv = document.createElement('div');
        hintDiv.style.cssText = 'color:rgba(255,255,255,0.06);font-size:0.65rem;text-align:center;margin-bottom:0.4rem;';
        hintDiv.textContent = '💡 点击数字 或 滑动方向 移动';
        container.appendChild(hintDiv);

        var grid = document.createElement('div');
        grid.className = 'puzzle-grid';
        grid.id = 'puzzleGrid';
        container.appendChild(grid);

        puzzleState = puzzle.map(function(row) { return row.slice(); });

        for (var r = 0; r < 3; r++) {
            for (var c = 0; c < 3; c++) {
                var cell = document.createElement('div');
                cell.className = 'puzzle-cell';
                var val = puzzle[r][c];
                if (val === 0) {
                    cell.classList.add('empty');
                    cell.textContent = '';
                    puzzleEmptyRow = r;
                    puzzleEmptyCol = c;
                } else {
                    cell.textContent = val;
                    cell.dataset.row = r;
                    cell.dataset.col = c;
                    cell.addEventListener('click', function() {
                        if (gameState.isAnswered) return;
                        var row = parseInt(this.dataset.row);
                        var col = parseInt(this.dataset.col);
                        movePuzzleTile(row, col);
                    });
                }
                grid.appendChild(cell);
            }
        }

        setupPuzzleSwipe(grid);
        startTimer(60);
    }

    function setupPuzzleSwipe(grid) {
        var startX = 0, startY = 0, isDragging = false;
        grid.addEventListener('mousedown', function(e) {
            startX = e.clientX; startY = e.clientY; isDragging = true; e.preventDefault();
        });
        grid.addEventListener('mouseup', function(e) {
            if (!isDragging) return;
            isDragging = false;
            handleSwipe(e.clientX - startX, e.clientY - startY);
        });
        grid.addEventListener('mouseleave', function() { isDragging = false; });
        grid.addEventListener('touchstart', function(e) {
            var touch = e.touches[0];
            startX = touch.clientX; startY = touch.clientY; isDragging = true; e.preventDefault();
        }, { passive: false });
        grid.addEventListener('touchmove', function(e) { e.preventDefault(); }, { passive: false });
        grid.addEventListener('touchend', function(e) {
            if (!isDragging) return;
            isDragging = false;
            var touch = e.changedTouches[0];
            handleSwipe(touch.clientX - startX, touch.clientY - startY);
        });
    }

    function handleSwipe(dx, dy) {
        if (gameState.isAnswered) return;
        if (Math.abs(dx) < 20 && Math.abs(dy) < 20) return;
        var targetRow = puzzleEmptyRow, targetCol = puzzleEmptyCol;
        if (Math.abs(dx) > Math.abs(dy)) {
            targetCol = dx > 0 ? puzzleEmptyCol - 1 : puzzleEmptyCol + 1;
        } else {
            targetRow = dy > 0 ? puzzleEmptyRow - 1 : puzzleEmptyRow + 1;
        }
        if (targetRow >= 0 && targetRow < 3 && targetCol >= 0 && targetCol < 3) {
            movePuzzleTile(targetRow, targetCol);
        }
    }

    function movePuzzleTile(row, col) {
        var dr = Math.abs(row - puzzleEmptyRow);
        var dc = Math.abs(col - puzzleEmptyCol);
        if ((dr === 1 && dc === 0) || (dr === 0 && dc === 1)) {
            puzzleState[puzzleEmptyRow][puzzleEmptyCol] = puzzleState[row][col];
            puzzleState[row][col] = 0;
            puzzleEmptyRow = row;
            puzzleEmptyCol = col;
            renderPuzzleState(puzzleState);
            if (checkPuzzleSolved(puzzleState)) {
                gameState.isAnswered = true;
                clearInterval(gameState.timer);
                handleAnswerResult(gameState.currentChallenge, true);
            }
        }
    }

    function renderPuzzleState(puzzle) {
        var cells = document.querySelectorAll('.puzzle-grid .puzzle-cell');
        var idx = 0;
        for (var r = 0; r < 3; r++) {
            for (var c = 0; c < 3; c++) {
                var val = puzzle[r][c];
                var cell = cells[idx];
                if (val === 0) {
                    cell.classList.add('empty');
                    cell.textContent = '';
                    cell.dataset.row = r; cell.dataset.col = c;
                    cell.onclick = null;
                } else {
                    cell.classList.remove('empty');
                    cell.textContent = val;
                    cell.dataset.row = r; cell.dataset.col = c;
                    cell.onclick = function() {
                        if (gameState.isAnswered) return;
                        var row = parseInt(this.dataset.row);
                        var col = parseInt(this.dataset.col);
                        movePuzzleTile(row, col);
                    };
                }
                idx++;
            }
        }
    }

    function checkPuzzleSolved(puzzle) {
        var expected = [[1,2,3],[4,5,6],[7,8,0]];
        for (var r = 0; r < 3; r++) {
            for (var c = 0; c < 3; c++) {
                if (puzzle[r][c] !== expected[r][c]) return false;
            }
        }
        return true;
    }

    // ============================================================
    // 1A2B 游戏
    // ============================================================
    function renderABGame(challenge) {
        var digitCount = challenge.digitCount || 4;
        var maxAttempts = challenge.maxAttempts || 15;
        var password = challenge.password || '';

        abState.password = password;
        abState.digitCount = digitCount;
        abState.maxAttempts = maxAttempts;
        abState.currentGuess = '';
        abState.attemptCount = 0;
        abState.history = [];
        abState.gameSolved = false;
        abState.isActive = true;

        var container = dom.gameContent;
        container.innerHTML = '';

        var ruleDiv = document.createElement('div');
        ruleDiv.style.cssText = 'background:rgba(139,92,246,0.03);border:1px solid rgba(139,92,246,0.04);border-radius:12px;padding:0.4rem 0.8rem;margin-bottom:0.5rem;width:100%;';
        ruleDiv.innerHTML = `
            <div style="color:rgba(255,255,255,0.15);font-size:0.7rem;display:flex;flex-wrap:wrap;gap:0.2rem 0.8rem;justify-content:center;">
                <span>🎯 猜 <strong style="color:#8B5CF6;">${digitCount}</strong> 位不重复数字（第一位不能是0）</span>
                <span style="color:rgba(255,255,255,0.04);">|</span>
                <span style="color:#28a745;">✅ <strong>A</strong> = 位置和数字都正确</span>
                <span style="color:#F59E0B;">🟡 <strong>B</strong> = 数字正确但位置不对</span>
            </div>
        `;
        container.appendChild(ruleDiv);

        var displayDiv = document.createElement('div');
        displayDiv.className = 'ab-display';
        displayDiv.id = 'abDisplay';
        displayDiv.textContent = ' '.repeat(digitCount).split('').join(' ');
        container.appendChild(displayDiv);

        var statusDiv = document.createElement('div');
        statusDiv.className = 'ab-status';
        statusDiv.id = 'abStatus';
        statusDiv.innerHTML = '💡 剩余 <strong id="abAttempts">' + maxAttempts + '</strong> 次机会';
        container.appendChild(statusDiv);

        var historyDiv = document.createElement('div');
        historyDiv.className = 'ab-history';
        historyDiv.id = 'abHistory';
        historyDiv.textContent = '📝 输入你的猜测...';
        container.appendChild(historyDiv);

        var keypadDiv = document.createElement('div');
        keypadDiv.className = 'ab-keypad';
        keypadDiv.id = 'abKeypad';
        var keypadRows = [['1','2','3'],['4','5','6'],['7','8','9'],['⌫','0','✅']];
        keypadRows.forEach(function(row) {
            row.forEach(function(key) {
                var btn = document.createElement('button');
                btn.className = 'ab-key-btn';
                btn.textContent = key;
                btn.dataset.key = key;
                if (key === '⌫') btn.classList.add('key-backspace');
                else if (key === '✅') btn.classList.add('key-submit');
                else btn.classList.add('key-number');
                btn.addEventListener('click', function() { handleABKeyPress(key); });
                keypadDiv.appendChild(btn);
            });
        });
        container.appendChild(keypadDiv);

        function handlePhysicalKey(e) {
            if (!abState.isActive || abState.gameSolved || gameState.isAnswered) return;
            var key = e.key;
            if (key >= '0' && key <= '9') { e.preventDefault(); handleABKeyPress(key); }
            else if (key === 'Backspace' || key === 'Delete') { e.preventDefault(); handleABKeyPress('⌫'); }
            else if (key === 'Enter') { e.preventDefault(); handleABKeyPress('✅'); }
        }
        document.addEventListener('keydown', handlePhysicalKey);
        window._abCleanup = function() { document.removeEventListener('keydown', handlePhysicalKey); };

        startTimer(challenge.timeLimit || 200);
    }

    function handleABKeyPress(key) {
        if (!abState.isActive || abState.gameSolved || gameState.isAnswered) return;
        var digitCount = abState.digitCount, maxAttempts = abState.maxAttempts, password = abState.password;
        var displayEl = document.getElementById('abDisplay');
        var historyEl = document.getElementById('abHistory');
        var attemptsEl = document.getElementById('abAttempts');

        if (key === '⌫') {
            if (abState.currentGuess.length > 0) {
                abState.currentGuess = abState.currentGuess.slice(0, -1);
                updateABDisplay();
            }
            return;
        }

        if (key === '✅') {
            if (abState.currentGuess.length !== digitCount) {
                showToast('请完整输入 ' + digitCount + ' 位数字', 'error'); return;
            }
            if (new Set(abState.currentGuess).size !== abState.currentGuess.length) {
                showToast('数字不能重复！', 'error'); return;
            }
            if (abState.currentGuess[0] === '0') {
                showToast('第一位不能是0！', 'error'); return;
            }

            abState.attemptCount++;
            var remaining = maxAttempts - abState.attemptCount;
            attemptsEl.textContent = remaining;

            var passwordArr = password.split(''), guessArr = abState.currentGuess.split('');
            var a = 0, b = 0, matchedIndexes = [];
            for (var i = 0; i < digitCount; i++) {
                if (guessArr[i] === passwordArr[i]) { a++; matchedIndexes.push(i); }
            }
            var unmatchedPassword = [], unmatchedGuess = [];
            for (var i = 0; i < digitCount; i++) {
                if (matchedIndexes.indexOf(i) === -1) {
                    unmatchedPassword.push(passwordArr[i]);
                    unmatchedGuess.push(guessArr[i]);
                }
            }
            for (var g of unmatchedGuess) {
                var idx = unmatchedPassword.indexOf(g);
                if (idx !== -1) { b++; unmatchedPassword.splice(idx, 1); }
            }

            var resultText = a + 'A' + b + 'B';
            var guessDisplay = abState.currentGuess.split('').join(' ');
            var historyItem = guessDisplay + ' → ' + resultText;
            abState.history.push(historyItem);
            updateABHistory();

            if (a === digitCount) {
                abState.gameSolved = true;
                abState.isActive = false;
                gameState.isAnswered = true;
                clearInterval(gameState.timer);
                disableABKeyboard();
                var points = 10 + Math.floor(gameState.level / 5) + Math.max(0, maxAttempts - abState.attemptCount);
                showFeedback(true, points, '🎉 ' + abState.attemptCount + ' 次猜中！');
                gameState.score += points;
                gameState.combo++;
                gameState.passed++;
                if (gameState.combo > gameState.maxCombo) gameState.maxCombo = gameState.combo;
                updateUI();
                setTimeout(function() { gameState.level++; updateUI(); loadChallenge(); }, 2000);
                return;
            }

            if (remaining <= 0) {
                abState.isActive = false;
                gameState.isAnswered = true;
                clearInterval(gameState.timer);
                disableABKeyboard();
                showFeedback(false, 0, '💀 机会用尽！密码是 ' + password);
                gameState.combo = 0;
                gameState.lives--;
                updateUI();
                if (gameState.lives <= 0) { setTimeout(gameOver, 1500); } else { setTimeout(loadChallenge, 2000); }
                return;
            }

            abState.currentGuess = '';
            updateABDisplay();
            showToast('第 ' + abState.attemptCount + ' 次：' + resultText, 'info');
            return;
        }

        if (abState.currentGuess.length < digitCount) {
            if (abState.currentGuess.indexOf(key) !== -1) {
                showToast('数字不能重复！', 'error'); return;
            }
            if (abState.currentGuess.length === 0 && key === '0') {
                showToast('第一位不能是0！', 'error'); return;
            }
            abState.currentGuess += key;
            updateABDisplay();
        } else {
            showToast('已输入 ' + digitCount + ' 位，请点 ✅ 提交', 'info');
        }
    }

    function updateABDisplay() {
        var displayEl = document.getElementById('abDisplay');
        if (!displayEl) return;
        var displayText = abState.currentGuess.padEnd(abState.digitCount, ' ');
        displayEl.textContent = displayText.split('').join(' ');
    }

    function updateABHistory() {
        var historyEl = document.getElementById('abHistory');
        if (!historyEl) return;
        if (abState.history.length === 0) { historyEl.textContent = '📝 输入你的猜测...'; return; }
        var html = '', items = abState.history.slice(-6);
        for (var i = 0; i < items.length; i++) {
            var isCorrect = items[i].indexOf('A' + abState.digitCount + 'B') !== -1;
            html += '<div class="history-item' + (isCorrect ? ' correct' : '') + '">🔹 ' + items[i] + '</div>';
        }
        historyEl.innerHTML = html;
        historyEl.scrollTop = historyEl.scrollHeight;
    }

    function disableABKeyboard() {
        document.querySelectorAll('.ab-key-btn').forEach(function(b) { b.disabled = true; });
    }

    // ============================================================
    // 找不同
    // ============================================================
    function renderFindDifferent(challenge) {
        var container = dom.gameContent;
        var displayTime = challenge.displayTime || 3;

        var textDiv = document.createElement('div');
        textDiv.className = 'display-text-container';
        textDiv.id = 'findDifferentDisplay';
        textDiv.innerHTML = '<div class="display-text" style="font-size:2.6rem;font-weight:bold;color:#8B5CF6;padding:1rem;margin:0.3rem 0;background:rgba(255,255,255,0.02);border-radius:14px;border:1px solid rgba(255,255,255,0.04);text-align:center;letter-spacing:10px;font-family:monospace;">' + challenge.originalDisplay + '</div>';
        container.appendChild(textDiv);

        dom.timerText.textContent = '⏱ 记忆 ' + displayTime + 's';
        dom.timerFill.style.width = '100%';
        dom.timerFill.className = 'timer-fill';

        var countdownInterval = setInterval(function() {
            displayTime--;
            dom.timerText.textContent = '⏱ 记忆 ' + displayTime + 's';
            var pct = (displayTime / (challenge.displayTime || 3)) * 100;
            dom.timerFill.style.width = pct + '%';
            if (pct < 30) dom.timerFill.classList.add('danger');
            else if (pct < 50) dom.timerFill.classList.add('warning');
            if (displayTime <= 0) {
                clearInterval(countdownInterval);
                var displayEl = document.getElementById('findDifferentDisplay');
                if (displayEl) {
                    var shuffled = challenge.shuffledDisplay || '';
                    var html = '';
                    for (var i = 0; i < shuffled.length; i++) {
                        var ch = shuffled[i];
                        html += '<span class="char-btn" data-index="' + i + '" data-char="' + ch + '" onclick="selectChar(' + i + ', \'' + ch + '\', this)">' + ch + '</span>';
                    }
                    displayEl.innerHTML = '<div class="display-text" style="padding:1rem;margin:0.3rem 0;background:rgba(255,255,255,0.02);border-radius:14px;border:1px solid rgba(255,255,255,0.04);text-align:center;letter-spacing:4px;">' + html + '</div>';
                    displayEl.dataset.correctAnswer = challenge.correctAnswer;
                    displayEl.dataset.shuffledPos = challenge.shuffledPos;
                    dom.timerText.textContent = '⏱ 选择';
                    startTimer(challenge.timeLimit || 10);
                }
            }
        }, 1000);
    }

    function selectChar(index, char, element) {
        if (gameState.isAnswered) return;
        var container = document.getElementById('findDifferentDisplay');
        if (!container) return;
        var shuffledPos = parseInt(container.dataset.shuffledPos);
        var isCorrect = (index === shuffledPos);
        var challenge = gameState.currentChallenge;

        gameState.isAnswered = true;
        clearInterval(gameState.timer);

        var allChars = container.querySelectorAll('.char-btn');
        allChars.forEach(function(el) {
            el.classList.add('disabled');
            el.onclick = null;
            if (parseInt(el.dataset.index) === shuffledPos) el.classList.add('correct-answer');
        });
        if (!isCorrect) {
            element.classList.add('wrong-answer');
            dom.challengeCard.classList.add('shake');
            setTimeout(function() { dom.challengeCard.classList.remove('shake'); }, 500);
        }
        handleAnswerResult(challenge, isCorrect);
    }

    // ============================================================
    // 数字记忆
    // ============================================================
    function renderMemoryGame(challenge) {
        var container = dom.gameContent;
        var memoryTime = challenge.memoryTime || 5;
        var timeLimit = challenge.timeLimit || 15;
        var correctAnswer = challenge.correctAnswer || '';

        container.innerHTML = '';

        var displayDiv = document.createElement('div');
        displayDiv.id = 'memoryDisplay';
        displayDiv.style.cssText = 'font-size:3.2rem;font-weight:bold;color:#8B5CF6;padding:1.5rem;margin:0.3rem 0;background:rgba(139,92,246,0.03);border-radius:14px;border:1px solid rgba(139,92,246,0.04);text-align:center;letter-spacing:16px;font-family:monospace;';
        displayDiv.textContent = challenge.displayNumber || '';
        container.appendChild(displayDiv);

        var statusDiv = document.createElement('div');
        statusDiv.id = 'memoryStatus';
        statusDiv.style.cssText = 'color:rgba(255,255,255,0.1);font-size:0.75rem;text-align:center;margin:0.1rem 0 0.3rem;';
        statusDiv.textContent = '⏱ 记忆 ' + memoryTime + 's';
        container.appendChild(statusDiv);

        var inputDiv = document.createElement('div');
        inputDiv.className = 'memory-input-display';
        inputDiv.id = 'memoryInputDisplay';
        inputDiv.style.display = 'none';
        inputDiv.textContent = '_ '.repeat(correctAnswer.length).trim();
        container.appendChild(inputDiv);

        var keyboardDiv = document.createElement('div');
        keyboardDiv.className = 'memory-keyboard';
        keyboardDiv.id = 'memoryKeyboard';
        keyboardDiv.style.display = 'none';

        var keyboardRows = challenge.keyboardRows || [['1','2','3'],['4','5','6'],['7','8','9'],['','0','⌫']];
        keyboardRows.forEach(function(row) {
            row.forEach(function(key) {
                if (!key) { var empty = document.createElement('div'); keyboardDiv.appendChild(empty); return; }
                var btn = document.createElement('button');
                btn.className = 'key-btn';
                if (key === '⌫') {
                    btn.classList.add('backspace');
                    btn.textContent = '⌫';
                    btn.addEventListener('click', function() { if (gameState.isAnswered) return; memoryBackspace(); });
                } else {
                    btn.textContent = key;
                    btn.addEventListener('click', function() { if (gameState.isAnswered) return; memoryKeyPress(key); });
                }
                keyboardDiv.appendChild(btn);
            });
        });
        container.appendChild(keyboardDiv);

        var controlsDiv = document.createElement('div');
        controlsDiv.className = 'memory-controls';
        controlsDiv.innerHTML = '<button class="btn-memory submit" id="memorySubmitBtn" style="display:none;" onclick="submitMemoryAnswer()">✅ 确认</button>';
        container.appendChild(controlsDiv);

        var memoryState = { input: '', correctAnswer: correctAnswer, isActive: true, isMemorizing: true };
        window._memoryState = memoryState;

        var memInterval = setInterval(function() {
            memoryTime--;
            var statusEl = document.getElementById('memoryStatus');
            if (statusEl) statusEl.textContent = '⏱ 记忆 ' + memoryTime + 's';
            var pct = (memoryTime / (challenge.memoryTime || 5)) * 100;
            dom.timerFill.style.width = Math.max(0, Math.min(100, pct)) + '%';
            if (pct < 30) dom.timerFill.classList.add('danger');
            else if (pct < 50) dom.timerFill.classList.add('warning');
            else dom.timerFill.classList.remove('danger', 'warning');

            if (memoryTime <= 0) {
                clearInterval(memInterval);
                var displayEl = document.getElementById('memoryDisplay');
                if (displayEl) {
                    displayEl.textContent = '✏️ 输入你记住的数字';
                    displayEl.style.color = 'rgba(255,255,255,0.2)';
                    displayEl.style.fontSize = '1.2rem';
                    displayEl.style.letterSpacing = 'normal';
                    displayEl.style.padding = '1rem';
                }
                var statusEl = document.getElementById('memoryStatus');
                if (statusEl) {
                    statusEl.textContent = '⌨️ 输入你记住的数字';
                    statusEl.style.color = 'rgba(255,255,255,0.05)';
                }
                document.getElementById('memoryInputDisplay').style.display = 'block';
                document.getElementById('memoryKeyboard').style.display = 'grid';
                document.getElementById('memorySubmitBtn').style.display = 'inline-block';
                memoryState.isMemorizing = false;
                dom.timerText.textContent = '⏱ ' + timeLimit + 's';
                dom.timerFill.style.width = '100%';
                dom.timerFill.className = 'timer-fill';
                startTimer(timeLimit);
            }
        }, 1000);
    }

    function memoryKeyPress(value) {
        if (gameState.isAnswered) return;
        var state = window._memoryState;
        if (!state || !state.isActive || state.isMemorizing) return;
        var len = state.correctAnswer.length;
        if (state.input.length >= len) return;
        state.input += value;
        updateMemoryDisplay(state);
    }

    function memoryBackspace() {
        if (gameState.isAnswered) return;
        var state = window._memoryState;
        if (!state || !state.isActive || state.isMemorizing) return;
        if (state.input.length > 0) {
            state.input = state.input.slice(0, -1);
            updateMemoryDisplay(state);
        }
    }

    function updateMemoryDisplay(state) {
        var display = document.getElementById('memoryInputDisplay');
        if (!display) return;
        var len = state.correctAnswer.length;
        var text = '';
        for (var i = 0; i < len; i++) {
            text += (i < state.input.length ? state.input[i] : '_') + ' ';
        }
        display.textContent = text.trim();
    }

    function submitMemoryAnswer() {
        if (gameState.isAnswered) return;
        var state = window._memoryState;
        if (!state) return;
        if (state.input.length !== state.correctAnswer.length) {
            showToast('请完整输入所有数字', 'error'); return;
        }
        var isCorrect = state.input === state.correctAnswer;
        var challenge = gameState.currentChallenge;
        document.querySelectorAll('.memory-keyboard .key-btn').forEach(function(b) { b.disabled = true; });
        document.getElementById('memorySubmitBtn').disabled = true;
        state.isActive = false;
        handleAnswerResult(challenge, isCorrect);
    }

    // ============================================================
    // 提交答案
    // ============================================================
    function submitAnswer(challenge, answer, btn) {
        if (gameState.isAnswered) return;
        var isCorrect = answer === challenge.correctAnswer;
        var container = dom.gameContent;
        container.querySelectorAll('.option-btn').forEach(function(b) {
            b.disabled = true;
            if (b.dataset.answer === challenge.correctAnswer) b.classList.add('correct');
        });
        if (!isCorrect) {
            btn.classList.add('wrong');
            dom.challengeCard.classList.add('shake');
            setTimeout(function() { dom.challengeCard.classList.remove('shake'); }, 500);
        }
        handleAnswerResult(challenge, isCorrect);
    }

    // ============================================================
    // 处理答案结果（核心）
    // ============================================================
    function handleAnswerResult(challenge, isCorrect) {
        gameState.isAnswered = true;
        clearInterval(gameState.timer);

        var points = 0;
        if (isCorrect) {
            var bonus = Math.min(gameState.combo, 10);
            points = 10 + Math.floor(gameState.level / 5) + bonus * 2;
            gameState.score += points;
            gameState.combo++;
            gameState.passed++;
            if (gameState.combo > gameState.maxCombo) gameState.maxCombo = gameState.combo;
            showFeedback(true, points, challenge.funMessage);
            gameState.level++;
            SoundFX.play('correct');
        } else {
            gameState.combo = 0;
            gameState.lives--;
            showFeedback(false, 0, '');
            SoundFX.play('wrong');
        }

        updateUI();

        if (gameState.lives <= 0) {
            SoundFX.play('gameover');
            setTimeout(gameOver, 1500);
            return;
        }
        setTimeout(function() { loadChallenge(); }, 1800);
    }

    // ============================================================
    // 计时器
    // ============================================================
    function startTimer(limit) {
        clearInterval(gameState.timer);
        gameState.timeLeft = limit || 15;
        updateTimerDisplay(limit || 15);

        gameState.timer = setInterval(function() {
            gameState.timeLeft--;
            updateTimerDisplay(limit || 15);
            if (gameState.timeLeft <= 0) {
                clearInterval(gameState.timer);
                if (!gameState.isAnswered) {
                    gameState.isAnswered = true;
                    disableAllInteractions();
                    gameState.combo = 0;
                    gameState.lives--;
                    updateUI();
                    showFeedback(false, 0, '⏱ 时间到！');
                    SoundFX.play('timeout');
                    if (gameState.lives <= 0) {
                        SoundFX.play('gameover');
                        setTimeout(gameOver, 1200);
                    } else {
                        setTimeout(function() { loadChallenge(); }, 1500);
                    }
                }
            }
            // 每秒滴答声（最后5秒）
            if (gameState.timeLeft <= 5 && gameState.timeLeft > 0 && !gameState.isAnswered) {
                SoundFX.play('tick');
            }
        }, 1000);
    }

    function updateTimerDisplay(limit) {
        var pct = (gameState.timeLeft / Math.max(1, limit || 15)) * 100;
        dom.timerFill.style.width = Math.max(0, Math.min(100, pct)) + '%';
        dom.timerText.textContent = '⏱ ' + gameState.timeLeft + 's';
        dom.timerFill.className = 'timer-fill';
        if (pct < 30) dom.timerFill.classList.add('danger');
        else if (pct < 50) dom.timerFill.classList.add('warning');
    }

    function disableAllInteractions() {
        var container = dom.gameContent;
        container.querySelectorAll('.option-btn').forEach(function(b) { b.disabled = true; });
        container.querySelectorAll('.color-option').forEach(function(el) { el.classList.add('disabled'); });
        container.querySelectorAll('.ab-key-btn').forEach(function(b) { b.disabled = true; });
        container.querySelectorAll('.puzzle-cell').forEach(function(cell) { cell.onclick = null; });
        container.querySelectorAll('.char-btn').forEach(function(el) { el.classList.add('disabled'); el.onclick = null; });
        container.querySelectorAll('.key-btn').forEach(function(b) { b.disabled = true; });
        document.getElementById('memorySubmitBtn')?.setAttribute('disabled', 'disabled');
    }

    // ============================================================
    // 反馈
    // ============================================================
    function showFeedback(isCorrect, points, funMessage) {
        var el = dom.feedback;
        el.style.display = 'block';
        el.className = 'feedback show ' + (isCorrect ? 'correct' : 'wrong');
        if (isCorrect) {
            el.innerHTML = '✅ 答对了！ +' + points + ' 分' +
                (funMessage ? '<div class="fun-message">' + funMessage + '</div>' : '');
        } else {
            el.innerHTML = '❌ ' + (funMessage || '答错了！连击中断');
        }
    }

    // ============================================================
    // UI 更新
    // ============================================================
    function updateUI() {
        dom.levelDisplay.textContent = gameState.level;
        dom.scoreDisplay.textContent = gameState.score;
        dom.comboDisplay.textContent = gameState.combo;
        dom.passedDisplay.textContent = gameState.passed;
        var hearts = '❤️'.repeat(gameState.lives) + '🖤'.repeat(Math.max(0, 3 - gameState.lives));
        dom.livesDisplay.textContent = hearts || '💀';
        var progress = (gameState.passed / gameState.totalLevels) * 100;
        dom.progressFill.style.width = Math.min(progress, 100) + '%';
    }

    function getDifficultyLabel(level) {
        if (level >= 81) return '👑 传说';
        if (level >= 61) return '💀 地狱';
        if (level >= 41) return '🔥 噩梦';
        if (level >= 21) return '⚡ 困难';
        return '📝 入门';
    }

    // ============================================================
    // Toast
    // ============================================================
    function showToast(message, type) {
        var container = document.getElementById('toastContainer');
        var toast = document.createElement('div');
        toast.className = 'toast-msg ' + (type || 'info');
        toast.textContent = message;
        container.appendChild(toast);
        setTimeout(function() {
            toast.style.opacity = '0';
            toast.style.transform = 'translateX(20px)';
            setTimeout(function() { toast.remove(); }, 300);
        }, 3000);
    }

    // ============================================================
    // 游戏结束
    // ============================================================
    function gameOver() {
        gameState.isPlaying = false;
        clearInterval(gameState.timer);
        var finalScore = gameState.score;
        dom.challengeCard.innerHTML = `
            <div class="game-over">
                <div class="big-icon">${finalScore > 100 ? '🏆' : '💪'}</div>
                <div class="title">${finalScore > 100 ? '🎉 挑战结束！' : '😅 继续加油！'}</div>
                <div class="sub">你成功通过了 ${gameState.passed} 关</div>
                <div class="final-score">${finalScore}</div>
                <div class="stats-grid">
                    <div class="stat-item"><div class="stat-value">${gameState.passed}</div><div class="stat-label">通关</div></div>
                    <div class="stat-item"><div class="stat-value">${gameState.maxCombo}</div><div class="stat-label">最高连击</div></div>
                    <div class="stat-item"><div class="stat-value">${gameState.level - 1}</div><div class="stat-label">到达关卡</div></div>
                </div>
                <button class="btn-restart" onclick="restartGame()">🔄 再来一次</button>
            </div>
        `;
        saveAndShowRanking();
    }

    function gameWin() {
        gameState.isPlaying = false;
        clearInterval(gameState.timer);
        dom.challengeCard.innerHTML = `
            <div class="game-over">
                <div class="big-icon">👑</div>
                <div class="title">🎉 恭喜通关！</div>
                <div class="sub">你通过了全部 100 关！</div>
                <div class="final-score">${gameState.score}</div>
                <div class="stats-grid">
                    <div class="stat-item"><div class="stat-value">100</div><div class="stat-label">通关</div></div>
                    <div class="stat-item"><div class="stat-value">${gameState.maxCombo}</div><div class="stat-label">最高连击</div></div>
                    <div class="stat-item"><div class="stat-value">🏆</div><div class="stat-label">传说</div></div>
                </div>
                <button class="btn-restart" onclick="restartGame()">🔄 再来一次</button>
            </div>
        `;
        saveAndShowRanking();
    }

    function restartGame() { renderStartScreen(); gameState.isPlaying = false; if (gameState.timer) { clearInterval(gameState.timer); } }

    // ============================================================
    // 排行榜（游戏内）
    // ============================================================
    function saveAndShowRanking() {
        var finalScore = gameState.score;
        fetch('/VerifyGame/SaveScore', {
            method: 'POST',
            headers: { 'Content-Type': 'application/x-www-form-urlencoded', 'RequestVerificationToken': token },
            body: 'score=' + finalScore + '&level=' + gameState.level + '&maxCombo=' + gameState.maxCombo + '&passed=' + gameState.passed
        }).then(function() { loadRanking(); });
    }

    function loadRanking() {
        var userId = @(Context.Session.GetInt32("UserId") ?? 0);
        fetch('/VerifyGame/GetRanking')
            .then(function(r) { return r.json(); })
            .then(function(data) {
                var container = dom.rankList;
                var section = dom.rankSection;
                if (data.success && data.data && data.data.length > 0) {
                    var html = '', medals = ['🥇', '🥈', '🥉'];
                    data.data.forEach(function(item, index) {
                        var medal = index < 3 ? medals[index] : '#' + (index + 1);
                        var avatarHtml = item.avatarUrl && item.isAvatarApproved ?
                            '<img src="' + item.avatarUrl + '" class="rank-avatar" />' :
                            '<div class="rank-avatar-placeholder">' + (item.username ? item.username.charAt(0).toUpperCase() : '?') + '</div>';
                        var isMe = item.isMe || false;
                        html += '<div class="rank-item">' +
                            '<span class="rank-medal">' + medal + '</span>' +
                            avatarHtml +
                            '<span class="rank-name">' + item.username + (isMe ? ' <span class="me">(我)</span>' : '') + '</span>' +
                            '<span class="rank-score">' + item.totalPoints + '</span>' +
                            '</div>';
                    });
                    container.innerHTML = html;
                    section.style.display = 'block';
                } else {
                    container.innerHTML = '<div class="rank-empty">暂无排行数据<br>快来挑战吧！</div>';
                    section.style.display = 'block';
                }
            })
            .catch(function() {
                dom.rankList.innerHTML = '<div class="rank-empty">加载排行榜失败</div>';
                dom.rankSection.style.display = 'block';
            });
    }

    // ============================================================
    // 启动
    // ============================================================
    document.addEventListener('DOMContentLoaded', function() {
        SoundFX.init();
        renderStartScreen();
    });
</script>
